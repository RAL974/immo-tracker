{
  "name": "immo-tracker",
  "private": true,
  "description": "Suivi des immobilisations Electricité Services Réunion",
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "test": "node --test",
    "verify": "npm run check && npm test",
    "check": "node --check worker.js && node --check app.js && node scripts/check-dashboard.js",
    "session:start": "node scripts/session-start.js",
    "session:rollback": "node scripts/session-rollback.js"
  }
}
