# Immo Tracker — Historique des décisions

*Journal chronologique des choix structurants et de leur raison d'être. Objectif : ne jamais perdre le "pourquoi" derrière une fonctionnalité. À compléter à chaque évolution notable.*

## Socle applicatif (avant juillet 2026)
- Choix d'une architecture PWA + Dashboard + Worker Cloudflare + SharePoint : zéro coût d'hébergement, propriété totale du code et des données par Electricité Services Réunion.
- Filtres site (Réunion/Mayotte) et badges drapeaux déployés sur toutes les rubriques d'inventaire.
- Authentification dashboard : passage de mots de passe en clair à un hachage PBKDF2/SHA-256 (100k itérations, sel par utilisateur).
- Création du super-admin permanent AIWI, protégé contre toute rétrogradation accidentelle.
- Sécurisation du secret Azure : passage en variable d'environnement Cloudflare chiffrée (au lieu d'être en dur dans le code).
- Filtrage des catégories visibles par rôle/compte comptable, avec l'exception des 19 étiqueteuses BROTHER mal affectées comptablement à l'origine.
- Ajout du rôle Encadrement (RH / services support), view-only, initialement oublié du menu de rôles assignables — corrigé.
- Fonction "sortir un employé" (passage inactif via `field_2`, historique conservé) — bug initial car le code visait à tort une colonne `Actif` inexistante sur la liste Employés ; corrigé.
- Flux de validation des retours dépôt par "garants" (Admin, Logistique, Logistique_Mayotte), avec distinction déclarant terrain / validateur dans l'affichage ("Donneur / Validé par").
- Refactoring des droits : passage de 5 tableaux de rôles séparés à une table centrale unique `ROLE_CAPS`.

## Veille concurrentielle (juillet 2026)
- La direction a demandé d'évaluer des alternatives : **Organilog** (GMAO généraliste, 19-59 €/utilisateur/mois) et **Hector** (gestion d'actifs, dès 19,99 €/mois facturé au nombre d'actifs).
- Constat : Immo Tracker est très économique et sur-mesure, mais avait 3 écarts fonctionnels face à Hector : pas de calcul d'amortissement/VNC, pas d'export comptable, module maintenance incomplet.
- **Décision** : combler ces 3 écarts avant de présenter tout argumentaire à la direction (ne pas plaider une position qu'on n'a pas encore rendue solide).

## Axe 1 — Amortissement & VNC
- V1 : durée par catégorie (table de correspondance compte → durée), avec possibilité de saisie manuelle par immo en cas d'exception, priorité saisie > catégorie > défaut 5 ans.
- Ajout de la colonne SharePoint `Duree_Amortissement`.
- **Révision (juillet 2026)** : William a transmis un barème officiel basé sur le **compte d'amortissement** (et non plus le compte d'immobilisation ni une saisie manuelle). Décision : durée **100% automatique**, plus aucune saisie manuelle. La colonne `Duree_Amortissement` et la fonction `modifierDureeAmort` ont été retirées de la logique (colonne SharePoint conservée mais inerte, peut être supprimée).
- VNC affichée sur chaque fiche immo + panorama consolidé "Valeur du parc" par site dans l'onglet Analyses.

## Axe 2 — Export comptable
- Export CSV 18 colonnes, format Excel français, réservé aux admins, accessible depuis Analyses.
- Objectif explicite : permettre le rapprochement avec EBP et détecter les incohérences (ex. véhicules mal localisés entre sites).

## Axe 3 — Maintenance préventive
- Remplacement de l'ancien onglet Maintenance (qui dépendait de colonnes SharePoint jamais renseignées) par un panneau **entièrement calculé** à partir des données déjà présentes : garanties estimées (2 ans après mise en service), immos à entretenir (état dégradé ou pannes récurrentes), immos dormantes.
- Règle métier précisée par William : une immo sans aucun mouvement enregistré n'est jamais comptée comme dormante, quelle que soit son ancienneté.

## Migration des données comptables EBP → SharePoint (juillet 2026)
- Constat : SharePoint ne contenait pas encore date de mise en service, comptes amortissement/dotation — ces données existaient uniquement dans l'export comptable EBP (`Export_immos_030726_avec_sites.xls`, 1167 immos, dont 1023 actives correspondant exactement aux 1023 immos SharePoint).
- Le fichier "avec_sites" corrige au passage la localisation Réunion/Mayotte de plusieurs immos (notamment des véhicules mal localisés depuis longtemps).
- 4 colonnes SharePoint créées : `Date_Mise_Service`, `Compte_Immobilisation`, `Compte_Amortissement`, `Compte_Dotation`.
- Outil de migration one-shot dans le dashboard (`enrichirImmosEBP()`), écriture par lots de 20 via Graph `$batch`.
- **Incident de déploiement** : confusion entre `immos.json` (tableau, catalogue courant) et `immos_full.json` (objet, migration), plus un problème de casse dans le nom de fichier uploadé sur GitHub → 404 et affichage cassé (codes affichés au lieu des libellés). Résolu ; leçon retenue dans `01_ARCHITECTURE_TECHNIQUE.md`.
- Migration exécutée avec succès : les 1023 immos actives enrichies, 0 orpheline.

## Affinage du filtre "immo d'activité" (juillet 2026)
- Un premier essai de filtre appliqué à tort sur l'onglet "Au dépôt" a cassé l'affichage ("8 immos" au lieu de ~800) à cause d'une lecture de compte comptable pas encore fiable (colonnes SharePoint pas encore migrées à ce stade). Corrigé en priorisant la lecture depuis `immos.json` (toujours fiable) plutôt que `immoMeta` (SharePoint, alors incomplet).
- Clarification de la demande réelle de William : exclure des **statistiques d'Analyses** (pas de l'inventaire "Au dépôt" qui doit tout montrer) les immos que les conducteurs de travaux ne voient pas normalement, en réutilisant exactement la même logique de comptes administratifs que la PWA.
- Le filtre a finalement été appliqué aux deux endroits une fois la cause du bug comprise : "Au dépôt" (avec bouton pour révéler les immos administratives) et toutes les stats d'Analyses (top mouvementées, pannes, taux d'utilisation par catégorie, coûts réparation).

## Affichage des données comptables sur les fiches (juillet 2026)
- Après la migration EBP réussie, ajout d'un bloc "🧾 Données comptables" sur les fiches immo (date mise en service, 3 comptes, N° série), visible uniquement par Admin, Gestionnaire Dépôt RUN et Encadrement (`peutVoirCompta()`).

## Documentation et gouvernance (juillet 2026)
- Rédaction de `Immo_Tracker_Documentation.docx` (documentation de pérennité, destinée à un repreneur potentiel de la solution).
- Rédaction de `Note_Synthese_Immo_Tracker.docx` (note comparative pour la direction, avec tarifs Organilog/Hector vérifiés par recherche web en juillet 2026).
- Passage en gestion "mode projet" avec base de connaissance structurée (ce jeu de documents) pour sécuriser la continuité malgré la dépendance à une seule personne.

## Coûts de réparation structurés & seuil de réforme (août 2026)
- Besoin exprimé par William (repris de `05_ROADMAP_EVOLUTIONS_FUTURES.md`, item C) : le ratio coût réparations / valeur d'achat affiché dans Analyses était calculé en extrayant un montant du texte libre de la `Note` d'un mouvement — fragile, et basé uniquement sur la valeur d'achat (pas la VNC).
- **Décisions prises avec William avant développement** : (1) coût stocké dans une colonne SharePoint dédiée `Cout_Reparation` sur `Mouvements` (une ligne = une réparation datée, donc plusieurs réparations sur la même immo = plusieurs lignes, cumul par simple somme) ; (2) le coût doit pouvoir être saisi non seulement à la résolution d'une panne, mais aussi indépendamment (entretien préventif) → nouveau type de mouvement `Entretien` ; (3) les **deux ratios** (valeur d'achat ET VNC) sont calculés et affichés, pas un seul ; (4) le seuil de déclenchement (60% suggéré) est **réglable** dans le dashboard (40/60/80%), sur le même principe que le seuil de dormance déjà existant.
- Distinction `Réparation` (résolution de panne, l'immo revient au dépôt) vs `Entretien` (coût structuré indépendant, sans effet sur la localisation courante de l'immo) — évite qu'un simple enregistrement de coût d'entretien fasse apparaître à tort une immo comme "revenue au dépôt" alors qu'elle est toujours sur un chantier.
- **Rétrocompatibilité** : les réparations enregistrées avant l'ajout de la colonne `Cout_Reparation` restent comptées, via un repli automatique sur l'ancien format texte (`##COUT:X##` dans `Note`) quand la colonne structurée est vide.
- **Changement d'infrastructure majeur au passage** : William a connecté le Worker Cloudflare `immo-proxy` au dépôt GitHub `RAL974/immo-tracker` (build automatique sur push). Cela lève la contrainte historique qui empêchait de committer `worker.js` (voir `01_ARCHITECTURE_TECHNIQUE.md`) : le fichier est désormais versionné normalement, accompagné d'un `wrangler.toml`. Premier déploiement Git testé et vérifié en lecture (endpoints `?debug_mouvements=1` et `?dashboard=1`) avant validation par William en conditions réelles.
- Bugs corrigés au passage : l'ancienne liste "Candidats au remplacement" ne regardait que les 8 immos aux coûts cumulés les plus élevés (plus les immos en panne), ce qui pouvait faire passer à côté d'une immo à faible valeur d'achat mais à ratio élevé ; élargi à toutes les immos concernées.

## Campagne d'inventaire de stock (articles/consommables — août 2026)
- Repris de la roadmap (`05_ROADMAP_EVOLUTIONS_FUTURES.md`, item A), mais **le besoin initialement décrit était en fait une confusion** : la roadmap parlait de scanner les QR codes des immobilisations, mais William a clarifié que ce chantier (QR codes immos) est un projet **séparé, non encore validé par la direction**. Le vrai besoin exprimé était un inventaire annuel du **stock d'articles/consommables** (visserie, câbles, appareillage électrique…), sans code-barres ni QR existants, et sans aucune liste de référence suivie au quotidien.
- Seul point de départ disponible : l'inventaire manuel mené fin décembre 2025 (fichier Excel `INV 226`, ~2417 lignes de comptage physique, valorisation totale 864 033 € à l'époque). La procédure `LS04V2 - Procédure immobilisations.docx` fournie en référence s'est avérée décrire le circuit des immobilisations (existant), pas ce comptage de stock — les deux sujets ne doivent pas être confondus.
- **Décisions de cadrage prises avec William avant développement** :
  - Portée : outil de **campagne ponctuelle** (pas de suivi de stock continu — rien ne justifiait de tracer chaque sortie/entrée au quotidien).
  - Comptage **manuel uniquement** (pas de code-barres/QR sur les articles) ; **aucune valorisation** dans cette version — seules les quantités sont comparées, car la base de prix historique évolue trop vite pour être fiable en l'état.
  - Périmètre : dépôt/atelier **et** chantiers actifs (le fichier déc. 2025 montrait 1891 lignes en dépôt/atelier vs 527 rattachées à un chantier précis).
  - Les données brutes de décembre 2025 sont importées comme première campagne clôturée, pour permettre un calcul d'écart dès la 2e campagne.
  - Saisie ouverte à un périmètre large : gestionnaires dépôt, CT/RA pour leur propre chantier, **et personnel temporaire d'agence d'intérim** — d'où la création d'un nouveau rôle allégé `Compteur_Inventaire` (voir `03_REGLES_METIER_ET_ROLES.md`).
  - Corrections : auteur ou admin, tant que la campagne est "En cours" ; figée à la clôture (comme un PV d'inventaire signé).
- **Conséquence technique** : comme le personnel temporaire n'a pas de carte BTP imprimée, une option "Saisir mon code manuellement" a été ajoutée à l'écran d'activation de la PWA (`activationManuelle()` dans `app.js`), en plus du scan QR existant.
- Implémentation : 2 nouvelles listes SharePoint (`Campagnes_Inventaire`, `Lignes_Inventaire`), 7 nouvelles actions Worker, nouvel écran PWA ("📋 Campagne d'inventaire"), nouvel onglet dashboard ("📦 Inventaire stock") avec rapport d'écart (Confirmé / Écart quantité / Manquant / Nouveau ou retrouvé) et export CSV.
- **Incident au premier essai d'import** : la migration décembre 2025 s'est terminée sans erreur visible mais avec 0 ligne réellement écrite — SharePoint rejetait le champ `Fabriquant` ("not recognized"). Diagnostiqué via un nouvel endpoint de debug (`?debug_inventaire_columns=1`, sur le même principe que les `debug_*` existants) qui liste les noms internes réels des colonnes. Cause : la colonne avait été recréée sous le nom `Fabricant` (orthographe standard, différente du nom `Fabriquant` initialement spécifié — le fichier Excel d'origine de déc. 2025 avait justement les deux orthographes sur deux colonnes distinctes, source de la confusion). Code et documentation alignés sur `Fabricant`. Leçon retenue : en cas d'erreur "Field 'X' is not recognized", vérifier le nom **interne** de la colonne (peut différer du nom affiché après un renommage), pas seulement le nom visible dans l'interface SharePoint.
- **Affinage du rapport d'écart (août 2026)** : le premier jet plafonnait l'affichage à 300 lignes sans filtre, et fusionnait à tort toutes les lignes sans référence sous une même clé de rapprochement (faussant les quantités comparées). Corrigé : les lignes sans référence sont désormais exclues du rapprochement automatique et listées à part dans une section dédiée ("Sans référence", non classées) ; ajout de filtres combinables (recherche texte, site, statut, "sans désignation uniquement") et d'un sélecteur d'affichage (100/300/1000/Tous) avec bouton de réinitialisation — reproduit à l'identique dans l'export CSV. À cette occasion, un bouton "🔄 Réinitialiser" a aussi été ajouté à tous les autres onglets ayant des filtres (Circulation, Dépôt, Historique, Transferts, Réservations, Documents, Utilisateurs), qui n'en avaient pas jusque-là.
- **Bug de recherche corrigé (août 2026)** : la recherche du rapport d'écart perdait le focus à chaque caractère tapé (un seul caractère s'appliquait à la fois), rendant la recherche inutilisable en pratique. Cause : la fonction de rendu reconstruisait tout le panneau — y compris le champ de recherche lui-même — à chaque frappe (`oninput`), ce qui détruit et recrée le `<input>` et casse le focus. Contrairement aux autres onglets (Circulation, Dépôt…) qui ne redessinent que la zone de résultats, jamais les champs de filtre. Corrigé en séparant la "coquille" (construite une seule fois à l'ouverture d'une campagne) des résultats (recalculés à chaque frappe dans une zone dédiée, sans toucher aux champs de filtre). *Piste initialement explorée par erreur* : une suspicion de corruption d'encodage UTF-8 (accents devenus illisibles côté SharePoint) s'est révélée être un faux positif causé par les propres outils de diagnostic de l'assistant (terminal Windows mal configuré pour l'UTF-8) — les données réelles n'ont jamais été corrompues. Leçon retenue : toujours valider un diagnostic d'encodage avec une méthode de lecture fiable (fichier + décodage explicite) avant de conclure à un bug de données.

## Pivot majeur : état du stock plutôt qu'écart entre campagnes (août 2026)
- **Retour terrain de William après le premier déploiement** : le rapport d'écart (comparaison à la campagne précédente) ne correspond pas au métier. Electricité Services Réunion est une entreprise du bâtiment : pas de stock minimum à tenir, la nature des chantiers varie (tertiaire vs logements neufs), des chantiers se terminent et d'autres démarrent — **il n'y a donc aucun rapport de continuité entre le stock de décembre 2025 et celui d'aujourd'hui**. Comparer les deux n'a pas de sens et produisait un rapport trompeur (tout apparaissait "Manquant" ou "Nouveau" sans que ça ne signifie quoi que ce soit).
- **Décision** : suppression complète de la logique d'écart/comparaison entre campagnes (`calculerEcartInventaire`, statuts Confirmé/Écart quantité/Manquant/Nouveau). Remplacée par un simple **état du stock à l'instant T** : pour la campagne consultée, un tableau regroupé par `(Zone, Site, Référence)` avec les quantités comptées, sans aucune référence à une campagne antérieure. Objectif clarifié par William : savoir ce qui est en stock maintenant, pour pouvoir le valoriser ensuite (en externe, pas dans l'outil).
- **Zone vs Chantier, deux axes différents** : le champ "Lieu" du rapport (qui fusionnait Zone et Chantier en un seul affichage) a été supprimé au profit de deux colonnes distinctes. Clarification de William : la **zone dépôt est obligatoire** (c'est l'unique axe de comptage — "nous nous attachons uniquement à compter le matériel par zone") et correspond à un emplacement physique précis (n° de rack, lettre d'étage) ; le **chantier reste optionnel**, simple information complémentaire quand on sait où l'article ira, pas un second périmètre de comptage indépendant comme envisagé initialement. La clé de regroupement de l'état du stock est donc passée de `(Référence, Site, Chantier ou Zone)` à `(Zone, Site, Référence)`.
- **Ajout du scan de code-barres fabricant** : pour accélérer la saisie sur les grandes marques (Legrand, Hager, Schneider Electric, puis Eurohm, SIB, BLM, Clareo, Courant...), qui ont presque toujours un code-barres (EAN) sur l'emballage. Réutilise le scanner déjà en place dans la PWA pour les immos (`Html5Qrcode`, qui détecte nativement QR ET codes-barres 1D sans configuration supplémentaire) : le code scanné remplit directement le champ Référence. La saisie manuelle reste toujours possible (motif de William : "il n'y a pas toujours la boîte", donc pas toujours de code-barres disponible sur le terrain).
- **Bug corrigé au passage** : "Sans référence" affichait 0 alors que des lignes en étaient dépourvues. Cause : ce compteur était calculé dans le bloc de comparaison à la campagne précédente, qui ne s'affichait donc que pour une campagne ayant une "précédente" — si l'utilisateur consultait une campagne sans précédente (ex. la toute première), ou une campagne vide comparée à une campagne bien remplie, le chiffre affiché ne reflétait pas ce qu'on croyait regarder. Résolu de fait par la suppression de toute la logique de comparaison : "Sans référence" est désormais calculé directement à partir des lignes de la campagne consultée, sans dépendance à une autre campagne.

## Ergonomie de saisie zone par zone (août 2026)
- Retour terrain de William après la mise en place de l'état du stock : le comptage se fait physiquement **zone par zone** (on reste devant un rack/un étage et on scanne/saisit tous les articles qui s'y trouvent avant de passer au suivant), mais le formulaire effaçait la zone après chaque ligne ajoutée — obligeant à la retaper à chaque article, contraire au geste réel sur le terrain.
- **Décision** : le champ Zone n'est plus effacé automatiquement après l'ajout d'une ligne (seuls chantier/fabricant/référence/désignation/quantité/chute de câble/observations le sont) ; il n'est vidé qu'à l'ouverture de l'écran ou via un bouton explicite "🔁 Changer" à côté du champ, pour signaler le passage à la zone suivante. Le focus revient automatiquement sur le champ Référence après l'ajout d'une ligne, pour enchaîner directement sur l'article suivant de la même zone sans re-cliquer.
- La zone reste **obligatoire** à la soumission (contrôle déjà en place, conservé).

## Module EPI — dotation & stock (août 2026)
- Besoin exprimé par William : automatiser la gestion des EPI (équipements de protection individuelle — pantalons, t-shirts, vestes, chaussures, gants, casques...), aujourd'hui gérée via 2 fichiers Excel séparés (`Liste EPI.xlsx` : personnel/tailles/catalogue, `1.3.3.4. EPI - Dotations.xlsx` : calculateur de commande + modèle de fiche papier) et une fiche de dotation remplie/imprimée manuellement, sans stock suivi.
- **Cadrage validé avec William avant développement** (questions posées, réponses retenues) : (1) **stock vivant** par (article, taille), décrémenté à l'émargement d'une fiche et incrémenté à la réception d'une commande — pas un simple suivi de distribution sans compteur ; (2) **grille de dotation standard éditable depuis le dashboard** (pas figée dans le code comme le barème d'amortissement), pour que William puisse l'ajuster lui-même si la politique EPI change ; (3) **fiche générée en HTML imprimable** + bouton "Imprimer / Enregistrer en PDF" via l'impression navigateur, plutôt qu'une librairie PDF externe — zéro nouvelle dépendance ; (4) **remise ponctuelle allégée** (destinataire en texte libre, ex. "Équipe de untel"), distincte du suivi de dotation annuelle individuelle ; (5) **émargement = photo/scan de la fiche signée jointe**, comme les FDS des immobilisations.
- **Analyse des données source** : le fichier `Liste EPI.xlsx` a révélé que les quantités par article dans l'onglet "Liste Personnel" sont en réalité entièrement déterminées par la colonne `Affectation` (C = Chantier/Travaux Neufs, M = Maintenance, Z = Conducteur de Travaux, A = Atelier) — 4 profils fixes, pas une personnalisation individuelle. Ceci a simplifié le modèle : une grille à 4 lignes plutôt qu'une dotation calculée par salarié.
- **Point de vigilance signalé puis levé** : la table de correspondance taille salarié → référence article (onglet "Correspondance tailles articles") présentait des apparences d'incohérence avec le catalogue (ex. taille 40 pointant vers la référence de la taille S, tailles 46 et 48 partageant la même référence). **William a confirmé que cette table est correcte telle quelle** ("mes correspondances sont correctes et valables par rapport aux articles et tailles des gars") — importée sans modification, leçon retenue : ne pas "corriger" des données métier sur la base d'une supposition technique sans validation du terrain.
- **Complément du catalogue** : les 2 articles signalés incomplets par William (T-shirt manches longues "MOTECARLO LS" rouge, Polo "IMPERIUM") ont été complétés avec les 5 tailles S/M/L/XL/XXL demandées, en réutilisant la même référence fournisseur pour toutes les tailles (cohérent avec la table de correspondance source, qui listait déjà ces 5 tailles pour ces 2 articles).
- **Décision de conception non re-demandée à William, actée dans le plan puis confirmée par son approbation globale** : le module est **100% piloté depuis le dashboard**, sans nouvel écran PWA — la génération des fiches, l'émargement et la réception de stock sont des actions de gestion (Logistique), pas des actions terrain des salariés eux-mêmes.
- **Réutilisation systématique de patterns existants** plutôt que nouvelle mécanique : le pipeline d'upload de fichier vers SharePoint (`upload_photo`, déjà utilisé pour les photos d'immos) a été dupliqué tel quel pour les photos de fiches EPI signées (dossier `Fiches_EPI` dédié) ; le modèle liste parent/liste enfant sans colonne Lookup (`Campagnes_Inventaire`/`Lignes_Inventaire`) a servi de gabarit pour `Dotations_EPI`/`Lignes_Dotation_EPI` ; l'écriture par lots `$batch` (`bulk_patch_immos`, `bulk_import_lignes_inventaire`) a servi pour l'import initial des 73 salariés, du catalogue (~58 références) et de la grille de dotation.
- **Stock initial à 0** : faute de comptage fiable du stock EPI actuel au moment du développement, le stock de chaque référence démarre à 0 après la migration initiale — à William de saisir une réception initiale correspondant à son stock physique réel via l'écran "Réception commande", plutôt que d'inventer un chiffre de départ.
- **Étapes bloquantes côté William avant mise en service** : création de 4 nouvelles listes SharePoint (`Catalogue_Articles_EPI`, `Grille_Dotation_EPI`, `Dotations_EPI`, `Lignes_Dotation_EPI`) et de 6 nouvelles colonnes sur `Employes` (`Affectation_EPI` + 5 tailles) — comme pour chaque évolution précédente ayant nécessité de nouvelles listes.

## Retours terrain après mise en production du module EPI (août 2026)
- **Colonne manquante découverte au déploiement** : `Dotations_EPI` créée par William sans les colonnes `Genere_Le`/`Emarge_Le` (Date/heure) — repéré via un nouvel endpoint générique `?debug_columns=<liste>` (généralisation de `?debug_inventaire_columns=1`), conservé comme outil réutilisable pour tout futur diagnostic de colonne. Corrigé côté SharePoint par William, sans impact sur le code.
- **Bug de correspondance taille "Gants"** : la grille de dotation avait été importée avec "Gants a picot"/"Gants gros oeuvre" (sans accent, tirés de l'onglet "Liste Personnel") alors que le catalogue utilise "Gants à picot"/"Gants gros œuvre" (avec accent, tiré de l'onglet "Correspondance tailles articles") — deux onglets source avec une graphie différente pour le même article réel. La comparaison stricte de chaîne de caractères échouait silencieusement (aucune ligne générée pour cet article). Corrigé dans le code et recréé en base avec la bonne orthographe.
- **5 articles universels absents de la grille** : Casque anti-bruit, Casque de chantier, Jugulaire pour casque, Lunettes incolores, Lunettes fumées existaient dans le catalogue mais n'apparaissaient dans aucune colonne de quantité par profil du fichier source (donnés en quantité 1 à tous, donc jamais isolés comme variable dans le tableur d'origine). **William a confirmé** qu'ils sont donnés en quantité 1 pour les 4 profils sans exception — ajoutés à la grille (60 lignes au total). A nécessité d'ajouter la notion d'"article à taille unique" (sans champ de taille employé associé) dans la logique de génération de fiche.
- **Fiche imprimable jugée trop sommaire** : William a demandé un rendu plus proche du document Excel d'origine, logo inclus. Refonte avec le logo Electricité Services Réunion (réutilisation de `logo.png` déjà hébergé), bandeau de titre, encadré destinataire structuré, tableau avec en-tête colorée.
- **Accès à la fiche émargée depuis le profil employé** : le flux d'émargement (upload photo) existait déjà mais n'était accessible que depuis l'onglet EPI. William voulait pouvoir cliquer directement depuis la fiche employé pour voir la preuve signée ou émarger une fiche en attente — ajouté comme lien contextuel dans le bloc EPI de la fiche employé (et dans les tableaux Dotations/Ponctuelle pour cohérence).

## Module Prime d'outillage (août 2026)
- Besoin exprimé par William : intégrer le suivi de la "prime d'outillage" (kit d'outils remis en nature aux salariés de terrain), aujourd'hui géré via `Détail prime d'outillage.xlsx` (3 onglets : Tx Neufs, Maintenance, Commande globale) et des fiches PDF signées individuelles déjà classées. Besoin explicite : voir facilement ce qui a été distribué et ce qui reste à distribuer, prix des articles intégré.
- **Analyse du fichier source** : structure en matrice (une ligne = un outil, une colonne = un employé, cellule 0/1 = reçu ou non) par service, plus un onglet `Commande globale` donnant quantités commandées/reçues et prix négocié par article. Le calcul du stock disponible (`Total reçu` − quantités déjà distribuées comptées dans les onglets par service) a été vérifié manuellement sur plusieurs lignes avant d'être retenu comme fiable (ex. Pince ampermétrique 600V : 63 reçus, 20 distribués, 0 en Tx Neufs → 43 en stock, cohérent avec la colonne `Surplus` du fichier).
- **Fiches PDF déjà signées repérées** (`Documents\Dotaçaos\Prime d'outillage - Dotations - <CODE>.pdf`) : même gabarit que les fiches EPI — a confirmé la pertinence de réutiliser directement le pipeline émargement + photo déjà construit pour les EPI, plutôt que d'inventer un nouveau mécanisme.
- **Cadrage validé avec William avant développement** : (1) périmètre = exactement les employés des 2 onglets `Tx Neufs`/`Maintenance` (William a précisé que ces listes contiennent déjà des CT et de l'Atelier — le service outillage est une dimension indépendante de `Affectation_EPI`, pas une redite) ; (2) **dotation ponctuelle et complétable**, sans cycle annuel comme les EPI (les outils sont durables, donnés une fois) ; (3) **stock vivant**, initialisé depuis les quantités déjà reçues du fichier source plutôt qu'à 0 (donnée fiable disponible, contrairement aux EPI où le stock de départ était inconnu) ; (4) source unique `Détail prime d'outillage.xlsx`, les autres fichiers `Prime d'outillage*.xlsx` repérés dans les Documents de William ignorés (probables exports antérieurs).
- **Modèle simplifié par rapport aux EPI** : une seule liste plate `Lignes_Outillage` (pas de fiche/lignes séparées avec statut Generee/Emargee) — la distribution est actée directement à l'émargement, sans étape intermédiaire, ce qui évite le risque de fiches "orphelines" jamais signées qui aurait faussé le stock.
- **Anomalie de données signalée sans être corrigée arbitrairement** : le code `ELSA` apparaît avec des données réelles distinctes dans les deux onglets de service (Tx Neufs ET Maintenance). Plutôt que de deviner lequel est le bon, les deux jeux de lignes ont été importés tels quels ; le modèle de données n'impose pas un service unique par employé côté `Lignes_Outillage`, seul le champ `Service_Outillage` sur `Employes` (utilisé pour le calcul de la grille cible) a dû recevoir une valeur unique — le premier service rencontré (Travaux Neufs) a été retenu par défaut, à corriger par William si ce n'est pas le bon.
- **Réutilisation systématique** : mêmes patterns que les EPI (upload de fichier vers un dossier drive dédié `Fiches_Outillage`, écriture par lots `$batch`, gabarit de fiche imprimable HTML avec logo) — quasiment aucune nouvelle mécanique inventée pour ce module.
- **Étapes bloquantes côté William avant mise en service** : création de 3 nouvelles listes SharePoint (`Catalogue_Outillage`, `Grille_Outillage`, `Lignes_Outillage`) et d'une colonne `Service_Outillage` sur `Employes`.

## Retour terrain Prime d'outillage : durée d'amortissement & prime annuelle (août 2026)
- **Bug signalé après mise en production** : William a constaté que la vue "Grille par service" n'affichait que 37 articles sur 41 (ex. Tenaille russe, Règle à échelle triangulaire absentes). Cause identifiée : la grille avait été construite à partir des colonnes de quantité des onglets opérationnels `Tx Neufs`/`Maintenance`, qui n'incluaient pas 4 articles pourtant présents dans le catalogue (ceux-ci avaient une quantité cible dans l'onglet `Commande globale` du fichier source, mais n'étaient jamais devenus des lignes de suivi dans les feuilles opérationnelles) — pas un bug de code, une incomplétude du fichier source utilisé pour la première migration.
- **Nouveau fichier de référence transmis** : `1.6.5.115. Kit outillage électricien et EPI.xlsx` (onglet "Liste matériel retenu") — le document de conception initial du kit, avec une colonne **durée d'amortissement retenue** par article et une note explicite *"Versement prime sur la paie du mois de décembre"*. Révèle que la prime d'outillage n'est pas qu'un suivi physique : c'est aussi une **prime monétaire versée en paie**.
- **Vérifications avant d'agir** : les 41 désignations et références du fichier "Kit" correspondent exactement à celles déjà en base (aucune recréation nécessaire) ; le diff entre l'ancienne grille et celle recalculée depuis le fichier Kit ne montre que 4 ajouts, 0 suppression, 0 changement de quantité — correction purement additive.
- **Formule de la prime annuelle vérifiée** : `Prix_Unitaire × 12 / Duree_Amortissement_Mois`, recoupée sur plusieurs lignes du fichier (ex. jeu de tournevis 24,90€/60 mois → 4,98€/an, exact) — appliquée par employé en sommant sur les outils **réellement reçus** (pas la grille cible).
- **2 lignes ignorées sur demande de William** : le fichier contenait 2 pistes de devis non tranchées ("A faire chiffrer par Leroy Merlin Le Port") — une 2e référence pour "Pince coupante de précision" et un "Tire-fil Polyester Tressé" alternatif — non intégrées, le catalogue actuel reste la référence tant que ces devis ne sont pas conclus.
- **Choix de conception** : la prime annuelle est calculée entièrement à la volée (aucun montant stocké) à partir des données déjà en base — cohérent avec le reste du module (pas de snapshot persistant) ; export CSV fourni pour la transmission au service paie, qui sert de trace externe si un historique est nécessaire.

## Relevé de matériel à restituer, sortie d'employé (août 2026)
- Besoin exprimé par William : pouvoir générer automatiquement, au départ d'un salarié, une liste regroupant tout le matériel qui lui a été attribué ou qu'il n'a pas rendu — immobilisations **et** prime d'outillage.
- **Exclusion explicite des EPI demandée par William** : "on ne peut pas les reprendre malheureusement" — les vêtements/protections ne sont pas repris au départ d'un salarié, donc cette catégorie ne doit pas apparaître sur le relevé. Une mention explicite l'indique sur le document généré pour éviter toute ambiguïté.
- **Réutilisation des données déjà suivies, aucun nouveau modèle** : le relevé combine simplement ce qui est déjà tracké — immobilisations actuellement en possession (même source que le bloc "En sa possession" de la fiche employé) et outils reçus (`Lignes_Outillage`, qui n'a pas de notion de "restitution" donc la totalité des outils déjà reçus y figure naturellement, cohérent avec l'objectif "ce qu'il n'a pas rendu"). Gabarit imprimable identique aux autres fiches (logo, bandeau, tableaux) pour rester cohérent visuellement.
- **Pas de suivi de restitution implémenté** : le document a une case "Restitué" vierge à cocher à la main lors de la collecte physique — aucune donnée de retour n'est enregistrée dans l'app à ce stade, ce point n'a pas été demandé et n'a donc pas été construit (à réévaluer si le besoin se confirme).
- Accessible à tout moment depuis la fiche employé (pas seulement au moment de la bascule "Inactif"), pour permettre de préparer la collecte avant la sortie effective.

## Ajout d'articles EPI, import Excel du stock & pagination "Au dépôt" (août 2026)
- Besoin exprimé par William : pouvoir ajouter un nouvel article au catalogue EPI en cours d'année (ex. nouveau modèle de chaussures acheté en promotion) sans repasser par une migration, et de même ajouter un article à la grille de dotation annuelle standard — jusque-là ces deux listes n'étaient modifiables que via l'import initial ou l'édition des quantités déjà existantes, pas la création.
- **Stock EPI — bouton "➕ Ajouter article"** : formulaire (type d'article, taille salarié/affichage, référence, désignation, fournisseur, stock initial) → nouvelle action Worker `ajouter_article_epi` (création simple, une ligne), par opposition aux imports `bulk_import_catalogue_epi` réservés à la migration initiale.
- **Grille de dotation — bouton "➕ Ajouter article"** : ajoute un nouveau type d'article à la grille (ligne vide, quantités à 0 pour les 4 profils), réutilise l'action `maj_grille_dotation_epi` déjà existante (upsert) plutôt qu'une nouvelle action dédiée — les quantités par profil restent ensuite éditables directement dans le tableau comme avant.
- **Import Excel du comptage de stock EPI** : au lieu de ressaisir manuellement chaque quantité après un inventaire physique des EPI, un fichier Excel (colonnes "Référence" + "Quantité"/"Stock", détection flexible de l'en-tête) peut être importé depuis la vue Stock. Réutilise la bibliothèque SheetJS déjà chargée dans le dashboard (jusque-là utilisée uniquement pour l'export du rapport complet Immos) — aucune nouvelle dépendance. Un aperçu (références reconnues/non reconnues, ancien vs nouveau stock) est affiché avant validation ; la quantité importée **remplace** le stock actuel (c'est un comptage, pas une réception à additionner — différence volontaire avec l'action `reception_commande_epi` existante). Nouvelle action Worker `bulk_maj_stock_epi` (PATCH par lots de 20, même pattern `$batch` que les autres imports).
- **Pagination de l'onglet "Au dépôt"** : retour terrain de William — en cherchant des immos achetées à une date précise, la limite d'affichage (50/100/200/Tous) sans possibilité de "page suivante" rendait la recherche peu pratique au-delà de la première page. Ajout de boutons Précédent/Suivant sous le tableau, la page revenant automatiquement à 1 dès qu'un filtre, un tri ou la taille de page change.
- Aucun de ces trois ajouts ne nécessite de nouvelle liste ou colonne SharePoint côté William — déployables immédiatement.

## Ajout d'immo/affectation depuis "Au dépôt", réservation manuelle & ajout de FDS (août 2026)
- **"Ajouter une immobilisation" dupliqué dans "Au dépôt"** : le formulaire existait déjà (onglet Utilisateurs) mais William devait changer d'onglet pour l'utiliser en travaillant sur le dépôt — même bouton, même formulaire (`ouvrirAjoutImmo()`), juste un deuxième point d'entrée là où le besoin se présente naturellement.
- **Import fichier "Au dépôt"** : besoin exprimé par William de pouvoir importer un fichier pour soit ajouter des immos en masse, soit renseigner l'affectation courante (qui détient quoi) d'immos déjà suivies — **un seul upload pour les deux cas**, le code IM (toujours présent dans les fichiers de William) faisant foi pour distinguer une création d'une mise à jour d'affectation. Anti-doublon **garanti côté Worker** (l'action `ajouter_immo` fait déjà une vérification bloquante avant toute création, réutilisée telle quelle) — jamais de doublon même en cas de ré-import du même fichier.
  - Code IM déjà connu → nouvelle action Worker `importer_affectation_immo` : crée directement un Mouvement (Transfert vers le détenteur indiqué, ou Retour vers DEPOT), **hors du flux "en attente de validation"** habituel — cohérent avec le fait qu'il s'agit d'une correction/reprise de données, pas d'un vrai transfert terrain à faire valider par quelqu'un.
  - Code IM inconnu → création via `ajouter_immo` (déjà existant) ; si le fichier indique aussi un détenteur ou un état, un mouvement initial est également créé dans la foulée pour que l'immo n'apparaisse pas à tort comme "Jamais sorti"/"Inconnu".
  - Aperçu de contrôle avant validation (créations / affectations / lignes ignorées avec motif), en-têtes de colonnes détectés de façon souple (accents et casse ignorés).
- **"Ajouter une réservation" dans le dashboard** : besoin de William — pouvoir enregistrer une réservation pour le compte de quelqu'un directement depuis le dashboard, le réseau étant parfois indisponible sur le terrain (à Mayotte en particulier), et en attendant la généralisation complète du flux PWA. Réutilise l'action Worker `reserver` déjà existante (celle utilisée par la PWA) — aucune nouvelle mécanique côté Worker. Réservé à Admin/Logistique/Logistique_Mayotte (même population que les garants), avec la même vérification de conflit de dates que la PWA avant enregistrement.
- **"Ajouter FDS" dans Documents** : jusque-là, aucune FDS ne pouvait être ajoutée depuis l'app — `FDS_URL` devait être renseigné manuellement dans SharePoint. Nouveau bouton (+ lien direct "Manquante → ajouter" sur chaque ligne sans FDS) : upload du fichier (PDF ou image) vers un nouveau dossier drive `FDS_Immos/{code_im}/`, puis `FDS_URL` renseigné avec un repère `FDS:code/fichier` (et non un lien de partage SharePoint) — le proxy `?fds=code` sait servir les deux formats, donc les FDS déjà présentes (liens de partage saisis à la main) continuent de fonctionner sans migration. Réservé à Admin/Logistique/Logistique_Mayotte.
- Aucun de ces trois ajouts ne nécessite de nouvelle liste ou colonne SharePoint côté William.

## Autorisation côté serveur des actions sensibles du Worker (août 2026)
- **Faille identifiée par l'assistant à la demande de William** ("que peut-on améliorer pour être au niveau d'un éditeur professionnel ?") : aucune des actions du Worker ne vérifiait les droits de l'appelant côté serveur — seule l'interface cachait les boutons selon le rôle. Depuis que `worker.js` est commité sur GitHub (août 2026, voir plus haut), les noms d'actions sont publics : n'importe qui connaissant l'URL du Worker pouvait appeler directement `ajouter_immo`, `maj_droits`, etc. sans jamais se connecter.
- **Portée volontairement limitée aux actions réservées au dashboard** : les actions utilisées aussi par la PWA terrain sans mot de passe (`reserver`, `transfert`, `declarer_panne`, `signaler_absence`...) reposent sur un modèle de confiance différent, assumé depuis le début du projet (identité = badge scanné, pas de mot de passe pour les 97 collaborateurs terrain) — les protéger aurait cassé ce flux pour tout le monde et n'était pas le problème signalé. Seules les **46 actions exclusivement dashboard** (créées ou modifiées uniquement via le dashboard admin) ont été protégées.
- **Mécanisme** : un jeton de session signé (HMAC-SHA256) est émis par le Worker à la connexion réussie (`verify_password`/`set_password`), encodant le code et le rôle de l'employé avec une expiration de 12h. Chaque action sensible vérifie ce jeton côté serveur avant de faire quoi que ce soit (`requireAdmin`/`requireGarant` selon le niveau requis, aligné sur les fonctions `estAdmin()`/`estGarant()` déjà utilisées côté client). Secret de signature (`SESSION_SECRET_ENV`) distinct de `CLIENT_SECRET_ENV`, à ajouter par William dans Cloudflare (Settings → Variables and Secrets) — **étape bloquante avant que la protection soit réellement active** ; tant que cette variable n'existe pas, la connexion au dashboard continue de fonctionner mais les 46 actions protégées échouent explicitement (`session_secret_manquant`) plutôt que de rester ouvertes sans contrôle.
- **Choix technique côté dashboard** : plutôt que de modifier individuellement les ~46 points d'appel `fetch(...)` dispersés dans `dashboard.html` (risque d'en oublier un), le jeton est injecté automatiquement par une interception unique de `window.fetch` au chargement de la page — un seul endroit à maintenir, la liste `GATED_ACTIONS` faisant le lien avec les actions protégées côté Worker.
- **Incident constaté pendant les tests** : en testant le mécanisme en local contre le Worker de production (erreur de ma part — j'aurais dû utiliser un environnement isolé), un appel `ajouter_immo` avec un code de test `IM999999` a répondu "doublon, existe déjà". Vérification faite : `IM999999` est bien un code déjà présent dans la base réelle (confirmé via `next_code_im` qui renvoie `max_num: 999999`), donc mon appel test n'a rien créé de nouveau — mais l'origine de ce code (visiblement un test, très éloigné de la plage réelle ~IM000001-IM001200) reste inexpliquée et n'a pas été investiguée plus loin. À vérifier par William et à nettoyer si c'est bien un résidu de test.
- **Vérification effectuée** : logique de signature/vérification testée unitairement en isolation (round-trip valide, rejet clé fausse, rejet payload modifié, rejet jeton expiré, aucun jeton émis si le secret n'est pas configuré) — tous les cas passent. Le mécanisme d'injection du jeton côté dashboard a été vérifié directement dans le navigateur (46 actions listées sans doublon, `window.fetch` bien intercepté).
- **Bug corrigé au passage** (retour terrain de William après un premier test réel d'ajout d'employé) : après création réussie, la liste ne se rafraîchissait pas (`loadEmployes()` appelé sans forcer le rechargement + mauvaise fonction de rendu) — l'employé était bien créé côté SharePoint mais invisible jusqu'à un rechargement complet de la page. Bug pré-existant, sans lien avec le contrôle d'accès, mis en lumière par ce test.

## Tests automatisés & garde-fous avant push (août 2026)
- Besoin exprimé par William après la mise en place du contrôle d'accès : un jeu de tests automatisés et une "préprod", en insistant sur le **zéro risque pour la production**. Question posée avant de commencer : comment isoler les données de test pour que ce soit vraiment zéro risque (site SharePoint séparé à créer, vs tests entièrement mockés sans nouvelle infra) ? **William a choisi les tests automatisés isolés dans un premier temps**, la préprod avec vraies données restant à évaluer plus tard si le besoin se confirme.
- **Choix technique** : `node --test` (natif depuis Node 18, zéro dépendance npm à installer) plutôt qu'un framework de test tiers — cohérent avec la philosophie du projet (aucun build, aucun `npm install` nécessaire jusqu'ici).
- **Refactorisation préalable, comportement inchangé** : la logique de session (signature/vérification du jeton, calcul des rôles) a été extraite de l'intérieur de `handleRequest()` vers le haut du fichier (fonctions pures paramétrées, ex. `signSessionWith(code, role, secret)` au lieu d'un `signSession(code, role)` fermé sur une variable interne) — uniquement pour la rendre testable indépendamment d'une vraie requête HTTP, sans changer le comportement en production (vérifié par re-passage des mêmes tests avant/après). `handleRequest()` continue de lire `SESSION_SECRET_ENV` exactement comme avant et se contente de le transmettre à ces fonctions.
- **Trois familles de tests, aucune ne touche SharePoint/Graph** :
  1. `tests/worker.session.test.js` : logique pure du jeton (signature, vérification, expiration, rejet falsification, normalisation de rôle).
  2. `tests/security.gated-actions.test.js` : **garde-fou anti-régression** — vérifie que la liste des actions protégées côté Worker et `GATED_ACTIONS` côté dashboard restent synchronisées (exactement le genre d'oubli qui a permis la faille initiale), et qu'aucune action partagée avec la PWA terrain n'est protégée par erreur.
  3. `tests/worker.integration.test.js` : simule de vraies requêtes HTTP contre `handleRequest()` (le vrai routeur), avec Microsoft Graph entièrement mocké (aucun appel réseau réel) — vérifie que le contrôle d'accès est bien branché de bout en bout, pas seulement dans les fonctions testées isolément.
- **Bug réel trouvé par les tests dès la première exécution** : `roleNorm()` ne retirait pas les espaces en début/fin avant de les remplacer par des underscores (`' admin '` devenait `'_admin_'`, pas reconnu comme `'admin'`) — corrigé (`.trim()` ajouté). Risque réel : un rôle SharePoint avec un espace superflu (saisie manuelle) aurait pu faire perdre ses droits admin à quelqu'un de légitime. Bonne illustration de la valeur des tests dès le premier jour.
- **Deux niveaux de garde-fou avant un push**, cohérents avec le mode de fonctionnement du projet (push direct sur `main`, sans revue de code, solo) :
  1. **Hook `pre-push` local** (`.githooks/pre-push`, activé via `git config core.hooksPath .githooks` — fait automatiquement au premier `npm install` via le script `prepare`) : bloque le push si `npm run verify` (syntaxe + tests) échoue. Contournable volontairement avec `git push --no-verify`.
  2. **GitHub Actions** (`.github/workflows/tests.yml`) : relance la même vérification à chaque push sur `main`, en filet de sécurité (n'empêche pas le déploiement GitHub Pages / Cloudflare, qui reste indépendant — sert juste à être alerté rapidement si le hook a été sauté).
- **Pas de site de préprod avec vraies données pour l'instant** (décision explicite de William) — resterait à faire si besoin : site SharePoint dédié + Worker en preview de branche Cloudflare (fonctionnalité déjà disponible côté Cloudflare, pas de nouvelle infra à payer) + un second repo/branche GitHub Pages pour le frontend (pour ne pas risquer de casser l'URL de la PWA installée sur les téléphones du terrain).

## Module Matériel IT — téléphones, puis ordinateurs (août 2026)

- Besoin exprimé par William : suivre les ordinateurs et téléphones professionnels, aujourd'hui difficiles à savoir "qui a quoi, depuis quand" — explicitement **hors circuit des immobilisations** ("ces articles ne sont pas des immos"). Fourni en pièce jointe : un fichier Excel de suivi de la flotte de téléphones (40 appareils, "incomplet" de son propre aveu) et la facture globale Free Pro.
- **Fichier Excel analysé** : onglet "2" (Téléphonie Mobile) — Type/Marque/Modèle/N° série, `N°Immobilisation` toujours "Non Immobilisé" (confirme le hors-périmètre immos), Date d'entrée/sortie, Utilisateur (codes employés existants + valeurs spéciales `RESERVE`/`ASTREINTE`/`ALARME`/`SANS AFFECTATION`), N° téléphone/Opérateur/SIM/codes PIN-PUK-RIO/déverrouillage. L'onglet "1" (Téléphonie Fixe) laissé hors périmètre (non demandé). Certains codes employés apparaissaient deux fois avec des dates d'entrée/sortie différentes (ex. IADA, PALO, TOHO) — pas une anomalie, juste deux appareils physiques différents successifs pour la même personne, correctement gérés par une entrée catalogue par ligne Excel.
- **Facture Free Pro analysée** : en réalité 4 pages (le fichier joint annoncé comme "168 pages" par l'outil de lecture ne correspondait pas au fichier réel sur le lecteur réseau — vérifié directement, 4 pages scannées). 29 lignes actives à 23,22€ HT/mois chacune (tarif plat "Forfait Mobile Free Pro 5G — Sans engagement"), avec le **nom complet réel** du porteur par ligne (pas le code employé) — utile pour confirmer que le numéro de téléphone est la clé de rapprochement fiable entre les deux sources (le nom sur la facture est parfois tronqué par le système de Free, ex. "Jean Bernard RI" pour RIJB).
- **Cadrage validé avec William avant développement** (questions posées, recommandations retenues) : (1) **historique complet des détenteurs successifs** (pas juste un détenteur actuel figé), pour retrouver la durée de détention comme demandé — même principe que les Mouvements des immos, mais sans le workflow panne/réservation ; (2) **coût mensuel inclus** (champ simple par appareil, pas d'amortissement comme les immos) ; (3) **100% dashboard**, pas d'écran PWA — cohérent avec EPI/Outillage (attribution pilotée par la logistique, pas en libre-service).
- **Modèle de données** : 2 nouvelles listes, `Materiel_IT` (catalogue) et `Mouvements_Materiel_IT` (historique de détention, détenteur courant = dernier mouvement par appareil). Codes générés par préfixe de type (`TEL######` pour les téléphones, `PC######` prévu pour les ordinateurs) plutôt qu'un préfixe unique, pour rester lisible une fois les deux catégories mélangées dans une même liste — décision anticipée pour éviter une refonte quand William fournira les PC dans un second temps (annoncé par lui, pas encore livré).
- **Droits réutilisés sans nouvelle capacité** : `peutGererEPI()` (Admin/Logistique/Logistique_Mayotte) réutilisée telle quelle côté dashboard pour ce module — même population que EPI/Outillage, pas de nouvelle ligne dans `ROLE_CAPS`.
- **Sécurité dès la conception** : les 5 nouvelles actions Worker (`ajouter_materiel_it`, `maj_materiel_it`, `affecter_materiel_it`, `bulk_import_materiel_it`, `bulk_import_mouvements_materiel_it`) ont été protégées par le mécanisme de jeton de session dès leur écriture (voir section précédente), pas ajoutées après coup — le test `security.gated-actions.test.js` a validé automatiquement la cohérence sans modification.
- **Migration préparée, données transparentes sur les incertitudes** : 40 appareils convertis en catalogue + historique initial. Coût mensuel (23,22€) appliqué uniquement aux appareils actifs avec une ligne FREE en cours (28 sur 40) — pas de tentative de reconstituer un rapprochement ligne à ligne avec la facture (dont les 29 lignes ne correspondent pas exactement aux 40 lignes Excel, celui-ci étant "incomplet" de l'aveu de William : au moins une ligne facturée — ex. "STAGIAIRE" — n'a pas de contrepartie identifiable dans le fichier de suivi). Date de détention = date d'entrée Excel si connue, sinon date du fichier source (23/05/2026) en repli assumé — la durée affichée pour ces appareils sera donc une approximation par défaut, pas la date réelle de première attribution. 3 codes détenteurs (`ADWI`, `HOAL`, `VIMA`) non retrouvés dans la liste `Employes` actuelle au moment de la vérification — importés tels quels plutôt que devinés, à corriger par William si besoin (probable erreur de saisie ou ancien collaborateur).
- **Étape bloquante côté William avant mise en service** : création des 2 listes SharePoint (`Materiel_IT`, `Mouvements_Materiel_IT`), comme pour chaque nouveau module.
- **Incident au premier import** : `Mouvements_Materiel_IT` importée correctement (40 lignes), mais `Materiel_IT` restée totalement vide. Diagnostiqué en lecture seule via `?debug_columns=Materiel_IT` (aucune donnée touchée) : la colonne créée par William pour le code de déverrouillage s'appelle en interne `Code_deverouillage` (sans le 2e "r", casse différente) et non `Code_Deverrouillage` comme écrit dans le code — un seul champ non reconnu suffit à faire échouer la création de **toute** la ligne SharePoint (pas seulement ce champ), d'où la liste entièrement vide malgré un import qui semblait progresser normalement à l'écran. Corrigé dans `worker.js` (4 occurrences) pour matcher le nom réel plutôt que de redemander à William de renommer la colonne — même logique que l'incident `Fabricant`/`Fabriquant` de la campagne d'inventaire (voir plus haut) : toujours vérifier le nom interne réel via `?debug_columns=<liste>` en cas de doute, jamais se fier au nom tel que tapé à la création.

## Mode hors-ligne de la PWA (service worker, août 2026)

- Suite à la question de l'assistant "que peut-on améliorer pour être au niveau d'un éditeur professionnel ?", deux points retenus par William : l'observabilité (voir plus bas) et l'absence de mode hors-ligne réel de la PWA malgré son nom — gênant vu les zones à réseau instable à Mayotte.
- **Périmètre choisi avec William** (deux options proposées) : coquille applicative + données en cache (l'app s'ouvre même sans réseau, affiche les dernières données connues, indique clairement quand une action nécessite le réseau), **pas** la mise en file d'attente des actions faites hors-ligne (scan, réservation...) — jugée trop risquée pour une première étape (conflits de données possibles, ex. deux personnes affectant la même immo) et à traiter dans une réflexion dédiée si le besoin se confirme.
- **Mécanisme** : nouveau fichier `sw.js` (service worker), enregistré depuis `index.html`. Met en cache la coquille applicative (`index.html`, `app.js`, `manifest.json`, logos) en stratégie réseau-d'abord-repli-cache (fraîcheur à chaque déploiement, mais fonctionne hors-ligne), et les catalogues statiques `immos.json`/`employes.json` en stratégie cache-d'abord-rafraîchi-en-arrière-plan (affichage instantané). **Les appels au Worker (réservations, transferts, données live) ne sont jamais interceptés** par le service worker — ils échouent proprement avec le message d'erreur réseau déjà existant si hors-ligne, pas de données périmées présentées comme à jour sur ce qui compte vraiment.
- **Bannière hors-ligne** ajoutée dans `index.html` (écoute `online`/`offline` + `navigator.onLine`) pour prévenir clairement l'utilisateur plutôt que de le laisser découvrir le problème en tapant sur un bouton qui échoue.
- **Vérifié** : enregistrement et activation du service worker confirmés dans le navigateur, les 8 ressources de la coquille effectivement mises en cache, bascule de la bannière testée directement (affichage/masquage corrects selon l'état réseau).

## Observabilité : intégration Sentry (août 2026)

- Suite au point "zéro observabilité" soulevé par l'assistant, William a choisi Sentry (recommandé) plutôt qu'une solution maison sur liste SharePoint — le regroupement automatique des erreurs similaires et les alertes email en cas de pic étaient jugés plus utiles qu'une solution 100% interne mais plus basique.
- **Mise en œuvre** : SDK Sentry Browser chargé via CDN (`https://browser.sentry-cdn.com/10.69.0/bundle.min.js`, cohérent avec la philosophie "zéro build" du projet — même pattern que Chart.js/SheetJS/html5-qrcode déjà utilisés) dans `index.html` (PWA) **et** `dashboard.html`, chacun avec son propre tag `environment` (`pwa` / `dashboard`) pour distinguer l'origine des erreurs dans Sentry. Script chargé tôt dans le `<head>` pour capter aussi les erreurs des scripts suivants.
- Le DSN fourni par William n'est pas un secret (conçu pour être embarqué côté client, comme une clé Firebase) — commité en clair dans le code, cohérent avec le reste de la configuration déjà publique du projet.
- **Vérifié** : `Sentry.getClient()` confirmé actif avec le bon DSN et le bon tag d'environnement dans le navigateur ; un message de test envoyé pour confirmer la réception côté Sentry (à vérifier par William dans son tableau de bord Sentry).

## Correctif : erreur non interceptée lors de l'activation manuelle (`stopScanner`, août 2026)

- **Signalé par William** en testant l'activation manuelle (saisie du code au lieu du scan) : erreur console `Uncaught Cannot stop, scanner is not running or paused`.
- **Cause identifiée en inspectant directement le code source de la librairie `html5-qrcode`** (récupéré en lecture seule depuis le CDN public, aucune donnée touchée) : `Html5Qrcode.stop()` lance une exception **synchrone** (`throw "Cannot stop, scanner is not running or paused."`, une chaîne de caractères, pas un objet `Error`) si le scanner n'est pas déjà en train de scanner — typiquement quand la caméra n'a jamais fini de démarrer (permission refusée, ou navigation vers l'écran manuel avant que le scanner ait pu se lancer). Le `.catch()` déjà présent sur `stopScanner()` ne protégeait que contre une **promesse rejetée**, pas contre ce rejet synchrone survenant avant même la création de la promesse — donc totalement inefficace dans ce cas précis.
- **Corrigé** dans `app.js` : `stopScanner()` encadre désormais l'appel dans un `try/catch` classique (en plus du `.catch()` conservé pour le cas où une promesse est bien retournée et rejetée), ce qui couvre les deux scénarios possibles.
- **Vérifié** : reproduit fidèlement la situation réelle (caméra bloquée → scanner jamais démarré) dans un navigateur de test, confirmé que l'ancienne version lève bien l'erreur et que la nouvelle l'absorbe proprement sans planter la navigation.
- **Second incident au retest par William** (même écran, après déploiement) : nouvelle erreur console `Uncaught (in promise) TypeError: Failed to execute 'clone' on 'Response': Response body is already used`, cette fois dans `sw.js` (mode hors-ligne, livré juste avant). Cause : dans la branche "coquille applicative" du service worker, `res.clone()` était appelé **à l'intérieur** d'un `.then()` différé (`caches.open(CACHE_NAME).then(cache => cache.put(req, res.clone()))`) au lieu d'être appelé immédiatement à réception de la réponse — le temps que cette promesse se résolve, le corps de la réponse pouvait déjà être en cours de lecture ailleurs (la réponse est aussi renvoyée telle quelle au navigateur), rendant le clone impossible. Corrigé en appelant `res.clone()` de façon synchrone dès réception de la réponse, avant tout passage asynchrone. Vérifié : plusieurs rechargements sans erreur, les 9 ressources de la coquille toujours mises en cache correctement.

## Punch-list dashboard : correctifs et petites fonctionnalités (août 2026)

Grosse session de retours terrain groupés par William ("grosse session dashboard uniquement"), 11 points traités d'affilée. Détail par point :

- **404 à l'ouverture du dashboard** : diagnostiqué comme la simple sonde automatique `/favicon.ico` du navigateur (déjà documentée comme anomalie bénigne dans `01_ARCHITECTURE_TECHNIQUE.md`) — aucune ressource référencée dans le code n'était réellement manquante. Corrigé quand même proprement en ajoutant `<link rel="icon" href="logo-icon.jpg">` dans `index.html` et `dashboard.html`, plutôt que de laisser l'erreur silencieuse.
- **Logo trop petit sur les fiches imprimables** : hauteur du logo passée de 64px à 110px sur les trois gabarits qui le partagent (EPI, Outillage, relevé de sortie).
- **Référence + marque manquantes sur les documents de pointage terrain** : les gars pointent le matériel reçu sur la base du document imprimé — un intitulé seul ne suffit pas à distinguer deux outils similaires. Ajouté sur la fiche outillage et sur le relevé de sortie d'employé.
- **Stock négatif possible en distribution d'outillage** : `distribuer_outillage` (Worker) vérifie désormais le stock disponible par type d'article *avant* toute écriture et refuse la distribution si insuffisant (message explicite listant les articles concernés). **Décision volontairement limitée à l'outillage** : l'émargement EPI (`emarger_dotation_epi`) n'a pas reçu le même blocage dans cette passe, car sa sémantique diffère — l'émargement *confirme* une remise qui a souvent déjà eu lieu physiquement (le salarié a déjà le matériel en main), alors que la distribution d'outillage *est* le moment de la décision. Bloquer l'émargement risquerait de laisser une remise réelle non enregistrée dans l'outil. À rediscuter avec William si le même problème se présente côté EPI.
- **Actions impossibles pour un employé inactif** : ajout d'un contrôle serveur (`estEmployeActifServeur`, lit `Employes.field_2`) sur `reserver`, `transfert` (receveur — le donneur "DEPOT" reste explicitement exempté, un retour de matériel par quelqu'un qui vient de sortir de l'effectif doit rester possible) et `generer_dotation_epi`. Volontairement une vérification **serveur**, pas seulement côté interface, cohérent avec le principe déjà acté pour l'autorisation des actions sensibles (voir plus bas dans ce journal).
- **Fiche de sortie sans quantités attendues** : ajout des colonnes Attendu/Rendu (immos : toujours 1 ; outillage : agrégé par type d'article, une même référence pouvant avoir été remise en plusieurs exemplaires au fil du temps puisque `Lignes_Outillage` n'a pas de colonne quantité).
- **Pas de circuit de retour physique après la fiche de sortie** : ajout d'un état "⏳ EN ATTENTE RETOUR FICHE DE SORTIE" affiché sur la fiche employé (bloc outillage) dès que l'employé est inactif, avec un bouton "↩️ Retour en stock" par ligne. Réutilise l'action Worker `annuler_ligne_outillage`, qui existait déjà côté serveur et dans `GATED_ACTIONS` mais n'était appelée par aucun point du dashboard (orpheline depuis sa création) — repris tel quel plutôt que dupliqué.
- **Filtre actif/inactif illisible sur les listes** : ajouté sur Utilisateurs, "Au dépôt" et Historique des mouvements, même pattern à 3 options (tout / actifs seulement / inactifs seulement) partout. A révélé au passage que le statut actif/inactif d'une immo (`Immos.Actif`, déjà utilisé côté écriture par `marquer_statut_immo`) n'était **jamais remonté au dashboard** : ajouté à la réponse `?immo_metadata=1` du Worker (`actif: (f.Actif||'Oui')!=='Non'`) — un seul point de correction côté serveur suffit puisque `immoMeta` est chargé intégralement sans fusion côté client.
- **Sélection d'immo à la main dans "Ajouter une réservation"** : remplacé le simple champ code-exact par une recherche en direct (tape un fragment de code, de libellé ou de marque, ex. "532", "rainureuse", "Makita") avec liste de suggestions cliquable — même logique de correspondance (`code.includes(...) || libellé.includes(...)`) que les recherches déjà utilisées partout ailleurs dans le dashboard (Circulation, Au dépôt...), pour que l'expérience soit cohérente d'un écran à l'autre.
- **Pas de correction manuelle du stock EPI/Outillage** : le comptage physique réel n'a pas encore été fait pour ces deux modules (stock parti de 0 ou de valeurs calculées à la migration) — un bouton "✏️ Éditer" à côté du "+ Réception" existant permet de fixer une valeur absolue plutôt que d'ajouter un delta. Côté EPI, réutilise l'action déjà existante `bulk_maj_stock_epi` (appelée avec un tableau d'un seul élément) ; côté Outillage, nouvelle action `editer_stock_outillage` (n'existait pas, seul un delta de réception était possible).
- **Génération massive des dotations annuelles EPI en PDF locaux** : bouton "📁 Générer les PDF" dans l'onglet EPI → Dotations, qui écrit un PDF par fiche (`Dotation_annuelle_EPI_<année>_<code>.pdf`) directement dans un dossier local choisi par William, sans repasser par l'impression navigateur fiche par fiche. **Question posée explicitement à William avant implémentation** (aucune API web ne peut écrire silencieusement dans un dossier arbitraire du disque) : accès dossier direct via la File System Access API (Chrome/Edge uniquement, un choix de dossier une fois pour toutes via `showDirectoryPicker`, handle persisté en IndexedDB pour ne pas le redemander à chaque session) vs. téléchargement d'un ZIP à extraire manuellement. **William a choisi l'accès dossier direct.** Implémentation : `html2canvas` + `jsPDF` ajoutés en CDN (cohérent avec la philosophie "zéro build" déjà en place pour Chart.js/SheetJS/Sentry/html5-qrcode), le gabarit HTML de la fiche EPI (déjà utilisé par l'aperçu imprimable existant) est rendu hors-écran puis rasterisé en PDF, un fichier par fiche de l'année sélectionnée. Fonctionne uniquement sur Chrome/Edge — message explicite si le navigateur ne supporte pas `showDirectoryPicker`.

## Retour terrain sur la génération PDF des dotations EPI (août 2026)

Premier essai réel par William du bouton "📁 Générer les PDF" livré juste avant : signalé comme "pas ce que je t'avais demandé". Trois points corrigés :

- **Deux boutons au lieu d'un, flux pas clair** : William avait demandé un unique bouton "Générer dotation annuelle" avec une popup demandant l'année, qui fait tout d'un coup (fiches + PDF) pour tous les employés actifs éligibles. La première implémentation avait scindé ça en deux boutons séparés ("Générer les fiches manquantes" puis "Générer les PDF"), ce qui explique aussi le premier symptôme signalé ("j'ai cliqué sur Générer les PDF, rien ne s'est passé") : sans fiches déjà générées pour l'année, le bouton PDF n'avait rien à traiter. **Fusionné en une seule fonction** (`genererDotationAnnuelleComplete`) déclenchée par un unique bouton : `prompt()` pour l'année, création des fiches SharePoint manquantes (avec confirmation), puis génération de tous les PDF de l'année dans la foulée, sans étape intermédiaire à comprendre.
- **Bruit console `Unable to clone canvas as it is tainted`** : répété pour chaque employé lors de la génération, mais sans conséquence réelle (William a confirmé que tous les PDF étaient malgré tout générés et bien nommés). Cause : `html2canvas` clone l'intégralité du document (pas seulement l'élément ciblé) pour préserver le contexte de rendu, et tombe donc sur les `<canvas>` des graphiques Chart.js de l'onglet Analyses — toujours présents dans le DOM même quand cet onglet n'est pas affiché — qu'il ne parvient pas à recopier. Corrigé avec l'option `ignoreElements` d'html2canvas pour écarter tout `<canvas>` de la capture (la fiche EPI elle-même n'en contient aucun), qui supprime l'avertissement à la source plutôt que de le laisser s'accumuler dans la console.
- **Fiche sans moyen de cocher la réception** : le tableau de la fiche (Référence/Désignation/Taille/Quantité) n'avait pas de colonne pour que l'employé coche chaque article reçu à la signature — pourtant présente dans le gabarit papier d'origine. Ajout d'une colonne "Émargement" (case à cocher vide) par ligne d'article, dans le gabarit HTML partagé par l'aperçu imprimable et la génération PDF (`epiFicheBodyHtml`) — un seul point de correction pour les deux sorties.

## Suivi des émargements EPI/Outillage : historique, filtre, import de scans en lot (août 2026)

Après le premier retour terrain sur la génération des dotations annuelles, William a détaillé une liste de manques qu'il avait en tête depuis le départ. Question explicitement posée avant de trancher l'un des points (le lien scan → fiche) via `AskUserQuestion`, plan écrit et validé avant implémentation vu l'ampleur du sujet.

- **Décisions validées** : (1) lien scan → fiche : le flux individuel existant (upload direct sur la fiche d'un employé précis) suffit au quotidien **et** un import en lot avec lecture automatique du code employé est utile pour rattraper un arriéré ou une session de signature groupée — les deux sont conservés ; (2) remises ponctuelles EPI : ajout d'une option "destinataire = employé nommé" (au lieu de deviner par recherche approximative dans le texte libre, jugé trop peu fiable) ; (3) portée outillage : parité complète avec les EPI pour la présentation des fiches et l'historique — **sauf l'import de scans en lot**, qui ne peut pas s'appliquer tel quel car l'outillage n'a pas d'état "en attente de signature" (la photo est déjà exigée au moment de la distribution) ; signalé explicitement à William plutôt que de forcer une fausse parité.
- **Présentation uniforme** : `voirFicheOutillage` était un gabarit dupliqué avec sa propre feuille de style — aligné sur le gabarit partagé des fiches EPI (`EPI_FICHE_CSS_RULES`/`EPI_FICHE_LOGO_URL`), avec la même colonne "Émargement" (case à cocher) ajoutée sur chaque ligne d'article. Le relevé de sortie d'employé n'est volontairement pas concerné (ce n'est pas une fiche d'émargement, il a déjà ses propres colonnes Attendu/Rendu).
- **Garde-fou "déjà généré cette année"** : le bouton unique de génération vérifie désormais si des fiches existent déjà pour l'année saisie et prévient explicitement avant de continuer (la dotation annuelle ne doit normalement se lancer qu'une fois par an) — remplace un confirm plus vague qui ne portait que sur les fiches manquantes.
- **Nouvel onglet "Historique"** (EPI et Outillage) : tableau employé × année (statut de dotation + nombre de remises ponctuelles nommées côté EPI, nombre d'outils reçus côté Outillage, cellules cliquables vers le détail) et une vue "consommation par année" (quantités par article, filtrable par type de dotation côté EPI, export CSV). Aucune nouvelle colonne/liste SharePoint requise : la matrice se lit entièrement depuis les données déjà chargées en mémoire (`epiDotations`, `outilLignes`) ; seule la vue consommation nécessitait un accès à toutes les lignes de détail en un seul appel, d'où un nouvel endpoint de lecture `?lignes_dotation_epi_toutes=1` (non gated, même famille que les endpoints `?dotations_epi=1`/`?catalogue_epi=1` existants).
- **Filtre Statut** (Tous/Émargées/En attente) + compteur visible sur l'onglet Dotations EPI, pour répondre directement au besoin de voir clairement qui n'a pas encore signé.
- **Scans PDF acceptés partout** : jusque-là, l'émargement (EPI comme Outillage) n'acceptait qu'une image (`accept="image/*"`), alors que William parlait explicitement de "PDF scanné". Un helper partagé (`fichierVersJPEGBase64`) a été introduit pour factoriser la logique dupliquée entre les flux EPI et Outillage, et rend désormais aussi la 1ère page d'un PDF (via `pdf.js`) avant de suivre le même chemin de compression JPEG que les photos.
- **Import de scans en lot (EPI)** : lecture optique best-effort (`Tesseract.js`, chargé via CDN, aucune infra serveur — même philosophie que les autres bibliothèques du projet) du code employé sur plusieurs scans à la fois. Choix de conception pour limiter les faux positifs : la reconnaissance ne cherche que des fragments de 4 lettres correspondant à un code parmi les fiches **jamais émargées** (univers volontairement restreint), et **aucun rattachement n'est appliqué automatiquement** — un tableau de contrôle (aperçu miniature, code détecté, sélecteur de correction manuelle par ligne) doit être validé explicitement avant tout appel aux actions d'émargement réelles. Assumé et documenté comme une aide, pas une garantie : la qualité d'un scan/photo de terrain reste très variable.
- **Remise ponctuelle EPI nommée** : `Title` de la fiche devient le code employé (au lieu de l'identifiant synthétique `PONCTUEL-...`) quand un destinataire nommé est choisi — sans risque de collision avec la logique anti-doublon des dotations Annuelle/Entree, qui filtre déjà explicitement par `Type_Dotation`.

## Retour terrain : mauvaise identité attribuée aux mouvements, liste "Transférer à" incomplète (août 2026)

William a signalé plusieurs symptômes en testant le lot précédent : la liste déroulante "Transférer à" (bouton rapide depuis la fiche immo) ne montrait qu'une poignée d'employés, avec en plus des comptes admin ("Dashboard Admin (ADMIN)", "AIMAR William... (AIWI)") qui n'ont rien à faire dans une liste de destinataires terrain ; un transfert fait vers GOLU s'est retrouvé enregistré avec "Utilisateur AIWI, donné par vide" dans l'historique des mouvements ; l'utilisateur fantôme "Dashboard Admin" réapparaissait à plusieurs endroits.

**Cause racine, la même partout** : ce bouton rapide "Créer un transfert"/"Créer un retour" (fonction `ouvrirActionMouvement`/`soumettreAction`, vestige antérieur au système de jetons de session mis en place plus tôt cet été) construisait sa liste d'employés en piochant dans l'historique des mouvements déjà enregistrés (`d.all_movements`) au lieu de la liste réelle des employés, et postait directement un mouvement SharePoint via un point d'entrée générique du Worker **sans aucune authentification ni vérification de droits** — l'identité du "donneur" n'était jamais celle de la personne réellement connectée, remplacée par un marqueur statique `'DASH|Dashboard'` ou par des codes codés en dur (`'CONI'`, `'ADMIN'`) selon les endroits. En creusant, le même anti-pattern (`body.par_code || 'CONI'` ou `'ADMIN'`) est apparu dans plusieurs autres actions déjà protégées par jeton de session (`maj_panne`, `enregistrer_reparation`, `marquer_statut_immo`, la migration `bulk_import_lignes_inventaire`) : ces actions faisaient bien vérifier les **droits** du jeton, mais utilisaient encore l'identité fournie par le client plutôt que celle, vérifiée, du jeton lui-même — un même compte pouvait donc se voir attribuer une action réalisée par quelqu'un d'autre si le code envoyé dans le corps de la requête ne correspondait pas à la session réelle.

- **Nouvelle action Worker `creer_mouvement_direct`** (gated `requireGarant`) remplace l'ancien point d'entrée générique non protégé : le donneur/déclarant est désormais toujours l'identité vérifiée de la session (`_auth.session.code`, résolue en nom via `getNomResolver()`), jamais une valeur envoyée par le client. Pour un Transfert, `Code_Employe` = receveur (fourni par le client, ce n'est qu'une désignation de destinataire, pas une identité à protéger) et `Code_Chantier` = `donneur_code|donneur_nom` — exactement le même format que celui déjà utilisé par l'action `valider` (retours/transferts en attente), pour que la colonne "Donneur / Validé par" de l'historique fonctionne enfin pour ce flux aussi. Pour un Retour, `Code_Employe` = l'identité de session (remplace le `'CONI'` codé en dur).
- **Liste "Transférer à" reconstruite depuis `employesList`** (la liste réelle des employés, déjà chargée) au lieu de l'historique des mouvements, filtrée aux comptes actifs et non-admin (`estAdmin()`, exclut AIWI/CONI/NAXA/BAKA et tout rôle SharePoint "Admin") — corrige à la fois l'absence de certains ouvriers/CT/RA (qui n'apparaissaient que s'ils avaient déjà été l'auteur d'un mouvement) et la présence des comptes admin qui n'en faisaient historiquement partie que par fuite de données. Recherche floue (tape un code ou un nom, clique une suggestion) ajoutée sur ce champ ET sur "Changer le receveur" (modification d'un transfert en attente), même pattern que la recherche d'immo — nouvelle fonction partagée `attacherRechercheEmploye()`.
- **Fallbacks `||'CONI'` retirés de tous les appels dashboard** (suivi de panne, résolution de panne, réparation/entretien, déclaration de vol, sortie définitive, marquage de statut, déclaration de panne) — remplacés par `ADMIN_SESSION.code`/`ADMIN_SESSION.nom` sans repli vers un nom codé en dur, cohérent avec "le vrai utilisateur connecté doit apparaître".
- **`declarer_vol` et `resoudre_panne` : tentative de les protéger par jeton de session annulée après coup** — ces deux actions sont en réalité **partagées avec la PWA terrain** (utilisées par le profil "admin" du badge scanné, sans mot de passe, voir `app.js`), contrairement à ce qu'une première lecture du code laissait penser. Le test automatisé `security.gated-actions.test.js` (garde-fou déjà en place depuis l'implémentation initiale de l'autorisation serveur) a immédiatement signalé l'erreur avant tout déploiement — les protéger aurait cassé ces deux actions pour les collaborateurs terrain. Laissées volontairement non protégées, avec juste le retrait du nom codé en dur en repli.
- **Remise ponctuelle EPI** : le sélecteur d'employé nommé (ajouté au lot précédent) a été refondu en un seul champ de recherche floue (au lieu d'un select + champ texte séparés) — cohérent avec "généraliser la liste déroulante" demandé par William, et supprime le risque de faute de frappe en saisie libre (l'exemple "CANi" au lieu de "CANI" cité par William) : taper un code/nom propose maintenant des suggestions cliquables, la saisie libre restant possible uniquement pour les remises de groupe.
- **Anomalie non corrigée, à surveiller** : `marquer_statut_immo` est protégé par jeton de session (`requireGarant`) alors que ce même endpoint est aussi appelé par `app.js` (PWA, profil "admin" terrain sans mot de passe) pour la sortie définitive d'une immo — un gel pré-existant, pas introduit par ce correctif (la protection avait déjà été ajoutée lors du chantier "Autorisation côté serveur"). Si William confirme que ce flux PWA "admin" est réellement utilisé sur le terrain, il faudra soit lui fournir un jeton (login PWA "admin"), soit sortir cette action de la protection comme `declarer_vol`/`resoudre_panne` — non tranché, à valider avec lui avant de toucher au code.

## Lot 3 : OCR fiable, signature obligatoire, Lignes téléphoniques, liens partout, nouveaux entrants EPI (août 2026)

Nouveau lot de retours après le lot précédent (historique EPI/Outillage, import de scans en lot).

- **Bug OCR réel, pas une question de qualité de scan** : William a testé en important en lot les PDF *déjà générés par l'appli elle-même* (texte net, pas un vrai scan) — les premiers étaient bien lus puis ça devenait incohérent. Cause confirmée en consultant la doc officielle de Tesseract.js : `analyserLotEPI` appelait `Tesseract.recognize()` à chaque fichier, créant et détruisant un worker WASM à chaque appel — l'anti-pattern explicitement documenté par la librairie elle-même ("users should create a worker once... rather than running the above snippet for every image"). Corrigé : un seul `Tesseract.createWorker('eng')` créé avant la boucle, réutilisé pour tous les fichiers, terminé une fois à la fin.
- **Détection de signature bloquante** : William a précisé vouloir une lecture directe dans la case "Signature" du document (pas une simple case à cocher de confiance). Implémenté en best-effort : analyse de pixels (`detecterSignatureDataURL`) sur une bande large en bas-droite de la page rendue (la position exacte varie selon le nombre de lignes d'articles, d'où une zone de recherche généreuse plutôt que des coordonnées fixes), qui compte la proportion de pixels non-blancs. **Bloquant par défaut** sur l'import en lot ET l'émargement individuel (EPI et Outillage) : une fiche sans signature détectée ne peut pas être validée sans une confirmation explicite ("Forcer malgré tout"), l'aperçu miniature déjà présent permettant de vérifier visuellement avant de forcer. Assumé comme un heuristique, pas une garantie à 100% (ce n'est pas de l'OCR de texte).
- **Lignes téléphoniques, entité indépendante des téléphones** : William a précisé qu'un employé peut garder sa ligne (numéro/SIM/opérateur) en changeant d'appareil, ou l'inverse — le modèle initial (tout sur le même enregistrement `Materiel_IT`) ne le permettait pas. Nouvelles listes `Lignes_Telephoniques`/`Mouvements_Lignes_Telephoniques`, miroir exact du modèle `Materiel_IT`/`Mouvements_Materiel_IT` (mêmes patterns d'actions, même principe de détenteur courant dérivé du dernier mouvement). L'onglet "Matériel IT" devient un conteneur à sous-onglets (Téléphones / Lignes, extensible plus tard pour Ordinateurs — "en attendant les PC" dixit William), même principe que les sous-onglets EPI/Outillage. **Migration one-shot** : pour chaque téléphone existant ayant un numéro, crée la ligne correspondante avec le même détenteur actuel comme point de départ de l'historique (bouton "Migrer maintenant" dans le nouvel onglet Lignes, tant qu'aucune ligne n'existe encore).
- **Codes cliquables généralisés dans EPI/Outillage** : le pattern `.link-emp` (délégation de clic globale déjà en place partout ailleurs dans le dashboard) était absent des vues EPI/Outillage. Nouvelle fonction `lienEmpSiConnu(code, texte)` qui ne rend cliquable que les codes correspondant à un employé réel (les identifiants synthétiques des remises ponctuelles de groupe, ex. `PONCTUEL-...`, restent du texte brut).
- **Vue "Nouveaux entrants EPI"** : règle métier précisée par William — tout employé actif d'un service concerné par la dotation doit recevoir sa fiche "Entree" dès l'embauche, peu importe la date. Nouvelle carte en tête de l'onglet Dotations EPI listant les employés actifs avec une affectation EPI mais sans fiche cette année, avec pour chacun le besoin en articles vs le stock disponible (nouveau helper client `epiCalculerBesoinStock`, miroir exact de `trouverArticleCatalogue`/`TAILLE_FIELD_PAR_TYPE` côté serveur) et un bouton direct pour générer sa fiche.
- **Formulaire "Ajouter un employé" étendu** : William reçoit les demandes d'embauche par mail avec un tableau (service, tailles, kits) — le formulaire de création d'employé n'avait aucun champ EPI, il fallait repasser ensuite par la fiche employé. Ajout de l'affectation + 5 tailles directement à la création, avec la règle confirmée par William "taille veste = taille t-shirt" (auto-remplie, reste modifiable). Après création, si une affectation est renseignée, une alerte informative (non bloquante, décision validée avec William) liste les articles en stock insuffisant pour son profil — réutilise `epiCalculerBesoinStock`, aucune nouvelle action Worker nécessaire (les tailles sont sauvegardées via l'action existante `maj_taille_employe`, appelée juste après `ajouter_employe`).

## Rollback de session, export de sauvegarde et cartographie d'architecture (août 2026)

- **Incident constaté en tout début de session : deux dossiers de travail divergents.** Le dépôt git réel (`C:\Users\ral\immo-tracker`, remote `RAL974/immo-tracker`) avait un code à jour (dernier commit "Lot 3", 9 août) mais des documents `00_*`-`05_*`/`CLAUDE.md` figés au 6 août, ne mentionnant même pas "Lot 3". Un second dossier (`Desktop\Immos`, sans `.git`, sans `worker.js`/`package.json`/`tests/`) avait l'inverse : documents à jour au 9 août (mentionnant déjà "Lot 3") mais du code du 3 août. Signalé par William en cours de session. **Résolu** en resynchronisant les documents du dépôt git depuis la version la plus récente (`Desktop\Immos`), après vérification qu'elle correspondait bien à l'état réel du code (présence de l'entrée "Lot 3"). Le dossier `Desktop\Immos` reste à traiter séparément par William si des sessions y sont encore actives — ne pas repartir de ce dossier pour du travail futur sur le code.
- **Besoin exprimé** : un filet de sécurité pour revenir en arrière après une session de modifications ratée, sans jamais toucher à l'historique git partagé (pas de `reset --force`/`push --force` sur `main`, conformément au mode de fonctionnement du projet — push direct, solo, sans branche de préprod).
- **Outillage ajouté** : `npm run session:start` (pose un tag git local `session-AAAAMMJJ-HHMM` sur HEAD) et `npm run session:rollback` (liste les tags, demande confirmation explicite "OUI", puis `git revert` — jamais de réécriture d'historique — de tous les commits postérieurs au tag choisi ; ne pousse jamais automatiquement). Deux scripts Node purs (`scripts/session-start.js`, `scripts/session-rollback.js`), cohérents avec le principe "zéro dépendance" déjà en place pour `scripts/check-dashboard.js`.
- **Distinction explicite code vs données**, documentée dans le nouveau `PROCEDURE_ROLLBACK.md` : un rollback git ne touche que ce dépôt (PWA, Dashboard, Worker) — **jamais** les données déjà écrites dans SharePoint par une session avant qu'elle soit annulée. D'où la règle : exporter en JSON les listes SharePoint concernées **avant** toute migration ou import de masse.
- **Nouvel endpoint de lecture seule `?export_liste=<nom_liste>&token=<jeton>`** dans `worker.js`, protégé `requireAdmin` (même mécanisme HMAC que les actions gated existantes, transmis en paramètre d'URL car c'est un GET sans corps JSON — les autres actions gated le reçoivent via l'interception `window.fetch` du dashboard, qui ne patche que les POST). Liste blanche `EXPORTABLE_LISTS` des listes réellement utilisées par le Worker (voir point suivant). Bouton dashboard "💾 Exporter une liste (sauvegarde)" ajouté dans Utilisateurs → Outils de maintenance avancés, à côté des autres outils d'administration ponctuels.
- **Bug découvert et corrigé pendant l'implémentation** : la liste blanche `EXPORTABLE_LISTS` avait été rédigée d'après `02_MODELE_DONNEES.md`, qui inclut `Chantiers` parmi les listes SharePoint. Vérification faite dans `worker.js` (grep sur tous les appels Graph `GL + '/NomListe'`) : **aucune liste `Chantiers` n'est réellement utilisée** — `Code_Chantier`/`Nom_Chantier` sont de simples champs texte libre sur `Reservations`/`Mouvements`, et `chantiers.json` à la racine n'est référencé nulle part dans le code. Retiré de `EXPORTABLE_LISTS` (aurait échoué à l'export) ; `Absences` (liste réellement utilisée mais absente de la doc, voir plus bas) ajoutée à la place. Leçon retenue : vérifier une liste blanche contre le code réel, pas seulement contre la documentation.
- **Nouveau test `tests/security.export-liste.test.js`**, sur le même principe que `security.gated-actions.test.js` mais adapté : `export_liste` étant un endpoint GET (paramètre de requête), il n'est pas capturé par le mécanisme `GATED_ACTIONS`/interception `window.fetch` (réservé aux POST), donc pas couvert par le test existant. Le nouveau test vérifie explicitement que le bloc `export_liste` appelle `requireAdmin`, que la liste blanche est bien vérifiée avant tout appel Graph, que le dashboard transmet bien le jeton de session dans l'URL, et qu'il n'a pas été ajouté par erreur à `GATED_ACTIONS` (où il n'aurait aucun effet).
- **`ARCHITECTURE_GLOBALE.md` créé à la racine** : cartographie complète générée en lisant le code réel (`worker.js`, `app.js`, `dashboard.html`) plutôt que la documentation seule — schéma de la chaîne, inventaire des modules/écrans, les 74 actions du Worker classées par niveau de protection (25 `requireAdmin`, 33 `requireGarant`, 16 publiques dont une orpheline), les listes SharePoint et leurs relations, l'inventaire des fichiers du dépôt, les dépendances CDN.
- **Écarts doc/code trouvés au passage et documentés dans `ARCHITECTURE_GLOBALE.md` §7, non corrigés dans le comportement** (hors périmètre de cette session, qui ne devait modifier aucun comportement métier existant) :
  - Le module **Absences** (liste SharePoint `Absences`, action `signaler_absence`, onglet dashboard, capacités `ROLE_CAPS.absences`/`voitAbsences`) existe pleinement dans le code mais n'est mentionné nulle part dans `00_*`-`05_*`.
  - `Chantiers` référencé comme liste SharePoint dans `02_MODELE_DONNEES.md` alors qu'aucune liste de ce nom n'est utilisée en pratique (voir plus haut).
  - Deux fichiers JSON orphelins supplémentaires à la racine (`outillage_durees.json`, `outillage_grille_ajouts.json`), 0 référence dans le code — probablement les données sources d'une migration ponctuelle déjà appliquée puis jamais nettoyées du dépôt.
  - L'action Worker `maj_duree_amort` existe encore, **sans protection `requireAdmin`/`requireGarant`**, mais n'est plus appelée nulle part dans `dashboard.html` — un endpoint d'écriture public orphelin sur une colonne (`Duree_Amortissement`) documentée comme inerte depuis juillet 2026. Signalé mais non supprimé cette session (respect de la consigne de ne rien changer au comportement métier existant) — à traiter dans une session dédiée.
  - `ROLE_CAPS` (`app.js`) a plus de capacités que les « cinq capacités possibles » présentées en introduction de `03_REGLES_METIER_ET_ROLES.md` (`absences`, `voitAbsences` non documentées du tout ; `gererInventaire`/`compterInventaire` documentées plus loin dans le même fichier mais pas rattachées à la liste générale).
- **Vérification finale** : `npm run verify` (syntaxe + 33 tests, dont les 4 nouveaux) passe avant tout push, comme l'exige le hook `pre-push`.

## Refonte visuelle du dashboard — design system (août 2026)

- **Demande de William** : faire passer `dashboard.html` au niveau visuel d'un éditeur SaaS professionnel, sans changer aucun comportement fonctionnel — navigation, cartes KPI, tableaux, formulaires, badges de statut harmonisés, toasts/modales à la place des `alert()`/`confirm()` natifs, squelettes de chargement, états vides, tablette. Contrainte forte : zéro framework, zéro build, gabarits imprimables (fiches EPI/Outillage/relevé de sortie) strictement inchangés.
- **Incident constaté avant de commencer, résolu en amont** : `Desktop\Immos` (dossier de travail sans `.git`) avait une doc plus récente (mentionnant déjà "Lot 3") que le vrai dépôt `immo-tracker`, qui lui avait le code le plus à jour. Les deux sessions avaient divergé. Resynchronisé (`00_*`-`05_*` + `CLAUDE.md` copiés depuis `Desktop\Immos` vers `immo-tracker`) avant de commencer ce travail, comme la session précédente l'avait déjà fait une fois — `Desktop\Immos` reste un dossier à ne plus utiliser pour du travail sur le code, seul `C:\Users\ral\immo-tracker` est le dépôt git réel.
- **`design-system.css`** (nouveau fichier racine, simple `<link>` depuis `dashboard.html`) : palette dérivée du logo Espace Soleil (dégradé or/bronze sur noir/charbon) — plutôt que d'étaler l'or en aplat sur toute l'interface (peu lisible en grande surface), il sert d'**accent de marque** (CTA, indicateur actif, focus, liens) sur un habillage neutre charbon/blanc, pattern courant des interfaces pro. Typo, espacements, rayons, ombres et transitions sur une échelle cohérente. Variables de mode sombre prêtes (`:root[data-theme="dark"]`) mais **non activées** (aucun bouton de bascule, hors périmètre demandé).
- **Aucune variable CSS historique renommée** : `--blue`, `--orange`, `--green`, `--red`, `--yellow`, `--grey`, `--bg`, `--card` (utilisées des centaines de fois dans `dashboard.html`) sont conservées telles quelles, mais leur valeur dans le `:root` inline de `dashboard.html` a été changée en une seule ligne pour pointer vers les nouveaux tokens `--ds-*` (ex. `--blue:var(--ds-primary)`). Toute l'interface hérite donc de la nouvelle palette sans qu'aucun des centaines d'usages de `var(--blue)` dans le fichier n'ait eu besoin d'être touché — le principal levier de cette refonte, à risque de régression quasi nul.
- **Contraste texte blanc / fond doré vérifié avant de choisir la teinte finale** : un or trop clair (gold-500) tombait sous un ratio de contraste lisible pour les nombreux boutons `background:var(--orange);color:#fff` déjà existants dans le fichier. Choix d'un gold-600 plus soutenu (`#A67A1E`) qui reste clairement "or" tout en gardant le texte blanc lisible partout où c'était déjà le cas — zéro bouton à corriger individuellement.
- **Navigation** : sidebar et barre mobile modernisées en place (mêmes ids/classes, `#sidebar`, `.nav-item`, `#bottom-nav`, `.bnav-item`) — indicateur actif remplacé par une barre d'accent dorée + fond teinté (au lieu d'un bloc orange plein), transitions affinées.
- **Nouveau palier tablette** (`@media(min-width:769px) and (max-width:1024px)`) : sidebar transformée en rail d'icônes (60px, labels masqués, badges en pastille) plutôt que de basculer directement en navigation basse pensée pour un pouce — le breakpoint mobile existant (768px) n'est pas modifié.
- **Cartes KPI** : puce d'icône colorée (teinte sémantique reprise des classes `.badge` existantes) ajoutée devant chaque valeur — un seul point de rendu JS à modifier (`renderVueGenerale`), classe `.kpi-icon` au lieu d'un style inline.
- **Tableaux** : `.tbl-wrap` devient un conteneur à hauteur bornée (`max-height:65vh`) avec en-tête `<th>` collant (`position:sticky`) — pattern universel qui fonctionne quel que soit l'endroit où le tableau est monté (page, tiroir fiche, modale), puisque sticky reste toujours relatif à son propre conteneur scrollable. Zébrage discret + survol teinté d'or clair. Indicateur visuel (fond doré + flèche ▲▼) ajouté sur les boutons de tri déjà existants de l'onglet Dépôt (`sortDepot`), sans ajouter de nouveau comportement de tri.
- **Badges de statut harmonisés entre modules** : plusieurs modules affichaient le même concept avec un traitement visuel différent de celui des Immos — statut Matériel IT et Lignes téléphoniques en texte coloré simple converti en pastille `.badge` (même classes `bn`/`bh`/`ba` que les états d'immo) ; stock bas EPI/Outillage (texte coloré) converti en pastille `.badge` `bn`/`bu`/`ba` (les seuils métier de rupture, différents entre les deux catalogues, n'ont pas été touchés) ; bouton actif/inactif employé passé des couleurs hex dupliquées aux tokens `--ds-success-bg`/`--ds-danger-bg` (même source de vérité que les badges).
- **Toasts** (remplacent `alert()`) : `window.alert` est surchargé pour afficher un toast non bloquant au lieu du popup natif — **aucun des ~144 sites d'appel `alert(...)` n'a eu besoin d'être modifié**, puisque `alert()` ne retourne jamais de valeur exploitée par l'appelant. Le type de toast (succès/erreur/avertissement/info) est déduit automatiquement du texte du message (préfixe ✅/❌/⚠️ déjà utilisé de façon quasi systématique dans les messages existants).
- **Modale de confirmation** (remplace `confirm()`) : `confirm()` natif est synchrone, une modale stylée ne peut pas l'être — `confirmModal()` retourne donc une `Promise<boolean>`. Les **34 sites d'appel** de `confirm()` ont été convertis en `await confirmModal(...)` un par un (jamais en bloc automatisé, pour vérifier chaque contexte individuellement) : la plupart des fonctions englobantes étaient déjà `async` ; 5 fonctions top-niveau (`declarerVol`, `validerSortieDefinitive`, `declarerPanneDash`, `marquerImmoInactive`, `resoudrePanne`) et 2 gestionnaires `onclick`/`onchange` anonymes imbriqués (dans `renderTransferts`/`renderResa`) ont dû être rendus `async`. Comportement de gating vérifié bout en bout dans le navigateur (exécution suspendue à l'`await`, reprise correcte après clic) avant de considérer la conversion terminée.
- **Découverte incidente, non corrigée (hors périmètre)** : `declarerVol`, `marquerImmoInactive` et `resoudrePanne` ne sont en réalité **appelées nulle part** dans `dashboard.html` (aucune référence trouvée en dehors de leur propre définition, aucun dispatch dynamique `window[...]` dans le fichier) — du code mort préexistant, dans le même esprit que l'action Worker orpheline `maj_duree_amort` déjà relevée dans `ARCHITECTURE_GLOBALE.md`. Signalé pour une éventuelle session de nettoyage, non touché ici.
- **Squelettes de chargement** : `#loading-screen` (l'unique écran de chargement de l'app — les données sont chargées en un seul appel global, pas par onglet) remplacé par un aperçu squelette (4 blocs KPI + lignes de texte, animation shimmer) plutôt qu'un simple spinner, pour préfigurer la forme du contenu à venir.
- **États vides** : la classe `.es`, déjà utilisée par une trentaine de messages "Aucun·e..." à travers tous les modules, enrichie d'une icône et d'une mise en forme plus posée — un seul point de CSS à modifier profite à tous les écrans vides existants sans toucher au JS.
- **Palette Chart.js harmonisée** : les 3 graphiques (répartition circulation/dépôt, distribution des états, top détenteurs) utilisaient des couleurs hexadécimales figées correspondant à l'ancienne palette — remplacées par les valeurs de la nouvelle palette (or, vert, bleu/orange/gris déjà utilisés ailleurs pour les états).
- **Gabarits imprimables vérifiés strictement inchangés** : les 3 gabarits (fiches EPI, fiche Outillage, relevé de sortie) sont des documents HTML autonomes générés par `window.open('','_blank')` — jamais liés au `<style>`/`design-system.css` de la page parente, donc structurellement immunisés contre cette refonte. Vérifié malgré tout par comparaison **octet pour octet** avec le dernier commit (script Node comparant les 3 blocs `'<style>'+...+'</style></head><body>'+...` et leur template complet jusqu'à `</html>`) : les trois sont strictement identiques. Vérification visuelle complémentaire faite en générant une vraie fiche EPI de test dans le navigateur (via les fonctions réelles `epiFicheBodyHtml`/`EPI_FICHE_CSS_RULES`) : rendu bleu marine d'origine inchangé, fichier de test supprimé après coup.
- **Méthode de travail** : par tranches (navigation → KPI → tableaux → formulaires/badges → toasts → confirmation → squelettes/vides → tablette → charts → vérif impression), avec `npm run verify` après chaque tranche et vérification visuelle réelle dans un navigateur (serveur statique local + `.claude/launch.json`, contournant les limites de rendu d'un simple `file://`) plutôt qu'une relecture de code seule.
- **Aucune donnée réelle consultée** : toutes les vérifications visuelles ont été faites avec des données factices injectées côté client (le Worker de production n'a reçu aucun appel d'écriture) — le login réel n'a jamais été nécessaire.

## Application du design system à la PWA terrain (août 2026)

- **Demande de William** : étendre le design system créé pour le dashboard à la PWA terrain (`index.html` + `app.js`), en pensant explicitement au public réel — ~97 collaborateurs de chantier, sur smartphone, parfois en plein soleil, parfois avec des gants, parfois sans réseau (Mayotte). Question posée en aparté avant de commencer : mode sombre pour le dashboard — **confirmé hors périmètre pour la PWA** (un fond sombre est moins lisible en plein soleil), scope dashboard-only, timing laissé à l'appréciation de l'assistant — reporté à une session dédiée pour ne pas mélanger deux sujets sans rapport dans la même session.
- **Deux fichiers CSS concurrents découverts dans la PWA** : `style.css` (feuille externe) et un `<style>` inline dans `index.html`, tous deux définissant un `:root` avec les mêmes noms de variables (`--blue`, `--orange`...) et plusieurs classes en double (`.btn-tertiary` notamment, avec des valeurs différentes). L'inline gagne toujours la cascade (chargé après), rendant une partie de `style.css` silencieusement morte — un piège de désynchronisation si jamais l'ordre de chargement changeait. Les deux `:root` ont été alignés sur les mêmes tokens `--ds-*` plutôt que de refactoriser l'architecture (hors périmètre d'une session de style pur) ; `.btn-tertiary` réconcilié entre les deux fichiers.
- **Bonne surprise** : les couleurs de badges d'état de la PWA (`.etat-neuf/.etat-bon/.etat-use/.etat-abime/.etat-horsservice` dans `style.css`) étaient déjà, par le hasard d'un développement antérieur cohérent, strictement identiques aux couleurs `.badge .bn/.bb/.bu/.ba/.bh` du dashboard. Ces classes CSS se sont toutefois révélées **jamais utilisées** dans `app.js` (harmonisées quand même, sans effet visuel, par cohérence) — le vrai rendu des badges d'état passe par une fonction JS distincte, `ebadge()`, qui avait deux couleurs légèrement différentes du dashboard (Abîmé, Hors service) : corrigées pour pointer sur les mêmes tokens `--ds-*`. `rsLabel()` (statuts de réservation) suffixe un canal alpha directement sur une chaîne hexadécimale (`${c}22`) — incompatible avec `var(...)`, donc converti en valeurs hex littérales recopiées des tokens plutôt qu'en `var()`.
- **Cibles tactiles ≥48px** : mesuré au pixel près dans le navigateur (`getBoundingClientRect`) plutôt que déduit du CSS — `.btn` mesurait 47px (à 1px de la cible), `.btn-link` 34-39px, `.photo-btn-label` ~32px, le label de la case à cocher "chute de câble" ~20px de haut malgré une largeur pleine. Tout remonté à `min-height:48px` (floor qui survit aux surcharges de padding plus spécifiques comme `.btn-group .btn`), revérifié après coup : tous les boutons de tous les écrans testés mesurent désormais 48-53px.
- **CTA principaux ancrés en bas d'écran (accessibles au pouce)** : nouvelle classe utilitaire `.sticky-actions` (`position:sticky;bottom:0`), appliquée au bouton principal + son "Annuler"/"Retour" adjacent sur une dizaine d'écrans de formulaire. Choix technique validé par la mesure : `position:sticky` reste relatif à son propre conteneur (`.screen`, qui défile avec la page) — fonctionne quel que soit l'endroit où il est monté, sans JS, et cède naturellement la place une fois la fin réelle du contenu atteinte (pas de recouvrement permanent du bouton "Retour" qui suit parfois derrière). Vérifié par mesure de position avant/pendant/après défilement, pas seulement visuellement.
- **États "envoi en cours" anti double-soumission** : nouveau helper `setBusy(btn, libellé)`/`clearBusy(btn)` (généralise le pattern `dataset.busy` déjà utilisé une fois pour l'inventaire), appliqué aux 8 fonctions de soumission qui attendent réellement une réponse réseau avant tout changement d'écran (`accepterTransfert`, `validerForceAttribution`, `soumettreAbsence`, `soumettreLigneInventaire`, `soumettreReservation`, `validerModifResa`, `soumettrePanne`, `soumettreResolution`, `retraitConfirm2`). Chaque bouton passe `this` en argument à l'`onclick` (seul changement de signature — aucune fonction n'était appelée depuis un autre endroit, vérifié un par un avant modification). **`confirmerAvecEtat` volontairement exclue** : elle navigue déjà de façon optimiste vers l'écran de confirmation *avant* d'attendre le réseau (`showRecap()` appelé en premier), donc le risque de double-soumission y était déjà nul par un mécanisme différent, antérieur — ajouter un état "busy" n'aurait rien apporté. Cycle busy→clear vérifié de bout en bout dans le navigateur (bouton désactivé + libellé "⏳ Envoi..." pendant l'attente, restauré après réponse).
- **Flux de scan retravaillé** : état "📷 Démarrage de la caméra..." affiché avant le flux vidéo (auparavant rien, ou directement l'erreur de permission) ; confirmation systématique (vibration + toast "✅ Scanné !") sur chaque scan réussi, centralisée dans `startScanner()` plutôt que dupliquée dans chacun des 9 callbacks ; **repli de saisie manuelle générique**, injecté sous la zone de scan sur tous les écrans de scan (auparavant réservé au seul écran d'activation) — réutilise exactement le même callback qu'un scan réussi (`cb(codeDécodé)`), donc zéro logique dupliquée, et fonctionne même caméra bloquée. **Écran d'activation explicitement exclu** de cette injection générique : il a déjà son propre repli manuel (`activationManuelle()`, un `prompt()` dédié), dont le callback attend un format `code|nom` différent de ce qu'un humain taperait dans un champ texte simple — les faire cohabiter aurait cassé l'un des deux chemins silencieusement. Son bouton existant a été rendu plus visible (`.btn-link` gris 12px → `.btn-tertiary` pleine largeur 48px) pour répondre à la même demande ("bascule bien visible") sans dupliquer le mécanisme.
- **Bannière hors-ligne avec horodatage** : nouvelle fonction `marquerSyncReussie()` (appelée après chaque chargement réussi de `immos.json`/`employes.json`, les deux seuls fichiers mis en cache par `sw.js` — les appels au Worker sont sur une autre origine, jamais interceptés) écrit un horodatage en `localStorage`, uniquement si `navigator.onLine` est vrai au moment du fetch (un fetch qui aboutit alors qu'on est hors-ligne vient forcément du cache, pas d'une donnée fraîche — l'horodater comme "maintenant" aurait été trompeur). La bannière affiche "Hors ligne — données du [date/heure]" en réutilisant `fmtDT()` déjà existant, avec un repli explicite ("données jamais synchronisées") si aucun horodatage n'existe encore. Vérifié dans le navigateur en simulant `navigator.onLine=false` (les deux cas).
- **`sw.js`** : `design-system.css` ajouté à `APP_SHELL`, `CACHE_VERSION` passé à `v2`. **Gap découvert au passage** : `style.css` n'était lui-même jamais mis en cache explicitement (seul `app.js` l'était) — corrigé dans la même liste, cohérent avec l'objectif de la tâche (compléter la coquille applicative). Clonage de réponse synchrone du gestionnaire `fetch` (déjà correct depuis l'incident précédent, voir plus haut dans ce journal) non touché. Vérifié par un cycle complet désinscription → réinscription → inspection du cache : les 10 fichiers de la coquille sont bien présents.
- **Vérification systématique à 380px** : les 12 écrans les plus représentatifs testés un par un (`document.documentElement.scrollWidth` vs `clientWidth`) — aucun débordement horizontal détecté sur aucun.
- **Mode sombre dashboard : non traité cette session**, comme convenu — les variables `--ds-*` sombres restent prêtes (préparées la session précédente) mais sans bascule fonctionnelle. À faire dans une session dédiée si confirmé.

## Seuils d'alerte stock EPI + Outillage (août 2026)

- **Demande de William** : trois fonctionnalités indépendantes dans une même session (seuils d'alerte stock, campagne d'inventaire physique par scan, recherche globale dashboard), livrées une par une avec `npm run verify` entre chaque. Cette entrée couvre la première.
- **Colonne `Stock_Mini`** ajoutée sur `Catalogue_Articles_EPI` et `Catalogue_Outillage` (Nombre) — créée par William en tout début de session, avant même que le code ne soit écrit (étape bloquante communiquée en premier, comme pour chaque nouveau module).
- **Repli sur les seuils historiques codés en dur** : `0`/vide sur `Stock_Mini` → seuil par défaut de 5 (EPI) / 3 (Outillage), exactement les valeurs déjà utilisées avant cette fonctionnalité pour colorer les badges de stock. Décision pour que la fonctionnalité soit utile dès le déploiement (badges déjà corrects) sans obliger William à renseigner 40+ seuils avant que quoi que ce soit ne s'affiche, et sans changer le comportement des articles pas encore configurés.
- **Deux nouvelles actions Worker** (`maj_stock_mini_epi`, `maj_stock_mini_outillage`), protégées `requireGarant` (même population que les actions de stock existantes `editer_stock_outillage`/`bulk_maj_stock_epi`), ajoutées à `GATED_ACTIONS`. Édition un par un via `prompt()`, même pattern que l'édition de stock existante — pas de saisie en masse, jugée inutile pour un réglage ponctuel.
- **Suggestion de commande — réutilisation exacte de la logique "reste à distribuer"** plutôt qu'un nouveau calcul : côté Outillage, `calculerManquantsOutillage()` (déjà utilisé par l'onglet du même nom) est directement réemployé, juste agrégé par article. Côté EPI, qui n'a pas d'équivalent direct (la dotation est annuelle, pas un kit one-shot comme l'outillage), une nouvelle fonction `calculerManquantsEPI(annee)` reproduit le même principe que la carte "Nouveaux entrants sans dotation" déjà existante (même source : `epiEmployesSansFiche`, même calcul par employé via `epiCalculerBesoinStock`), simplement agrégée par article plutôt que listée par employé.
- Formule de suggestion (identique EPI/Outillage) : `à commander = besoin restant à distribuer + seuil mini − stock actuel`, plafonnée à 0. Vérifiée par calcul manuel sur un jeu de données factices avant d'être considérée correcte (pas seulement relue).
- **Carte récapitulative "articles sous seuil"** en tête de chaque onglet Stock (EPI et Outillage), cliquable pour filtrer directement la liste sur l'article concerné.
- **Nouvel onglet "🛒 Suggestion de commande"** ajouté aux deux modules, même emplacement dans la barre de sous-onglets (juste après "Stock").
- Aucune nouvelle capacité `ROLE_CAPS`/droit : réutilise `peutGererEPI`/`peutGererOutillage` déjà en place.

## Campagne d'inventaire physique des immobilisations par scan (roadmap item A1, août 2026)

- **Deuxième des trois fonctionnalités de la session** (voir entrée précédente pour le cadrage global). Reprend la roadmap item A1 (`05_ROADMAP_EVOLUTIONS_FUTURES.md`), jamais développée jusqu'ici, jugée dépendante de la pose préalable de QR codes/plaques sur les immos — supposée déjà en place ou en cours côté William pour que cette fonctionnalité ait un sens à l'usage.
- **Décision structurante : isolation totale vis-à-vis de `Mouvements`**. La logique "détenteur courant = dernier Mouvement" est le cœur de tout le reste de l'app (circulation, dépôt, analyses, amortissement, export comptable) — y toucher pour un scan d'inventaire aurait été le risque le plus élevé de cette fonctionnalité. Choix : deux nouvelles listes entièrement séparées (`Campagnes_Inventaire_Immos`, `Scans_Inventaire_Immos`), un scan n'écrit jamais de `Mouvement`, review explicite de ce point avant d'écrire la moindre ligne de code.
- **Deux nouvelles listes SharePoint annoncées avant codage** (colonnes exactes dans `02_MODELE_DONNEES.md`), même principe bloquant que pour chaque module précédent — création confirmée par William avant la mise en service réelle (le code est livré et testé en isolation dans l'intervalle, comme d'habitude).
- **Écarts calculés uniquement à partir de données déjà fiables** : par manque de signal de "détenteur théorique" au sens propre (rien dans le modèle actuel ne dit qui est censé avoir une immo à un instant T, hors dernier mouvement réel), le rapport se limite à deux écarts défendables avec les données existantes — site du scan ≠ `Immos.Site`, et immo scannée alors que `Immos.Actif = Non` (sortie du parc). Pas de tentative de deviner un "détenteur théorique" qui n'existe pas dans le modèle de données — aurait produit un rapport trompeur.
- **Bug trouvé par test en navigateur avant livraison, pas par relecture de code** : la première version du calcul du taux de couverture ("Immos vues") comptait toutes les immos scannées (y compris celles hors service/sorties) au numérateur, contre le total des immos actives au dénominateur — avec un jeu de données factice (2 immos actives, 1 sortie ; 1 active scannée, 1 active non scannée, la sortie scannée), affichait à tort "2/2 (100%)" au lieu de "1/2 (50%)". Corrigé en ne comptant au numérateur que les immos **actives** effectivement vues — une immo sortie scannée reste un écart, pas une preuve de couverture.
- **Scan PWA volontairement non protégé par jeton de session**, cohérent avec la contrainte du sujet ("respecte le modèle de confiance de la PWA terrain, pas de mot de passe") : `enregistrer_scan_inventaire_immo` suit exactement le même modèle que `reserver`/`transfert`/`ajouter_ligne_inventaire` (identité reçue du corps de la requête, pas de jeton), ajoutée à `PWA_SHARED_ACTIONS` dans `security.gated-actions.test.js` pour que le garde-fou anti-régression ne la signale pas à tort comme un oubli de protection. Accès à l'écran filtré côté PWA aux codes admin uniquement (`CONFIG.admins`), comme le reste de la section admin déjà présente sur l'écran d'accueil.
- **Lancement/clôture de campagne, à l'inverse, strictement `requireAdmin`** côté dashboard : une seule campagne active à la fois, sa clôture fige le rapport d'écarts — identité (`Cree_Par`/`Cloture_Par`) toujours résolue depuis `_auth.session.code`, jamais depuis le corps de la requête, conformément à la contrainte explicite du sujet.
- **Scan en continu côté PWA** : contrairement à tous les autres écrans de scan de l'app (un scan → une action → navigation), l'écran d'inventaire ré-arme la caméra automatiquement après chaque scan enregistré (`startScanner` gère déjà lui-même l'arrêt/redémarrage du flux vidéo précédent, réutilisé tel quel) — le geste terrain (scanner toutes les immos d'un dépôt à la suite) est répétitif par nature, contrairement aux autres flux qui traitent une immo puis reviennent à un menu.
- **Petit oubli corrigé au passage** : `ajouter_ligne_inventaire` (campagne de stock, déjà existante) manquait de `PWA_SHARED_ACTIONS` dans le test de garde-fou — gap pré-existant sans lien avec cette fonctionnalité, repéré en modifiant ce même fichier de test, corrigé dans la foulée plutôt que laissé de côté.
- Aucune nouvelle capacité `ROLE_CAPS` : l'accès dashboard réutilise `estAdmin()` (déjà utilisé pour d'autres actions strictement admin), l'accès PWA réutilise `CONFIG.admins` (déjà utilisé pour la section admin existante).

## Recherche globale dashboard (août 2026)

- **Troisième et dernière des trois fonctionnalités de la session** (voir les deux entrées précédentes pour le cadrage global). La plus petite des trois par surface de code, volontairement : sujet spécifié comme "purement client-side, aucune nouvelle action Worker" dès le départ, donc pas de nouvelle liste SharePoint ni de nouveau garde-fou de sécurité à concevoir.
- **Décision de conception principale : réutiliser plutôt qu'inventer**. Les immobilisations et employés ont déjà une fiche (panneau latéral `ouvrirFiche`) : la recherche s'y branche directement, aucun nouvel écran de détail. Les articles EPI/outillage n'ont pas de fiche individuelle et n'en ont jamais eu besoin ailleurs dans l'app — plutôt que d'en créer une pour ce seul usage, la sélection redirige vers l'onglet Stock déjà existant, pré-rempli avec la référence dans le champ de filtre déjà en place. Zéro nouvelle UI de détail construite.
- **Compromis assumé sur la fraîcheur des catalogues EPI/outillage** : la recherche lit `epiCatalogue`/`outilCatalogue`, qui ne sont chargés en mémoire qu'à la première ouverture de leur onglet respectif dans la session (comportement de chargement paresseux déjà en place, pas modifié par cette fonctionnalité). Un article EPI ne sera donc pas trouvable tant que l'onglet EPI n'a pas été ouvert au moins une fois — accepté plutôt que de forcer le chargement anticipé de toutes les données au démarrage du dashboard (coût réseau non justifié pour un gain marginal, les immos/employés étant de toute façon déjà chargés en premier).
- **Raccourci `/` avec garde anti-collision** : n'intercepte la frappe que si aucun autre champ texte n'a le focus (input/textarea/select/contenteditable), sur le modèle de GitHub/Slack — testé explicitement (frappe `/` dans le champ PIN de connexion ne détourne pas le focus).
- **Vérifié en navigateur avant livraison** (pas seulement relu) : recherche par n° de série d'immo, par nom d'employé, par référence EPI et par marque d'outillage, chacune retournant le bon groupe ; état vide correct ; seuil de 2 caractères minimum respecté (pas de recherche sur un caractère isolé, trop bruyant sur ~1000 immos) ; `/` focalise bien le champ ; Échap ferme le panneau et vide le champ.
- Aucune nouvelle capacité `ROLE_CAPS`/droit dashboard : la recherche elle-même est ouverte à tout utilisateur connecté (elle ne fait que filtrer des données déjà chargées pour lui), la navigation vers un article EPI/outillage revérifie `peutVoirEPI`/`peutVoirOutillage` au moment du clic.
- **Session terminée** : les trois fonctionnalités demandées sont livrées, chacune vérifiée par `npm run verify` (33/33 tests) avant de passer à la suivante. Deux nouvelles listes SharePoint restent à créer par William (`Campagnes_Inventaire_Immos`, `Scans_Inventaire_Immos`, colonnes détaillées dans `02_MODELE_DONNEES.md`) avant mise en service réelle de la campagne d'inventaire physique — étape bloquante communiquée en cours de session, comme pour chaque nouveau module.

## Audit et durcissement de sécurité (9-10 août 2026)

- **Demande de William** : après création par lui des listes `Campagnes_Inventaire_Immos`/`Scans_Inventaire_Immos` (session précédente), audit et durcissement de sécurité en 6 points — limitation de débit sur la connexion, audit d'exhaustivité des actions protégées, en-têtes de sécurité/CORS, scrubbing PII sur Sentry, absence de secrets dans le dépôt public, rapport de synthèse. Contrainte explicite et rappelée en tête de consigne : ne jamais casser le modèle assumé de la PWA terrain (badge scanné, sans mot de passe).
- **Limitation de débit** : verrou progressif en mémoire du Worker (Map au niveau module, pas de KV/Durable Object — reste dans la philosophie "zéro infrastructure" du projet, assumé "best effort" et documenté comme tel plutôt que présenté comme une garantie). Paliers 5/7/10 échecs → 30s/5min/15min. Un piège évité dès la conception : le dashboard sonde `verify_password` avec un mot de passe sentinel (`__probe__`) avant même que l'utilisateur ait tapé quoi que ce soit (pour savoir s'il doit créer un mot de passe ou en saisir un) — sans exclusion explicite, chaque connexion légitime aurait consommé une tentative avant de commencer. `reset_password` (admin) lève désormais aussi le verrou du code concerné, pour ne pas laisser un employé bloqué après une intervention légitime.
- **Vulnérabilité trouvée en creusant `set_password` pour brancher le verrou** : un flag `body.is_reset`, jamais envoyé par le dashboard (vérifié par grep) mais lu et respecté par le Worker sans aucune vérification d'identité, permettait de sauter la vérification de l'ancien mot de passe. Concrètement : n'importe qui connaissant le code d'un employé (les 97 codes terrain ne sont pas secrets — affichés sur les cartes BTP) pouvait écraser son mot de passe dashboard sans le connaître, juste en forgeant une requête POST avec `is_reset:true`. Corrigé en ne se fiant plus qu'à la présence réelle d'un hash existant (`withHash`) pour décider si l'ancien mot de passe est exigé — exactement le même critère que le flux légitime (`reset_password`, gated `requireAdmin`, efface le hash ; puis `set_password` sans hash à vérifier). Zéro changement de comportement réel puisque `is_reset` n'était de toute façon jamais envoyé — correction pure, sans régression possible.
- **Audit d'exhaustivité `GATED_ACTIONS`** : recompté par script indépendant (pas seulement relancé le test existant) — 27 `requireAdmin` + 35 `requireGarant` = 62 actions protégées, exactement les 62 entrées de `GATED_ACTIONS`, zéro écart. Le nombre total d'actions a grossi de 74 à 79 depuis le dernier inventaire (`ARCHITECTURE_GLOBALE.md`, 9 août) à cause des 5 actions ajoutées par la session précédente (seuils de stock, campagne d'inventaire immos) — toutes correctement classées, rien oublié. Seule anomalie confirmée : l'action orpheline `maj_duree_amort` (déjà signalée le 9 août, non traitée à l'époque pour respecter la consigne « ne rien changer au comportement métier » de cette session-là) — neutralisée cette fois sur le modèle déjà existant de `bulk_maj_immos` (`{success:false,error:'deprecated'}`), cohérent avec le fait que cette session est justement celle du durcissement.
- **CORS et en-têtes de sécurité** : `Access-Control-Allow-Origin: '*'` remplacé par une réflexion dynamique de l'origine réelle de la requête, limitée à `https://ral974.github.io` (repli sur cette même valeur si l'origine n'est pas reconnue — jamais de wildcard). N'affecte que les appels `fetch`/XHR cross-origin depuis du JS tiers, pas les visites directes d'URL (`?debug_*`, téléchargements de photos/FDS) ni les appels de la PWA/du dashboard eux-mêmes (même origine). Ajout groupé de `X-Content-Type-Options`, `X-Frame-Options`, `Content-Security-Policy`, `Referrer-Policy`, `Strict-Transport-Security` sur toutes les réponses — un seul objet `cors` déjà partagé par toutes les réponses du fichier (~20 `new Response(...)`), donc un seul point de correction pour tout couvrir d'un coup.
- **Scrubbing Sentry** : constat de départ rassurant — aucun `Sentry.setUser`/`captureException`/`captureMessage` explicite nulle part dans le projet (vérifié par grep), donc pas de fuite directe déjà en place. Le risque réel identifié est plus subtil : les breadcrumbs *automatiques* de Sentry (clics DOM, URLs de requêtes fetch/XHR, logs console) pouvaient embarquer un code employé — en particulier via les nombreux éléments `.link-emp`/`.link-im` du dashboard, qui portent des attributs `data-emp`/`data-cim`/`data-nom` sur chaque code/immo cliquable de l'app. Plutôt que d'auditer un par un les milliers de `console.log`/clics possibles, un scrubber générique (`beforeBreadcrumb`/`beforeSend`) a été ajouté dans les deux fichiers (`index.html`, `dashboard.html`, dupliqué comme le reste de la config Sentry faute de build partagé) : les breadcrumbs de catégorie `ui.click` sont supprimés entièrement plutôt que scrubés (le risque d'un attribut `data-*` non prévu dépassait la valeur de debug d'un simple clic), et le reste passe par un scrubber heuristique par motif (codes dans des paramètres d'URL connus, "NOM Prénom"/"Prénom NOM"). Vérifié en navigateur : `Sentry.getClient().getOptions()` confirme les deux callbacks actifs, et un breadcrumb `ui.click` de test est bien renvoyé `null`.
- **Audit des secrets, la partie la plus sérieuse de cette session** : trois trouvailles réelles, pas seulement théoriques.
  1. `?debug_employes=1` (endpoint de diagnostic non authentifié par conception, comme tous les `?debug_*`) faisait un echo brut de la réponse Graph — `MotDePasse` (le hash PBKDF2) y compris. Corrigé par redaction du champ (`[redacted]`) sans toucher au reste de la réponse, pour que le diagnostic reste utile.
  2. `?materiel_it=1` et `?lignes_telephoniques=1` renvoyaient en clair, sans aucune authentification, les codes PIN/PUK/RIO/déverrouillage des cartes SIM réelles de l'entreprise (~40 téléphones) — une classe de sensibilité très différente d'une simple donnée de localisation d'immo : un PUK ou un RIO connu permet de déverrouiller une carte ou de porter un numéro à l'insu de l'entreprise. Protégés `requireGarant`, jeton transmis en paramètre d'URL (`&token=`) sur le même modèle que `?export_liste=` (GET, pas de corps JSON). Ces deux modules sont 100% dashboard (aucun écran PWA) : le durcissement ne touche donc jamais le modèle de confiance terrain.
  3. **La plus grave** : `materiel_it_catalogue.json`, le fichier source de la migration Matériel IT (toujours suivi par git, servi tel quel par GitHub Pages), contenait ces mêmes PIN/PUK/RIO/déverrouillage réels en clair, dans un dépôt **public**. Les champs ont été redactés dans l'arbre de travail. **William a choisi de réécrire l'historique** (`git filter-repo --path materiel_it_catalogue.json --invert-paths` puis force-push) plutôt que de laisser le commit d'origine (`b48a4f7`) accessible — opération destructive assumée en connaissance de cause (casse tout autre clone existant du dépôt ; aucun autre clone connu à ce jour). Sauvegarde complète du dépôt prise avant l'opération par précaution. **Recommandation transmise, indépendante du sort de l'historique git** : ces codes doivent être considérés comme compromis (ils ont été exposés publiquement pendant plusieurs jours) et idéalement changés auprès de l'opérateur (Free Pro) — la réécriture d'historique seule ne suffit pas si les valeurs elles-mêmes ne sont jamais renouvelées, et ne couvre de toute façon pas d'éventuels autres clones/caches hors de portée de ce dépôt.
  4. **Décision de William : le PIN partagé reste en l'état.** `dashboard.html` contient un code d'accès partagé en clair (`var PIN='ES974'`) affiché avant le sélecteur d'identité. Ce n'est pas la vraie barrière de sécurité (le mot de passe par employé, vérifié serveur, reste intact et maintenant protégé par le verrou anti brute-force) — plutôt un filtre "cosmétique" contre les visiteurs curieux. Question posée explicitement (le retirer / le garder en connaissance de cause / le remplacer) : William a choisi de le garder tel quel, décision produit assumée et documentée plutôt que changée unilatéralement.
- **29 nouveaux tests** (4 nouveaux fichiers), suite passée de 33 à 62 — chaque durcissement vérifié par un test qui échouerait si la régression réapparaissait (pas seulement relu). `npm run verify` avant chaque commit, comme toujours sur ce projet.
- Aucune des 6 corrections n'a demandé de toucher au modèle de confiance de la PWA terrain — vérifié explicitement à chaque étape (actions partagées PWA jamais gated, testé par `security.gated-actions.test.js`, module Matériel IT/Lignes téléphoniques 100% dashboard donc gatable sans impact terrain).

## Nettoyage, mode sombre et cohérence visuelle du dashboard (10 août 2026)

- **Code mort retiré** : `declarerVol`, `marquerImmoInactive` et `resoudrePanne` (dashboard.html) — trouvées lors de l'audit de sécurité, signalées mais volontairement non touchées à l'époque ("hors périmètre : aucun changement de comportement"). Vérifié par grep qu'aucun appel (direct ou via chaîne construite dynamiquement) ne les référence : superseded depuis un moment par les flux modale actuels (`ouvrirModalSortie`/`validerSortieDefinitive` pour la sortie définitive/vol, `resoudrePanneComplet` pour la résolution de panne — formulaires structurés avec confirmation intégrée, remplaçant d'anciens enchaînements de `prompt()`/`confirm()`). Suppression pure, aucun test ni comportement affecté.
- **Mode sombre activé** : les variables `:root[data-theme="dark"]` existaient déjà dans `design-system.css` depuis la refonte visuelle de début août, explicitement laissées "prêtes, non activées... implémentation future d'un bouton de bascule". C'est cette session qui les active : bouton 🌙/☀️ dans le header (`toggleTheme()`), persistance en `localStorage`, et par défaut (première visite) alignement sur `prefers-color-scheme` — validé visuellement dans le navigateur avant activation, exactement la condition posée par la note d'origine.
  - **Écran de connexion inclus dès le chargement** : le thème est posé sur `<html>` par un script synchrone tout en tête de `<head>` (avant les feuilles de style), donc actif dès le PIN d'accès — pas seulement après connexion — sans flash clair→sombre.
  - **Séparation fond de "chrome" / couleur de texte, découverte en cours de route** : `--blue` (alias historique de `--ds-primary`) servait à la fois de fond pour le header/sidebar/boutons (a besoin de rester sombre dans les deux thèmes, texte blanc dessus) et de couleur de texte pour les titres/liens/valeurs sur les cartes (doit devenir clair en mode sombre, sinon texte quasi noir sur carte sombre = illisible). Un seul token ne pouvait pas servir les deux usages une fois le mode sombre activé. Résolu en séparant : nouveau token `--blue-chrome` (marine fixe, ne suit jamais le thème) réservé aux 26 usages de fond ; `--ds-primary` reste réservé au texte et devient une teinte bleu clair en mode sombre (réutilise `--ds-info-dark`, déjà introduit lors de l'audit de sécurité pour le même besoin sur les badges).
  - **Échelle neutre et couleurs "-dark" des badges inversées pour le mode sombre** : `--ds-neutral-50..900` n'étaient qu'un alias direct vers l'échelle `--ds-ink-*` (pensée pour du texte sombre sur fond clair) — sans inversion, un badge "neutre" clair en mode jour serait resté clair et illisible sur fond sombre. Ajout d'une échelle neutre inversée + de teintes `-dark` plus claires (succès/alerte/danger/info) spécifiquement pour le mode sombre.
  - **Sweep des couleurs codées en dur** dans le `<style>` inline de `dashboard.html` (body, écran de connexion, recherche globale, KPI, cartes détenteurs, gantt réservations, tiroir fiche, modale d'action, formulaires `addFieldStyle` réutilisé ~92 fois) vers les tokens `var(--ds-*)` — tout ce qui passe par ces tokens s'adapte automatiquement. Les gabarits d'impression (fiches EPI/Outillage en PDF) sont délibérément laissés en clair fixe : un document imprimé doit rester lisible sur papier, indépendant du thème d'écran.
  - **`color-scheme` posé sur `:root`** (`light`/`dark` selon le thème actif) en filet de sécurité pour les nombreux petits `<select>`/`<input>` de filtre par onglet qui n'avaient pas de `background`/`color` explicites (ex. sélecteurs de site répétés sur Transferts/Maintenance/Alertes/Utilisateurs) — fait passer leurs rendus natifs (liste déroulante, cases à cocher) en thème sombre côté navigateur sans avoir à retoucher chacun individuellement.
  - Vérifié en navigateur (pas seulement relu) : bascule clair↔sombre via le bouton, contraste des cartes KPI/graphiques/tiroir fiche/modales en mode sombre, régression du mode clair après le rework des tokens partagés.
- **Irrégularités de mise en forme EPI/Outillage corrigées** : contrairement aux autres listes (Dépôt, Circulation, Historique, Documents...), les vues EPI/Outillage (Stock, Dotations, Grille, Remise ponctuelle, Distribution, Prime annuelle) construisaient leur champ de recherche avec un style inline ad hoc (bordure/rayon différents, aucun état de focus doré cohérent avec `.filters`) et n'affichaient pas le badge de comptage `card-cnt` présent partout ailleurs (curieusement déjà correct sur les vues "Suggestion de commande", ajoutées lors de la session précédente en suivant le bon patron). Corrigé : le champ de recherche réutilise la classe `.filters` existante (mêmes bordure/focus/couleurs que partout ailleurs, gratuit une fois la classe posée), et un `card-cnt` a été ajouté à chaque vue listant réellement des enregistrements filtrables (Stock EPI/Outillage, Dotations, Remise ponctuelle, Distribution, Prime annuelle, "Ce qui reste à distribuer"). Les vues de type grille de configuration (Grille de dotation EPI/Outillage — un tableau de référence à 4×N ou 2×N cellules, pas une liste filtrée) ont volontairement été exclues : `card-cnt` n'y aurait pas de sens (pas de notion de "résultat filtré").
- Aucun nouveau test nécessaire (changements purement visuels/CSS, comportement inchangé) — `npm run verify` repassé après chaque étape pour confirmer l'absence de régression sur la logique existante (62/62).

## Sauvegarde complète des listes SharePoint (10 août 2026)
- **Constat de départ** : jusqu'ici, aucune sauvegarde d'ensemble des données. L'endpoint `?export_liste=` (créé avec le chantier rollback, voir plus haut) permettait d'exporter **une** liste à la fois, mais rien ne produisait un instantané complet du site SharePoint — un dépôt public, un push cassé ou une migration ratée pouvaient laisser William sans filet côté données (le rollback git ne touche jamais SharePoint).
- **Recensement** : 21 listes SharePoint réellement utilisées par le Worker. La whitelist `EXPORTABLE_LISTS` n'en couvrait que 19 — `Campagnes_Inventaire_Immos` et `Scans_Inventaire_Immos` (ajoutées avec la campagne d'inventaire physique des immos) manquaient, donc n'auraient jamais été sauvegardées. Complétée aux 21, et un test de garde-fou (`tests/backup.export-structure.test.js`) vérifie désormais automatiquement que **toute** liste lue/écrite via Graph dans `worker.js` figure dans la whitelist, pour ne plus jamais oublier une liste à la prochaine évolution.
- **Contrainte technique décisive : le plafond du plan gratuit Cloudflare** (~50 sous-requêtes `fetch` par invocation du Worker). Faire boucler le Worker sur les 21 listes en une seule invocation aurait explosé ce budget (chaque liste pagine sur plusieurs pages Graph). **Décision : orchestration côté dashboard (option C sur 3 étudiées)** — une invocation Worker par liste (`?export_liste=` répété 21 fois depuis le navigateur), le dashboard agrégeant les 21 réponses en un seul fichier JSON téléchargé à la fin. Chaque invocation Worker reste ainsi largement sous le plafond ; les alternatives (un méga-endpoint Worker unique, ou un service externe) auraient soit dépassé le budget, soit ajouté de l'infrastructure — contraire à la philosophie "zéro infra" du projet.
- **Anti-troncature silencieuse** : nouvelle fonction `paginateStatus()` (variante de `paginate()`) qui renvoie `complete:false` si la pagination s'arrête au plafond de pages (45) alors qu'un `@odata.nextLink` reste — pour une très grosse liste (>9000 lignes), le fichier signale explicitement qu'elle est tronquée plutôt que de la présenter comme complète. Le dashboard nomme alors le fichier `..._INCOMPLETE.json` et affiche quelles listes exporter séparément (ou passer le Worker en plan payant). Un instantané présenté à tort comme fiable serait pire que pas de sauvegarde du tout.
- **Garde-fous de tests** : suite passée de 62 à 68 (6 nouveaux tests) — structure du JSON, concaténation multi-pages, détection de troncature, refus hors whitelist, refus sans jeton Admin, et synchro stricte worker.js ↔ dashboard.html.
- **Restauration : volontairement pas d'outil automatique** (`PROCEDURE_RESTAURATION.md`) — une ré-injection de masse mal maîtrisée est plus dangereuse que le problème qu'elle corrige. La procédure privilégie d'abord la corbeille SharePoint (rétention 93 jours), puis une ré-injection assistée liste par liste (clé = `Title`, jamais réutiliser l'`id` SharePoint), en réutilisant les actions `bulk_import_*` déjà existantes.
- **Aucune nouvelle liste ni colonne SharePoint**, aucune nouvelle dépendance, modèle de confiance de la PWA terrain inchangé (l'endpoint reste `requireAdmin`, jamais partagé avec la PWA).

## Environnement de recette (staging), séparé de la production (10 août 2026)

- **Constat de départ** : tout était testé et déployé directement en production — le Worker `immo-proxy` se déploie automatiquement à chaque push sur `main`, et les données réelles (1023+ immobilisations) servaient de terrain de test pour toute action d'écriture. Demande explicite de William : un environnement de recette séparé, au coût le plus proche possible de zéro.
- **Deux options comparées avant tout code** (analyse écrite, validée par William avant implémentation) pour l'isolation des données :
  1. Même site SharePoint, listes préfixées `TEST_`, un Worker de recette lisant ce préfixe.
  2. **Site SharePoint séparé** (retenu), un Worker de recette pointant vers son propre `SITE_ID`.
  - **Argument décisif, trouvé en lisant le code réel plutôt qu'en devinant** : les noms de liste ne sont pas centralisés dans `worker.js` — ce sont ~150 littéraux dispersés (`GL+"/Immos/..."`, `GL+"/Employes/..."`, etc. sur 21 listes). L'option "préfixe" aurait exigé de paramétrer chacune de ces occurrences, avec un risque réel d'oubli (une seule occurrence non préfixée = une action de test qui écrit dans une vraie liste de production). L'option "site séparé" ne demande qu'**un seul changement** : `SITE_ID` lu depuis une variable d'environnement Cloudflare (`SITE_ID_ENV`), avec repli sur la valeur de production actuelle si absente — exactement le même pattern déjà en place pour `CLIENT_SECRET_ENV`/`SESSION_SECRET_ENV`. Moins de code touché *et* plus sûr : décision facile.
- **Niveau de séparation du pipeline de déploiement, également tranché avec William avant de coder** : entre "tout depuis `main`, même commit pour prod et recette" (isolation par les données uniquement) et "branche `staging` dédiée avec pipeline CI découplé" (isolation aussi dans le temps, mais exigeant une modification additive de `deploy.yml`, seule entorse identifiée à la consigne "ne rien changer au comportement de prod sauf strict nécessaire"). **William a choisi l'option simple** — cohérent avec le mode de fonctionnement du projet (push direct sur `main`, solo, sans revue de code) : le vrai test "avant que ce soit en ligne" se fait localement (serveur statique pointé sur le Worker de recette, pratique déjà utilisée lors des sessions de refonte visuelle), la recette hébergée servant surtout à la validation sur appareil réel (téléphone, caméra, PWA, hors-ligne) juste après un push.
- **Implémentation** :
  - `wrangler.toml` : bloc `[env.staging]` (`name = "immo-proxy-staging"`, `SITE_ID_ENV`/`ENV_NAME_ENV` en `vars`, non sensibles). Déployé comme une **ressource Cloudflare séparée**, connectée au même dépôt Git avec une commande de build dédiée (`npx wrangler deploy --env staging`) — à configurer par William dans Cloudflare, même geste que la connexion Git initiale de `immo-proxy`.
  - `worker.js` : `SITE_ID` et un nouveau `ENV_NAME` (exposé en en-tête `X-Immo-Env`, pur diagnostic) lus depuis l'environnement avec repli sur les valeurs de production — comportement de production strictement inchangé si les variables sont absentes, vérifié par un nouveau test (`tests/worker.staging-env.test.js`, suite passée de 68 à 70) qui interroge le Worker sans puis avec ces variables et vérifie que le bon site SharePoint est appelé dans les deux cas.
  - `app.js`/`dashboard.html` : détection de l'environnement de recette par le **chemin** de la page (`/staging/`), jamais par un paramètre d'URL — un chemin persiste au rechargement/partage/marque-page, contrairement à un `?recette=1` qui se perd dès qu'on navigue sans le reporter et n'est donc pas une protection fiable contre l'erreur. Un bandeau rouge permanent (« 🧪 RECETTE ») s'affiche dès que ce chemin est détecté, avant même l'écran de connexion du dashboard.
  - `staging/` : sous-dossier du dépôt, miroir **strictement identique** du code de production (`index.html`, `dashboard.html`, `app.js`, `style.css`, `design-system.css`, `sw.js`, logos), généré par un nouveau script (`scripts/sync-staging.js`) — jamais édité à la main. Un script de vérification (`scripts/check-staging-sync.js`) est ajouté à `npm run check` : un push qui modifierait un de ces fichiers sans avoir relancé la synchronisation **échoue** plutôt que de laisser la recette dériver silencieusement du code réel. `deploy.yml` (production) n'a **pas été modifié** : il uploade déjà tout le dépôt tel quel, donc `staging/` s'y trouve automatiquement inclus dès qu'il est commité.
  - `staging/manifest.json`, `staging/immos.json`, `staging/employes.json` : volontairement **non synchronisés** (seuls fichiers distincts entre les deux environnements) — un manifest différent (nom « RECETTE », couleur rouge, `start_url` propre) pour qu'une PWA installée depuis la recette ne se confonde jamais avec la vraie ; des catalogues **fictifs** (20 immos codées `IM900001`-`IM900020`, hors de toute plage réelle utilisée en production, et ~9 employés fictifs codés `FIC1`-`FIC9`, noms génériques suivis de `(test)` — aucune donnée personnelle réelle).
  - `scripts/seed-staging-data.js` : peuple les listes SharePoint de recette via les actions Worker déjà existantes (`ajouter_employe`, `ajouter_immo`, `importer_affectation_immo`) — aucune nouvelle action créée pour ce besoin. Un garde-fou explicite refuse de s'exécuter si l'URL cible ne contient pas "staging", pour qu'une erreur de copier-coller ne puisse jamais écrire du jeu de données factice dans la vraie base.
  - **Bootstrap identifié et documenté** (`PROCEDURE_RECETTE.md`) : impossible de se connecter au dashboard de recette tant qu'aucun employé n'existe (le Worker répond `employe_introuvable`), et impossible de créer un employé via l'app tant qu'on n'est pas connecté — un classique problème d'amorçage. Solution : une première ligne (`AIWI`, rôle Admin) doit être ajoutée **à la main** dans la liste `Employes` du site de recette avant tout, exactement comme au tout premier démarrage du système réel a dû l'être.
- **Listes SharePoint créées par William** : plutôt que de recréer les 21 listes colonne par colonne à la main (chronophage), découverte en cours de route d'un raccourci natif SharePoint — **"Créer une liste → À partir d'une liste existante"**, qui duplique la structure d'une liste (colonnes, types) sans copier son contenu, y compris en pointant vers une liste d'un autre site. William a ainsi dupliqué les 22 listes du site de production (`Logistique-Immos`) vers le nouveau site `Logistique-Immos-Recette` en quelques minutes plutôt que plusieurs heures — mise à jour dans `PROCEDURE_RECETTE.md` comme méthode recommandée (l'ancien plan « recréer à la main, s'aider de `?debug_columns=` pour les 3 listes non documentées » reste en repli si cette fonctionnalité n'était pas disponible sur un tenant).
- **Incident de configuration Cloudflare corrigé en cours de route** : le premier déploiement de `immo-proxy-staging` a été lancé avec la commande par défaut (`npx wrangler deploy`, sans `--env staging`) — un avertissement explicite de Wrangler ("Failed to match Worker name... Overriding using the CI provided Worker name") a révélé que le Worker avait été déployé avec la configuration de **premier niveau** de `wrangler.toml` (donc sans `SITE_ID_ENV`, avec repli silencieux sur le site de production). Repéré avant toute configuration de secret sur cette ressource, donc sans aucun risque réel — corrigé en modifiant la commande de déploiement Cloudflare (`Settings → Build → Deploy command` et `Version command` → `npx wrangler deploy --env staging`) puis en redéclenchant un build (commit vide poussé sur `main`). Vérifié après coup via `?debug_immos=1` : le Worker de recette renvoie bien 0 immo (liste vide, site séparé), par opposition aux 1023 de la production — preuve concrète de l'isolation, pas seulement une configuration supposée correcte.
- **Aucune donnée personnelle réelle copiée** dans le jeu de données factice — exigence explicite de William, respectée par construction (noms génériques inventés, jamais un vrai nom d'employé ou une vraie donnée d'immobilisation).
- **Mise en service complète et vérifiée** (10 août 2026) : site SharePoint de recette créé et peuplé (9 employés + 20 immos + 5 affectations factices via `scripts/seed-staging-data.js`, exécuté depuis le poste de William — le Worker de recette, comme la production, n'est volontairement joignable que depuis de vrais navigateurs/postes, pas depuis l'environnement d'exécution cloud de l'assistant, dont la politique réseau bloque les domaines `*.workers.dev` génériques), Worker `immo-proxy-staging` opérationnel avec ses propres secrets, bootstrap du compte `AIWI` effectué, bandeau RECETTE confirmé visible. Environnement pleinement utilisable pour la suite des développements.

## Journal d'audit des actions sensibles (fin août 2026)

- **Constat de départ, signalé par William** : `Mouvements` trace fidèlement le matériel, mais aucune action d'**administration** (mot de passe, droits/rôles, suppressions, modifications de fiches) ne laissait de trace exploitable — un blanc dans la traçabilité, complémentaire (pas redondant) au contrôle d'accès (`requireAdmin`/`requireGarant`) déjà en place depuis l'audit de sécurité du 9-10 août : ce dernier décide *qui a le droit*, le journal enregistre *qui a fait quoi*.
- **Étape 1 bloquante avant tout code** (demande explicite de William, cohérente avec le mode de fonctionnement du projet — chaque nouveau module attend la création manuelle des listes SharePoint) : proposition détaillée de la structure `Journal_Audit` (colonnes, types, traitement RGPD du champ `Detail`), validée par William puis créée par lui-même sur les deux sites (`Logistique-Immos` production **et** `Logistique-Immos-Recette`) avant que le code ne soit écrit.
- **Décision structurante : un point d'insertion unique plutôt que 62 modifications**. Avant de coder, vérification que les 62 actions gated suivent un pattern **strictement uniforme** (`if (action === 'xxx') { const _auth = await requireAdmin(body)|requireGarant(body); if (!_auth.ok) return json({success:false, error:_auth.error}, ...); ... }`, avec exactement 3 codes d'erreur possibles : `session_invalide`/`droits_insuffisants`/`session_secret_manquant`). Cette uniformité a permis d'envelopper l'intégralité de la chaîne `if (action === ...)` dans une fonction interne (`dispatchPost()`), appelée une seule fois — la journalisation inspecte la `Response` déjà produite (clonée) après coup, sans qu'aucun des 62 blocs n'ait eu besoin d'être modifié individuellement. Contrainte explicite de William respectée à la lettre (« si un tel point unique n'existe pas, dis-le-moi avant de coder ») — il existait bel et bien.
- **Identité toujours résolue depuis le jeton, jamais depuis le corps de la requête** — même règle que l'attribution des mouvements (voir "Autorisation côté serveur" plus haut). Le point d'insertion unique re-vérifie lui-même `body.token` (HMAC) plutôt que de dépendre de la variable interne `_auth.session` de chaque bloc, ce qui a justement permis de ne toucher aucun bloc individuel.
- **Garantie préexistante "zéro écriture SharePoint si l'auth échoue" délibérément préservée pour le journal lui-même** : les 3 réponses d'échec d'authentification sont explicitement exclues de la journalisation. Sans cette exclusion, deux tests d'intégration existants (`worker.integration.test.js`) auraient cassé, et surtout n'importe qui aurait pu faire grossir `Journal_Audit` en boucle avec des jetons invalides, sans même détenir un compte réel. Compromis assumé et documenté : une tentative avec un jeton invalide/absent n'est pas journalisée (elle reçoit déjà un 401/403, juste sans trace persistée).
- **Exception explicite pour les échecs de connexion** (`verify_password` raté, verrou anti brute-force déclenché) : demande spécifique de William ("utile avec le verrou anti brute-force existant"), traitée à part du mécanisme générique puisque `verify_password` ne passe jamais par `requireAdmin`/`requireGarant` (c'est le point d'entrée qui *produit* le jeton). Aucune session n'existe encore à ce moment : `Code_Employe` reste vide et le code **tenté** (non vérifié, potentiellement usurpé) va dans `Cible` — respecte la lettre de la règle d'identité (jamais une valeur non vérifiée dans `Code_Employe`) sans pour autant perdre le signal de monitoring.
- **Écriture best-effort, RGPD** : `logAudit()` s'exécute après l'action métier, absorbe ses propres erreurs (`try/catch`) et ne renvoie jamais rien à l'appelant — une panne de `Journal_Audit` ne doit jamais faire échouer une vraie action admin. Trace Sentry ajoutée en cas d'échec d'écriture, sur le projet Sentry déjà existant (nouveau tag `environment: 'worker'`) via l'API HTTP "Store" brute (pas de SDK `@sentry/cloudflare` : ce Worker n'a ni build ni `npm install`, un SDK à bundler aurait cassé le mode de déploiement "zéro build" — cohérent avec la philosophie déjà documentée dans `01_ARCHITECTURE_TECHNIQUE.md`). Le rapport Sentry ne contient jamais le corps de la requête ni de code employé, seulement le nom de l'action et un message d'erreur généré par le Worker lui-même — rien à scruber, contrairement au scrubbing PII déjà en place côté dashboard/PWA pour les breadcrumbs riches en contexte. Côté `Detail` : liste noire de clés (mots de passe, PIN/PUK/RIO/déverrouillage SIM, jeton de session, charges de fichier base64), et les imports en masse sont résumés en `{"__count": N}` plutôt que recopiés intégralement.
- **Lecture strictement Admin, nouveau pattern d'endpoint** : `?journal_audit=1` (GET, jeton en paramètre `&token=`), protégé `requireAdmin` — explicitement **pas** traité comme les ~35 endpoints GET publics existants (données de fonctionnement courant), conformément à la demande de William. Onglet dashboard "🗂️ Journal d'audit" (nouveau groupe "ADMINISTRATION" en bas de la barre latérale) masqué par défaut, révélé uniquement par `estAdmin(ADMIN_SESSION.code)` — réutilisation stricte du mécanisme de droits déjà existant, aucun nouveau système inventé (consigne explicite de William). Lecture seule : filtres employé/action/résultat/période, pas d'action possible depuis cet onglet.
- **Effet de bord positif, découvert en cours de route** : `Journal_Audit` est désormais couverte par le mécanisme de sauvegarde `?export_liste=` — un garde-fou de test déjà existant (`tests/backup.export-structure.test.js`) exige que toute liste SharePoint réellement lue/écrite par `worker.js` figure dans `EXPORTABLE_LISTS`, ce qui a mécaniquement forcé son ajout (22 listes désormais, au lieu de 21) plutôt que d'être oubliée.
- **Incident local sans conséquence sur le dépôt** : les premières éditions de `worker.js`/`dashboard.html` dans cette session ont fait basculer ces deux fichiers en fins de ligne CRLF (probablement un artefact de l'outillage d'édition sous Windows), cassant temporairement plusieurs tests qui analysent le code source par expression régulière (`\n` littéral). `core.autocrlf=true` déjà configuré sur ce dépôt aurait de toute façon normalisé en LF au moment du commit (donc aucun risque réel pour l'historique Git), mais corrigé immédiatement dans l'arbre de travail par prudence, pour que `npm test` reflète fidèlement ce qui sera commité.
- **Tests** : 16 nouveaux tests (4 fichiers) — synchronisation `GATED_ACTIONS_AUDIT` (worker) ↔ `GATED_ACTIONS` (dashboard) ↔ blocs réellement protégés ; protection de l'endpoint `?journal_audit=1` (même famille que `security.export-liste.test.js`) ; écriture réussie sur une action gated avec la bonne identité ; non-blocage de l'action métier quand l'écriture du journal échoue (+ trace Sentry) ; absence d'écriture si l'auth échoue ; journalisation d'un échec de connexion sans jamais exposer le mot de passe tenté ; fonctions pures `auditDetailFrom`/`auditCibleFrom` (exclusions RGPD, résumé des imports en masse). Suite passée de 70 à 86 tests. `npm run verify` (syntaxe + 86 tests) et `npm run sync:staging` passés avant toute chose — le code étant strictement identique entre production et recette (seul `SITE_ID_ENV` diffère), `Journal_Audit` fonctionne nativement dans les deux environnements dès que la liste existe des deux côtés (déjà fait par William).
- **Portée volontairement non couverte** : le champ `Detail` n'est pas HTML-échappé à l'affichage dans le dashboard, cohérent avec le reste du fichier (aucune fonction d'échappement HTML n'existe nulle part dans `dashboard.html` à ce jour — `Note`, `Commentaire`, `Motif`... sont déjà rendus de la même façon ailleurs). Risque pré-existant à l'échelle de tout le fichier, pas une régression introduite ici ; à traiter dans une session dédiée si jamais priorisé.

## Pseudonymisation d'employes.json (chantier en cours, fin août 2026)

- **Demande de la direction, relayée par William** : `employes.json` (noms et codes des ~99 salariés) est publiquement lisible sur GitHub Pages — déjà documenté comme un point ouvert depuis l'audit de sécurité du 9-10 août (« ce ne sont pas des secrets, mais restent des données personnelles publiquement lisibles »), la direction souhaite maintenant le corriger. Consigne explicite : analyse d'impact d'abord, validation avant tout code, implémentation par étapes réversibles (nouvelle voie posée et testée avant de retirer quoi que ce soit du public), pas de réécriture d'historique git sans accord explicite.
- **Cartographie précise avant tout code** : seuls deux endroits lisent `employes.json`, et dans les deux cas c'est un **repli**, jamais la source prioritaire — `chargerEmployes()` (app.js) et `loadEmployes()` (dashboard.html) tentent d'abord le Worker (`?employes=1`), le fichier statique n'intervient que si ce premier appel échoue. **Découverte rassurante** : le nom affiché pour l'utilisateur connecté sur la PWA (badge « 👷 Prénom Nom ») ne vient jamais de ce fichier — il est encodé directement dans le QR code de la carte BTP (`code|nom`) ou saisi en activation manuelle, stocké en `localStorage`. Le flux de scan de badge terrain, le plus fréquent et le plus sensible à la friction, n'a donc aucune dépendance au fichier concerné.
- **Découverte critique qui a changé le périmètre proposé initialement** : l'endpoint Worker `?employes=1` — déjà utilisé en priorité par le code existant — renvoie lui aussi l'intégralité des noms complets (+ poste, rôle, site, actif, mot de passe défini ou non) **sans aucune authentification**, exactement comme les ~35 autres endpoints GET publics déjà documentés. L'URL du Worker est publique (visible dans le code source public d'app.js/dashboard.html) : `curl .../immo-proxy.ral-85d.workers.dev/?employes=1` renvoie déjà tous les noms aujourd'hui, indépendamment de GitHub Pages. **Conséquence actée avec William** : pseudonymiser uniquement le fichier statique sans toucher `?employes=1` n'aurait rien réglé — les deux sont traités dans le même chantier.
- **Seconde découverte, plus large, volontairement laissée hors périmètre** : en préparant l'implémentation, un résolveur de noms interne au Worker (`getNomResolver()`) s'est révélé utilisé par une dizaine d'autres endpoints publics — `?dashboard=1` en tête (jusqu'à 5000 mouvements, chacun avec `nom_employe`, probablement le plus gros volume de données personnelles exposées de tout le Worker, loin devant `?employes=1`), plus réservations/campagnes d'inventaire/dotations EPI. Question posée explicitement à William avant de coder quoi que ce soit : élargir tout de suite, ou rester borné à `employes.json`/`?employes=1` et documenter le reste comme un chantier séparé ? **Décision : rester borné.** Fermer toute la propagation via `getNomResolver()` est une refonte bien plus large (re-router l'endpoint le plus consulté de tout le dashboard) que ce que la direction a signalé (le point documenté depuis l'audit portait spécifiquement sur les catalogues statiques). Documenté dans `SECURITE_ETAT.md` § Reste à faire pour une session dédiée future.
- **Troisième découverte, également hors périmètre** : en balayant les autres fichiers JSON publics à la racine (demande explicite de William : « vérifie tout autre fichier statique »), deux fichiers de migration one-shot déjà exécutée mais jamais nettoyés du dépôt contiennent encore des données personnelles réelles — `epi_personnel.json` (nom+prénom+tailles de 73 salariés) et `materiel_it_mouvements.json` (nom complet associé à un numéro de téléphone réel). `immos.json` et `chantiers.json`, en revanche, ne contiennent aucune donnée personnelle (vérifié). Nature différente (suppression de fichiers obsolètes, pas de logique applicative) — signalé pour une session dédiée séparée plutôt que mélangé à ce chantier-ci.
- **Architecture validée avec William avant codage** : `employes.json` réduit à terme aux codes + poste (non nominatif) ; nouvel endpoint Worker dédié et minimal (`?noms_employes=1`, uniquement `{code, nom}`) pour la résolution ; protection *best effort* (le modèle terrain — scan de badge sans mot de passe — interdit toute exigence de jeton de session sur cet endpoint) combinant vérification d'origine (`Origin`/`Referer` contre `ALLOWED_ORIGINS`) et limitation de débit par IP (20/min, seuil large pour ne jamais bloquer un dépôt avec plusieurs téléphones derrière une IP partagée) ; cache côté client en `localStorage` (pas le service worker, qui n'intercepte jamais les appels cross-origine au Worker) pour un affichage hors-ligne dégradé vers le code seul si aucune résolution n'a jamais eu lieu sur l'appareil.
- **Implémentation en 2 étapes réversibles, comme demandé** : étape 1 — nouvel endpoint + cache ajoutés, `chargerEmployes()`/`loadEmployes()` rebranchés pour préférer le nom résolu par ce nouveau mécanisme, **avec repli transitoire sur l'ancien champ nom de `?employes=1`/`employes.json` si la résolution échoue** (zéro régression, rien retiré des sources publiques à ce stade) ; étape 2 (retrait du nom de `?employes=1`, bascule d'`employes.json` en version pseudonymisée) — **non commencée, attend un feu vert explicite après vérification de l'étape 1 en conditions réelles post-déploiement**.
- **Bonne surprise architecturale en cours d'implémentation** : côté dashboard, ~95 points d'affichage lisent le nom d'un employé, mais la quasi-totalité passe par les mêmes structures en mémoire (`employesList`/`employesByCode`, alimentées par la seule fonction `loadEmployes()`) plutôt que par un accès direct au réseau — rebrancher ce point de construction unique a suffi à propager la résolution partout, sans toucher individuellement aux dizaines de call sites (`nomEmploye()`, accès directs `e.Nom`). Vérifié en navigateur (pas seulement supposé) : réponse mockée du nouvel endpoint prioritaire sur l'ancien champ, repli correct quand le nouvel endpoint échoue mais que l'ancien fonctionne encore, repli correct jusqu'au fichier statique quand le Worker entier est indisponible, cache réutilisé quand le nouvel appel échoue (simulation hors-ligne), non-régression du badge de scan (nom affiché avant tout appel réseau, confirmé par une sonde sur `window.fetch`).
- **Tests** : nouveau fichier `tests/worker.noms-employes-endpoint.test.js` (13 tests) — payload strictement limité à `{code, nom}` (pas de fuite du hash de mot de passe ni d'autres champs), rejet 403 sans origine reconnue ou avec une origine tierce, 429 après dépassement du seuil par IP avec IP différente non affectée, déduplication par code. Suite complète passée à 99 tests, `npm run verify` vert.
- **Pas encore déployé** : `worker.js`/`app.js`/`dashboard.html` modifiés localement et vérifiés (tests automatisés + simulation navigateur avec réponses mockées, car le Worker de production n'a pas encore la nouvelle route et la vérification cross-origine depuis un serveur de test local est de toute façon bloquée par le CORS resserré de l'audit précédent — comportement attendu, pas un bug). Rien poussé sur `main` à ce stade — un push déclenche un déploiement automatique en production (voir `01_ARCHITECTURE_TECHNIQUE.md`), à faire seulement après accord de William pour la vérification en conditions réelles.

### Suite : déploiement de l'étape 1, vérification réelle, puis étape 2 (même session)

- **William a rappelé un point important avant de donner son accord pour pousser** : l'environnement de recette existe précisément pour ce genre de vérification — la demande initiale de tester "en conditions réelles" avait été comprise à tort comme "contre la production" dans le message précédent. Corrigé : la vérification post-déploiement de l'étape 1 s'est faite contre `https://ral974.github.io/immo-tracker/staging/` (Worker `immo-proxy-staging` réel, site SharePoint de recette séparé, données 100% fictives `FIC1`…`FIC9`) — zéro risque, exactement l'usage prévu par `PROCEDURE_RECETTE.md`.
- **Étape 1 poussée et vérifiée en conditions réelles** : commit dédié (`git log` — « Ajoute une résolution de noms employés via le Worker »), `npm run verify` passé par le hook `pre-push` (99 tests), déploiement confirmé via l'API GitHub Actions publique (`Deploy to GitHub Pages` → succès sur le commit poussé) et directement contre le Worker de recette déployé : `curl .../immo-proxy-staging.../?noms_employes=1` avec un `Origin` reconnu renvoie bien les 10 employés fictifs de la recette (dont `AIWI`, le compte bootstrap, avec un nom vide — cohérent, seul `Title`/`field_2`/`Code_CT`/`Site` ont été saisis à la création de ce compte selon `PROCEDURE_RECETTE.md`) ; sans `Origin` reconnu ou avec une origine tierce, `403 origine_non_reconnue` confirmé en conditions réelles (pas seulement en test mocké). Vérification navigateur complémentaire sur les vraies pages de recette (`dashboard.html` et `index.html` de `staging/`) : `loadEmployes()`/`chargerNomsResolus()` résolvent bien les noms fictifs réels depuis le Worker déployé, cache `localStorage` peuplé, bandeau « 🧪 RECETTE » présent (confirme qu'on est bien sur le bon environnement).
- **Étape 2 réalisée sur le feu vert donné par William** (donné en même temps que l'autorisation de pousser, mais appliquée seulement après la vérification réelle de l'étape 1 ci-dessus, conformément au déroulé annoncé) :
  - Retrait du champ `nom` de la réponse `?employes=1` (worker.js) — les autres champs (poste, rôle, site, actif, mot de passe défini, tailles EPI, service outillage) restent inchangés, seule la résolution de noms passe désormais exclusivement par `?noms_employes=1`.
  - `employes.json` régénéré par script (lecture → suppression du champ `Nom` → réécriture), pas retapé à la main — 98 entrées, structure `[...]` inchangée (toujours un tableau, cohérent avec le reste du projet), seuls `Code` et `Poste` restent.
  - **Aucune modification côté client n'a été nécessaire pour cette étape** — confirmation a posteriori que la conception de l'étape 1 (résolution prioritaire, repli transitoire uniquement si elle échoue) était la bonne : puisque `chargerEmployes()`/`loadEmployes()` préfèrent déjà le nom résolu par le nouveau mécanisme, la disparition du nom dans les deux anciennes sources ne fait que rendre leur repli transitoire silencieusement inopérant (il retombe sur le code, jamais une erreur) — aucun risque de régression même si l'étape 2 avait été poussée avant que l'étape 1 soit éprouvée.
  - Nouveau garde-fou anti-régression (`tests/security.employes-sans-nom.test.js`, 3 tests) : la clé `nom` doit avoir disparu de la réponse `?employes=1`, le nom réel ne doit apparaître nulle part dans le payload JSON brut, et les autres champs doivent rester présents. Suite passée à 102 tests, `npm run verify` vert.
- **Historique git non purgé, volontairement** : la bascule ne retire pas les noms des commits déjà poussés (`employes.json` contient des noms en clair depuis le tout début du projet). Signalé dans `SECURITE_ETAT.md` § Reste à faire, même situation que `materiel_it_catalogue.json` — **pas de réécriture d'historique faite ni proposée dans cette session**, en l'absence d'un accord explicite dédié à cette action spécifique (le feu vert donné portait sur la bascule des fichiers/du code, pas sur la réécriture d'historique).

## Nettoyage des fichiers de migration orphelins avec PII (fin août 2026)

- **Demande de William**, en suite directe du chantier de pseudonymisation d'employes.json : traiter les deux fichiers de migration signalés comme hors périmètre (`epi_personnel.json`, `materiel_it_mouvements.json`).
- **Vérification avant toute modification** : pour chacun des deux fichiers, tracé précisément le code qui les lit (`importerDonneesEPI()`/`importerDonneesMaterielIT()`, dashboard.html) pour s'assurer qu'aucune régression n'était possible avant de toucher aux données.
  - `epi_personnel.json` : les champs `nom`/`prenom` n'étaient **jamais lus** par `importerDonneesEPI()` — seule la clé `code` sert à retrouver l'employé (`employesByCode[p.code]`), seuls `affectation`/les 5 tailles sont effectivement envoyés au Worker. Pur mort-bois du point de vue applicatif.
  - `materiel_it_mouvements.json` : `nom_detenteur` est bien envoyé au Worker (`bulk_import_mouvements_materiel_it` l'écrit dans `Mouvements_Materiel_IT.Nom_Detenteur`), mais la fonction d'affichage réelle (`nomDetenteurMaterielIT()`) **préfère déjà** la résolution live depuis `employesByCode` (elle-même alimentée par `?noms_employes=1` depuis le chantier précédent) — ce champ statique n'est qu'un repli pour un code introuvable dans `Employes`, jamais la source affichée en pratique.
- **Décision** : retirer les champs nominatifs des deux fichiers (`nom`+`prenom` de l'un, `nom_detenteur` de l'autre), sans toucher au code — aucune modification de `worker.js`/`dashboard.html` n'était nécessaire, les deux fonctions de migration restent fonctionnelles à l'identique (`rows`/`mouvements` construits exactement pareil, les champs retirés n'y figuraient pas ou n'y étaient jamais lus).
- **Vérifié** : tous les codes préservés (73 et 40 entrées respectivement, aucune perte), `npm run verify` inchangé (102/102, ces fichiers n'étant couverts par aucun test existant), et vérification réelle en navigateur (serveur statique local) — rejoue exactement la logique de construction des lignes de `importerDonneesEPI()` contre les fichiers réellement nettoyés servis par le serveur, confirme que `rows`/`mouvements` ont la forme attendue et qu'aucun champ nominatif ne subsiste.
- **Même limite que pour employes.json, assumée pareillement** : l'historique git conserve ces noms dans les commits antérieurs à cette bascule — non purgé, aucune réécriture d'historique demandée ni faite dans cette session.

### Régression corrigée : plantage de l'onglet Utilisateurs (10 août 2026)

Signalé par une alerte Sentry (`TypeError: Cannot read properties of undefined (reading 'localeCompare')`, `filterUsers`/`renderUsers`) — sans lien avec le travail du jour sur le mot de passe/KV. **Cause** : `?employes=1` ne renvoie plus de champ `nom` depuis la pseudonymisation ci-dessus ; `loadEmployes()` peuple `employesList` avec `Nom:e.nom` (donc `undefined`) avant d'attendre la résolution asynchrone via `?noms_employes=1`. Si l'onglet Utilisateurs se rend pendant cette fenêtre, le tri (`localeCompare`) plante sur un `Nom` non défini. **Corrigé** : `Nom:e.nom||e.code||''` à la construction initiale (dashboard.html) — garantit une chaîne dès le départ, jamais `undefined`, même avant que la résolution des noms n'ait abouti ; même style de repli déjà utilisé trois autres fois dans le fichier pour ce même champ. `npm run verify` inchangé (117/117, aucun test ne couvre cette fonction spécifique du dashboard).

## Politique de mot de passe (8 caractères) et verrou anti brute-force persistant via KV (10 août 2026)

- **Contexte** : deux limites explicitement assumées lors de l'audit de sécurité du 9-10 août 2026 (voir "Reste à faire" de `SECURITE_ETAT.md`, points 2 et 3 à l'époque) — le verrou anti brute-force reposait uniquement sur un compteur en mémoire du Worker (perdu au recyclage d'isolate, non partagé entre nœuds edge), et la politique de mot de passe minimale n'avait pas été retouchée (4 caractères, héritée d'avant tout durcissement). William a demandé de solder les deux dans cette session.
- **Politique de mot de passe — décision de ne jamais casser un compte existant** : le minimum passe à 8 caractères, mais uniquement pour les **nouveaux** mots de passe (`set_password`, création ou changement). `verify_password` ne vérifie et n'a jamais vérifié que le hash, jamais la longueur — un compte créé avant ce durcissement avec 4-7 caractères continue de se connecter sans rien faire de spécial, sans renouvellement forcé, sans migration de données. Décision cohérente avec la philosophie déjà appliquée ailleurs dans le projet (ex. `Duree_Amortissement` conservée mais inerte plutôt que migration forcée) : durcir pour l'avenir sans casser le passé.
- **Indicateur "mot de passe faible" proposé, pas construit** : évoqué comme option par l'assistant (comptes antérieurs au durcissement, à identifier et signaler pour renouvellement), mais volontairement non implémenté cette session — nécessiterait de trancher des questions produit non posées à William (où l'afficher : fiche employé ? bandeau de connexion ? juste informatif ou renouvellement forcé après N jours ?). Proposé explicitement dans la restitution, à cadrer séparément s'il est retenu.
- **Verrou anti brute-force — persistance KV scopée à `verify_password` uniquement, par choix explicite de latence** : `LOGIN_ATTEMPTS` (Map mémoire) reste le mécanisme de base et le repli automatique de tout. Un binding Cloudflare KV optionnel (`LOGIN_ATTEMPTS_KV`) rend ce compteur persistant/partagé entre nœuds edge, mais uniquement branché sur `verify_password` — pas `set_password` (qui reste memory-only), pas `reset_password` pour la *lecture* (l'admin agit rarement, pas besoin de cohérence KV à ce niveau), même si `reset_password` **purge aussi** la clé KV en écriture pour ne pas laisser un employé bloqué après une intervention admin si son verrou avait été posé via KV par un autre isolate. Raisonnement : ajouter une latence réseau KV sur un chemin appelé à chaque connexion réelle (`verify_password`) est le compromis demandé explicitement ; l'étendre ailleurs n'aurait apporté aucun bénéfice pratique pour un coût de latence à chaque fois.
- **Limites du plan gratuit Cloudflare KV vérifiées avant de s'engager** (documentation officielle, 10 août 2026) : 100 000 lectures/jour, 1 000 écritures/jour, 1 écriture/seconde par clé, 1 Go/namespace. Confirmé largement suffisant pour ~97 comptes avec quelques connexions par jour chacun — et le verrou lui-même borne naturellement le pire cas (une fois bloqué, plus aucune écriture KV pendant 30s/5min/15min), donc pas de scénario réaliste de dépassement même sous tentative de brute-force soutenue.
- **Pourquoi le binding KV n'a pas été activé directement dans `wrangler.toml` dès l'écriture du code** : contrairement à un secret Cloudflare absent (échec explicite mais déploiement toujours réussi, pattern déjà en place pour `SESSION_SECRET_ENV`), un `kv_namespaces` déclaré avec un ID de namespace inexistant fait échouer **le déploiement entier du Worker** au prochain push — `wrangler deploy` valide l'existence du namespace à chaque déploiement. Comme aucun outil de cette session ne peut créer de ressource Cloudflare à la place de William, committer un ID inventé aurait risqué de bloquer tous les futurs déploiements (même sans rapport avec ce chantier) jusqu'à correction manuelle. Décision initiale : bloc `kv_namespaces` ajouté à `wrangler.toml` mais **commenté**, avec la procédure exacte pour l'activer.
- **Étape manuelle réalisée dans la foulée, même session** : William a créé le namespace `immo_tracker_login_attempts` (Cloudflare dashboard → Storage & databases → Workers KV → Create Instance — l'emplacement réel dans l'interface actuelle, différent de "Workers & Pages → KV" indiqué dans une première restitution, corrigé en cours d'échange). Son ID a été collé dans `wrangler.toml` (bloc décommenté), format vérifié (32 caractères hexadécimaux, cohérent avec un ID de namespace Cloudflare KV). Le binding est donc **actif dès le prochain déploiement Git**, plus une simple bascule en attente.
- **Tests** : `tests/worker.password-policy.test.js` (nouveau fichier) — refus à 7 caractères sans écriture SharePoint, acceptation à 8, et surtout un compte existant à 4 caractères (hash PBKDF2 réel recalculé dans le test, pas un mock arbitraire) qui reste connectable via `verify_password`. `tests/worker.rate-limit.test.js` complété — paliers via un mock KV, **persistance simulée entre deux "isolates"** (même mock KV réutilisé à travers deux rechargements du Worker, chacun réinitialisant `LOGIN_ATTEMPTS` comme le ferait un vrai recyclage d'isolate Cloudflare, pour prouver que l'état survit bien via KV et non par accident via la mémoire), repli gracieux si KV échoue (mock qui rejette systématiquement), et vérification explicite que `set_password` n'appelle jamais KV même sous brute-force soutenu (compteur d'appels resté à 0). `npm run verify` : 117/117 avant push, dont le garde-fou `is_reset` toujours vert (non retouché).

## Photos de mouvement — roadmap item B (10 août 2026)

- **Point de départ, roadmap item B** : joindre une photo à un retour/transfert de matériel, preuve visuelle contre les litiges "c'était déjà abîmé avant" sur un retour en mauvais état validé par un garant.
- **Découverte majeure avant tout développement** : un mécanisme photo complet existait déjà dans le code, en production, mais **jamais documenté** dans cette base de connaissance. Sur les écrans PWA "État du matériel" (retour/transfert) et "Réception de matériel" (accepter un transfert), un bouton photo optionnel compressait déjà l'image côté client (canvas, ≤1200px, JPEG q0.75) et l'envoyait via une action Worker `upload_photo`/`delete_photo` déjà **volontairement non gatée** (même modèle de confiance que `reserver`/`transfert`, déjà dans `PWA_SHARED_ACTIONS` du test anti-régression), stockée dans `Photos_Immos/{code_im}/` sur le drive et déjà affichée côté dashboard (bouton "🖼️ Photos" sur la fiche immo, badge dans l'onglet Documents). Un 3ᵉ écran PWA (`screen-photos`, admin uniquement) permettait même déjà d'ajouter des photos hors mouvement. Ce socle a été **réutilisé intégralement**, conformément à la consigne explicite de ne pas réinventer un stockage.
- **Écart réel identifié entre l'existant et le besoin** : (1) aucun lien structuré entre une photo et le mouvement précis qui l'a produite — juste un tas de photos par immo, regroupées par proximité temporelle ; sur une immo à l'historique chargé, ça ne suffit pas comme preuve en cas de litige ; (2) une seule photo par mouvement, jamais plusieurs ; (3) absent des écrans panne/résolution ; (4) `upload_photo` n'avait aucune vérification serveur de taille ou de type réel de fichier (le `Content-Type` déclaré par le client n'était jamais vérifié).
- **Cadrage validé avec William avant tout code** (questions posées via `AskUserQuestion`, toutes les recommandations retenues) :
  1. **Lien photo ↔ mouvement : nouvelle colonne structurée `Photos` sur `Mouvements`**, plutôt qu'une inférence par horodatage — même raisonnement déjà tranché une fois pour `Cout_Reparation` (abandon d'un marqueur en texte libre jugé trop fragile, voir plus haut dans ce journal). William a créé la colonne (Texte) en production **et** dans l'environnement de recette (`Logistique-Immos-Recette`) avant que l'implémentation commence.
  2. **Écrans étendus** à Déclaration de panne et Résolution de panne/réparation, en plus de Retour/Transfert/Réception déjà en place.
  3. **Message non bloquant** (pas un blocage réel) si l'état saisi à un Retour est Abîmé/Hors service sans photo jointe.
  4. **3 photos maximum** par mouvement.
- **Mécanisme de liaison en deux temps pour Retour/Transfert** (le seul point réellement délicat de l'implémentation) : contrairement à `declarer_panne`/`resoudre_panne` qui créent le `Mouvement` en une seule requête (photos transmises directement dans le même appel), un retour/transfert passe par `Transferts_En_Attente` en attendant qu'un garant valide (`?action=transfert` puis `?action=valider`, potentiellement des heures ou des jours plus tard, par une personne différente du déclarant). Comme cette liste transitoire n'a pas sa propre colonne `Photos` (pas nécessaire pour une donnée qui ne vit que quelques heures/jours), les noms de fichiers du déclarant voyagent via un marqueur `##PHOTOS:fichier1;fichier2##` ajouté à son `Note` — même famille que les marqueurs `##COUT:X##`/`##PRESTA:X##` déjà utilisés ailleurs, mais ici volontairement transitoire (retiré et jamais réaffiché une fois la colonne structurée `Mouvements.Photos` renseignée à la validation). Les photos prises par le validateur/receveur au moment même de la validation sont, elles, transmises directement dans l'appel `valider` — combinées avec celles du déclarant dans la colonne finale.
- **Durcissement de `upload_photo`** (action non authentifiée, risque d'abus signalé explicitement à William avant implémentation, conformément à sa demande) : taille max stricte (1,5 Mo décodés — marge au-dessus de la cible de compression client 500 Ko), vérification de la signature réelle des octets (`FF D8 FF`, en-tête JPEG — jamais le `Content-Type` déclaré, qui n'a jamais rien garanti), validation du nom de fichier (caractères autorisés, extension, longueur — empêche une tentative de traversée de chemin même si `encodeURIComponent` la neutralisait déjà de fait). **Décision explicite de ne pas construire de quota/rate-limit dédié** : infrastructure disproportionnée (aucun KV/Durable Object prévu pour ça) pour un risque résiduel équivalent à celui déjà accepté sur les autres actions partagées PWA (`reserver`, `transfert`) depuis le début du projet — signalé à William, pas corrigé au-delà de la validation taille/type.
- **Compression durcie côté client** : l'ancienne qualité fixe (0.75) est remplacée par des paliers (0.75 → 0.6 → 0.5 → 0.4) essayés jusqu'à passer sous 500 Ko, pour une photo smartphone brute de 3-5 Mo — cible demandée explicitement par William au vu du réseau 4G parfois limité sur chantier.
- **UI multi-photo** : les anciens champs `S.photoEtatBase64`/`S.photoReceptionBase64` (une seule photo, remplacée à chaque sélection) deviennent des tableaux (`S.photosEtat`, `S.photosReception`, `S.photosPanne`, `S.photosResolution`), avec vignettes + bouton de suppression individuel (`renderPhotoThumbs`/`removePhotoMulti`). Toujours "1 tap = 1 photo" (contrainte de friction minimale demandée), répétable jusqu'à 3.
- **Affichage dashboard** : badge "🖼️ N" sur la ligne d'un mouvement dans Historique et Transferts en attente dès qu'il a des photos liées, ouvrant une galerie filtrée à ce mouvement (`ouvrirPhotosMouvementModal`) — distincte de la galerie complète par immo déjà existante (`ouvrirPhotosModal`), qui reste inchangée.
- **Tests** : nouveau fichier `tests/worker.photos.test.js` — fonctions pures (validation nom de fichier, signature JPEG, construction/extraction/retrait du marqueur transitoire), garde-fous serveur de `upload_photo` (taille, type réel, nom de fichier, cas nominal sans jeton), liaison de bout en bout `declarer_panne`/`resoudre_panne` (photos → colonne `Photos`), et le mécanisme en deux temps `transfert`→`valider` (marqueur posé, puis extrait/retiré/combiné à la validation, y compris le cas "aucune photo" qui doit écrire une colonne vide sans planter). `npm run verify` : 133/133 avant push.
- **Découverte annexe, hors périmètre de cette session** : en traçant les chemins de création de mouvement pour choisir où brancher la liaison photo, la fonction `enregistrerMouvement()` (PWA, utilisée pour un retour direct déclaré par un garant) s'est révélée poster sans paramètre `?action=` — aucun bloc du routeur Worker ne semble gérer ce cas, ce qui suggère un 404 silencieux (avalé par un `.catch` vide côté client) depuis un temps indéterminé. Non corrigé cette session (pas de rapport avec les photos, risque de casser un chemin déjà potentiellement fragile sans validation préalable) — signalé à William comme tâche séparée à investiguer, corrigée dans la foulée par une session parallèle (voir entrée suivante).

## Correctif : retour direct par un garant cassé côté PWA depuis le 9 août (10 août 2026)

- **Bug signalé par William** : sur la fiche immo du dashboard, une redirection vers `worker.js` avait été faite au 9 août (`bf7eaa6`, voir "Retour terrain : mauvaise identité attribuée aux mouvements" plus haut dans ce journal) pour remplacer l'ancien point d'entrée générique "Mouvement direct" (non authentifié) par l'action gated `creer_mouvement_direct`. Cette action gated remplaçait correctement le bouton rapide Transférer/Retour du **dashboard**, mais un second appelant du même ancien point d'entrée générique — `enregistrerMouvement()` dans `app.js` (PWA terrain), utilisé uniquement par `confirmerAvecEtat()` pour le cas "retour déclaré directement par un garant" — n'avait pas été identifié ni mis à jour lors de ce commit. Il continuait de poster sans `?action=`, ce qui tombait depuis sur le `return new Response('Not found', {status:404})` de fin de routeur.
- **Échec totalement silencieux** : `enregistrerMouvement` ne fait qu'un `.then(() => rafraichirBadges()).catch(() => {})` — un 404 n'est pas une erreur réseau pour `fetch`, donc aucune erreur n'était affichée. L'écran de confirmation (`showRecap`) s'affichait normalement juste avant l'appel, laissant croire au garant que le retour était enregistré alors qu'aucune ligne `Mouvements` n'était créée dans SharePoint. Portée : uniquement les retours effectués **par un garant sur lui-même** depuis la PWA (Admin/Logistique/Logistique_Mayotte) — le retour déclaré par un rôle terrain (`action=transfert` puis `valider`) et les transferts classiques n'étaient pas concernés, ces deux chemins étant correctement routés depuis le début.
- **Décision de correctif, validée par William** : créer une **variante non gated dédiée** (`creer_retour_direct_garant`) plutôt que de rediriger vers `creer_mouvement_direct` — cette dernière exige `requireGarant(body)`, donc un jeton de session HMAC, que la PWA ne peut pas fournir (identification par badge scanné, sans mot de passe, comme `reserver`/`transfert`/`declarer_panne`). Nouvelle action ajoutée à `PWA_SHARED_ACTIONS` dans `tests/security.gated-actions.test.js`, sur le même modèle que `enregistrer_scan_inventaire_immo`.
- **Durcissement au passage par rapport à l'ancien point d'entrée générique** : celui-ci acceptait `type_mouvement`/`code_chantier` depuis le corps de la requête sans contrainte. La nouvelle action `creer_retour_direct_garant` est strictement à usage unique (retour vers DEPOT) — `Type_Mouvement: 'Retour'` et `Code_Chantier: 'DEPOT'` sont fixés côté serveur, jamais lus de `body`, réduisant la surface de ce qu'un appel forgé pourrait produire.
- **Tests** : deux nouveaux cas dans `tests/worker.integration.test.js` (fonctionne sans jeton et écrit les bons champs ; refuse `donnees_invalides` si `code_im`/`code_employe` manquant, rien écrit). `npm run verify` repassé avant livraison — 115/119, les 4 échecs restants préexistaient déjà sur le dépôt avant ce correctif (`export_liste`/`journal_audit`, sans rapport, non touchés ici).
- **Leçon retenue** : quand un point d'entrée générique partagé par plusieurs appelants (dashboard **et** PWA) est remplacé par une action plus stricte, vérifier tous les appelants réels (`grep` sur l'ancien pattern d'appel) avant de considérer le remplacement complet — le message du commit `bf7eaa6` ne mentionnait que le cas dashboard, ce qui a masqué l'appelant PWA resté orphelin.
- **Rebase avec la session "Photos de mouvement" (entrée précédente), développée en parallèle sur la même base** : celle-ci a ajouté `photos: noms` au payload construit par `confirmerAvecEtat()` pour ce même cas (retour direct par un garant), et une colonne structurée `Mouvements.Photos`. `creer_retour_direct_garant` a donc été complétée pour lire `body.photos` et écrire `Photos: joinPhotos(body.photos)`, avec les mêmes helpers (`joinPhotos`/`sanitizePhotoList`) que `declarer_panne`/`resoudre_panne`/`valider` — sans cet ajustement, les photos jointes à un retour direct par un garant auraient été silencieusement perdues.

## Digest hebdomadaire de notifications (roadmap item F, août 2026)

- Repris de la roadmap (`05_ROADMAP_EVOLUTIONS_FUTURES.md`, item F) : le flux Power Automate "Notification_Mouvement_Immo" envoie un e-mail par mouvement — utile en temps réel, mais un point resté sans suite (transfert jamais validé, garantie qui expire, panne oubliée) peut se noyer dans ce flot continu. Besoin : un récapitulatif hebdomadaire des points nécessitant une **action**, pas juste une synthèse d'activité.
- **Cadrage fait avant tout code** (deux architectures comparées, validation explicite de William avant développement) : (a) endpoint Worker `?digest=1` calculé à la demande, appelé par un nouveau flux Power Automate planifié qui envoie l'e-mail lui-même — cohérent avec l'existant, aucune permission Azure supplémentaire ; (b) Cron Trigger Cloudflare + `sendMail` Graph direct depuis le Worker — écarté : aurait exigé d'ajouter la permission applicative `Mail.Send` sur l'app Azure AD partagée "Immo Tracker", ce qui élargit ce qu'un `CLIENT_SECRET_ENV` compromis pourrait faire (envoyer un e-mail au nom de n'importe quelle boîte du tenant), pour un gain marginal face à l'option (a). **William a validé l'option (a).**
- **Contenu du digest, complété par l'assistant au-delà de la demande initiale de William** (transferts en attente > 7j, retours à valider, garanties < 30j — repris tels quels), **avec validation explicite avant développement** : pannes non résolues depuis plus de 7 jours (dernier mouvement d'une immo = `Panne`), stock bas EPI/Outillage (réutilise les seuils `Stock_Mini` déjà en place depuis la session précédente), et campagnes d'inventaire immos ouvertes à faible couverture (< 50%, ouvertes depuis plus de 14 jours — pour ne pas alerter une campagne qui vient de démarrer). Seuils choisis par l'assistant comme point de départ raisonnable, tous ajustables dans `DIGEST_SEUILS` (worker.js) si l'usage réel montre qu'ils sont mal calibrés — pas de réglage dashboard pour cette V1, cohérent avec une demande volontairement minimale.
- **Décision structurante : réutiliser la logique métier existante plutôt que la réécrire.** Les règles garanties/pannes/comptes administratifs de l'onglet Maintenance (dashboard.html : `GARANTIE_ANS`, `COMPTES_ADMIN_DASH`, `ETIQUETEUSES_DASH`) et le calcul de couverture de campagne (`calculerEcartsInventaireImmos`) n'existaient que côté client, en JavaScript exécuté dans le navigateur — inutilisable tel quel depuis le Worker (edge Cloudflare). Comme le projet n'a ni build ni bundler (contrainte "zéro build" documentée depuis le début), un vrai partage de code entre `dashboard.html` et `worker.js` n'est pas possible : les mêmes règles ont été **portées** côté Worker (`DIGEST_SEUILS`, `COMPTES_ADMIN_DIGEST`, `ETIQUETEUSES_DIGEST`, `estImmoActiviteDigest`), avec un test anti-dérive dédié (`tests/worker.digest.test.js`) qui extrait par regex les constantes littérales de `dashboard.html` et les compare à celles du Worker — même principe que `security.gated-actions.test.js` pour `GATED_ACTIONS`, appliqué ici à des constantes de calcul plutôt qu'à une liste d'actions.
- **Authentification du endpoint, différente du reste du Worker** : les actions sensibles existantes sont protégées par un jeton de session HMAC (`requireAdmin`/`requireGarant`), obtenu à la connexion d'un employé au dashboard (12h). Un flux Power Automate planifié n'a personne connecté derrière lui — impossible de lui fournir un tel jeton sans le faire "se connecter" artificiellement chaque semaine. Décision : un secret dédié `DIGEST_TOKEN_ENV` (nouvelle variable Cloudflare, même famille que `CLIENT_SECRET_ENV`/`SESSION_SECRET_ENV`), comparé directement (à temps constant, `timingSafeEqualStr`) plutôt que vérifié par HMAC de session. Les données agrégées par le digest sont de toute façon déjà toutes individuellement publiques via d'autres endpoints (`?dashboard=1`, `?catalogue_epi=1`, `?campagnes_inventaire_immos=1`...) — cette protection est une précaution supplémentaire (empêcher un calcul lourd déclenchable par n'importe qui), pas une fuite comblée.
- **Écart pré-existant signalé, non corrigé** : le filtre "transferts en attente" du digest exclut explicitement tout ce qui n'est pas `Statut = 'En attente'` (allow-list), alors que `?dashboard=1` utilise une deny-list plus permissive qui n'exclut jamais `'Annulé'` — un transfert annulé pourrait donc, en théorie, apparaître encore dans l'onglet Transferts du dashboard sans jamais apparaître dans le digest. Comportement pré-existant de `?dashboard=1`, non modifié dans cette session pour ne pas changer l'onglet déjà en production sans demande explicite — à corriger si William le confirme gênant à l'usage.
- **Digest vide = pas d'email, décision déléguée au flux Power Automate** (pas au Worker) : la réponse JSON porte un champ `vide` (booléen) ; le flux Power Automate contient une action Condition qui n'envoie l'e-mail que si `vide = false`. Choix cohérent avec l'architecture (a) : le Worker reste un pur fournisseur de données, c'est Power Automate qui décide de l'action (envoyer ou non), configurable sans toucher au code si William change d'avis plus tard (ex. vouloir quand même un accusé de réception hebdomadaire même vide).
- **Destinataires** (choisis avec William) : `logistique@espacesoleil97.onmicrosoft.com` (même boîte que le flux de notification existant) + `lm@espacesoleil97.onmicrosoft.com` (référent Mayotte) — les deux seuls destinataires capables d'agir concrètement sur les points remontés (valider un transfert, gérer le stock, résoudre une panne). Encadrement (view-only) volontairement exclu de la V1 — pourrait être ajouté en copie plus tard si le besoin de visibilité se confirme, sans changement de code (juste la liste de destinataires dans Power Automate).
- **Pas de liens profonds vers un onglet précis du dashboard** : constaté en cours de développement que `dashboard.html` ne fait aucun routage par URL (`#hash`, jamais implémenté) — chaque ligne du digest précise donc en texte l'onglet à ouvrir, avec un unique bouton "Ouvrir le dashboard" pointant vers la racine. Écart mineur par rapport à la demande initiale ("liens directs vers le dashboard sur les éléments actionnables"), signalé à William plutôt que d'ajouter un mécanisme de routage par URL non demandé (aurait été un chantier à part entière).
- **Le flux "Notification_Mouvement_Immo" existant n'a pas été touché** (contrainte explicite de William pour cette session) — le digest s'ajoute en complément, la question de réduire le volume d'e-mails unitaires est explicitement reportée à après un premier retour d'usage sur le digest.
- **Tests** : 27 nouveaux tests (`tests/worker.digest.test.js` — chaque règle de sélection avec des jeux de données factices déterministes basés sur une référence temporelle fixe, cas digest vide, rendu HTML avec échappement vérifié, section anti-dérive vs `dashboard.html` ; `tests/security.digest-endpoint.test.js` — garde-fou du token en intégration avec Microsoft Graph entièrement mocké, aucune écriture SharePoint possible via cet endpoint). `npm run verify` : 162/162 avant livraison.
- **Étape bloquante côté William avant mise en service réelle** : ajouter `DIGEST_TOKEN_ENV` dans Cloudflare (Settings → Variables and Secrets) et créer le flux Power Automate planifié (5 actions : Récurrence → HTTP GET → Analyser JSON → Condition sur `vide` → Envoyer un e-mail) — détail pas-à-pas dans `01_ARCHITECTURE_TECHNIQUE.md` § Digest hebdomadaire.

## Demande de matériel planifiée — roadmap item D, Lot 1 (août 2026)

- Repris de la roadmap (`05_ROADMAP_EVOLUTIONS_FUTURES.md`, item D) : la réservation était jusque-là 100% réactive et exigeait une immo précise. Besoin exprimé par William : qu'un CT puisse demander une **catégorie** de matériel (ex. "2 perceuses") pour une date future, sans savoir laquelle sera fournie, et que le gestionnaire dépôt puisse traiter ces demandes.
- **Cadrage initial proposé par l'assistant (2 nouvelles listes SharePoint `Demandes_Materiel`/`Lignes_Demande_Materiel`) révisé après que William a montré un extrait réel de la liste `Reservations`** — capture d'écran demandée explicitement par lui avant validation ("regarde ça avant qu'on valide"), plutôt que de valider un cadrage sur la seule base de la documentation. Bonne pratique retenue : vérifier l'état réel des données avant de figer un modèle, pas seulement relire les `.md`.
- **Ce que la capture a révélé** : `Reservations` (déjà existante) supportait déjà bien plus que documenté — `Code_IM` optionnel côté Worker (jamais exploité côté PWA, qui imposait de choisir une immo), `Date_Debut`/`Date_Fin` future déjà utilisées en pratique (mécanisme de "contre-proposition logistique" visible dans les données réelles), un Gantt planning 6 semaines déjà en place côté dashboard, une vérification de conflit de dates déjà en place PWA + dashboard, et un statut `Confirmee` déjà défini dans les couleurs/libellés de l'UI (`SCOL`/`SLAB`, `app.js`) — plus creusé, ce statut s'est avéré **déjà actif** via un flux admin PWA existant (`afficherReservationsAdmin`/`actionResaPWA`), pas juste un vestige jamais câblé comme d'abord supposé.
- **Décision : étendre `Reservations` plutôt que créer une liste séparée** — créer une liste parallèle aurait dupliqué le Gantt, la vérification de conflit et 3 écrans de traitement déjà existants (PWA CT, PWA admin terrain, dashboard gestionnaire). Cohérent avec le principe de réutilisation systématique déjà suivi pour EPI/Outillage/Matériel IT. 3 nouvelles colonnes seulement (`Categorie`, `Quantite`, `Libelle_Libre`, créées par William avant le développement) et un nouveau statut `Refusee` (valeur libre sur une colonne Texte déjà existante, aucun changement de schéma).
- **`Confirmee` réutilisé tel quel** (plutôt qu'un nouveau mot `Preparee`) — décision de William : coller au vocabulaire déjà présent dans l'UI plutôt que d'ajouter un synonyme.
- **`statut_resa` durci en passant** : ce PATCH acceptait n'importe quelle chaîne pour `Statut` sans validation — resserré à une liste blanche (`Demandee`/`Confirmee`/`Refusee`/`Rendue`/`Annulee`/`Contre-proposition`) découverte en relisant `app.js` (le statut `Contre-proposition`, utilisé par le flux admin de contre-proposition d'immo, aurait été cassé par une liste blanche mal informée — repéré avant d'écrire le code, pas après).
- **Gating inchangé, vérifié avant de coder** : `statut_resa`/`modifier_resa`/`reserver` sont déjà dans `PWA_SHARED_ACTIONS` (décision antérieure, déjà testée) — utilisées à la fois par le CT (annuler sa demande), le profil admin terrain PWA (confirmer/refuser sans mot de passe) et le dashboard. Aucun changement de gating nécessaire pour ce chantier.
- **Isolation vis-à-vis de `Mouvements` confirmée et non modifiée** : aucun statut de `Reservations` n'écrit de mouvement — seule la remise réelle via `transfert`/`valider` (inchangé) déplace une immo. Pas de blocage automatique des immos à la date demandée (une demande reste une intention, arbitrage humain au moment de la préparation).
- **Bug pré-existant trouvé et corrigé en testant, sans rapport avec ce chantier** : le bouton principal "+ Nouvelle réservation" de la PWA (`showScreen('screen-reserver')` sans `skipInit`) déclenchait une boucle infinie — `showScreen` relance `initReservationScreen()` sur cet écran, qui se terminait lui-même par `showScreen('screen-reserver')` (sans le 2e argument `skipInit`), d'où un aller-retour infini jusqu'au `RangeError: Maximum call stack size exceeded`. Repéré en testant le Lot 1 en navigateur (le bug empêchait d'atteindre l'écran modifié) — corrigé en un mot (`showScreen('screen-reserver', true)`), vérifié en rejouant exactement le geste du bouton (plus de crash, écran actif).
- **Lot 1 livré** : mode "demande par catégorie" côté PWA (bascule sur l'écran de réservation, catégorie + quantité + précision libre, aucune vérification de conflit puisqu'aucune immo n'est encore choisie), affichage cohérent dans "Mes réservations" (PWA) et dans la liste Reservations (dashboard, avec un site dérivé du demandeur — `siteEffectifResa` — puisqu'une demande par catégorie n'a pas d'immo pour dériver le site comme d'habitude), boutons Confirmer/Refuser/Annuler côté dashboard et côté PWA admin terrain.
- **Vérifié en navigateur avant livraison** (serveur statique local, pas seulement relu) : bascule immo précise ↔ catégorie sur l'écran PWA, contenu exact du corps de requête envoyé à `reserver` en mode catégorie, rendu de la carte "Mes réservations" pour une demande catégorielle et pour un statut `Refusee` (bon classement dans "Terminées"), rendu du tableau dashboard (colonnes, drapeau site absent en mode catégorie, options du sélecteur d'action sans "Proposer modification" pour une ligne sans immo), filtre par site et recherche texte incluant catégorie/précision libre, câblage complet du sélecteur d'action jusqu'à `majStatutResa`.
- **Tests** : 5 nouveaux tests d'intégration (`tests/worker.integration.test.js`) — création par catégorie écrit les bons champs, refus si ni immo ni catégorie (`destination_manquante`), refus d'un statut hors liste blanche, acceptation de `Refusee`, non-régression sur `Contre-proposition`. `npm run verify` : 167/167 avant livraison.
- **Lot 2 livré** (après retour d'usage positif de William sur le Lot 1 — "tout semble fonctionner", plus 3 bugs sans rapport trouvés et corrigés en cours de route, voir plus bas) : sous-onglet "📅 Planning" (Réservations → à côté de "📋 Liste") groupant les demandes actionnables par semaine puis par chantier, avec actions Confirmer/Refuser directement sur chaque ligne — et une 6e règle de digest (`digestDemandesReservationEnAttente`, seuil 5 jours) pour les demandes `Demandee` non traitées. 4 nouveaux tests (`tests/worker.digest.test.js`). `npm run verify` : 171/171 avant livraison. Item D de la roadmap considéré fait (Lot 1 + Lot 2).

## Trois bugs trouvés par William en testant le Lot 1 en conditions réelles (10 août 2026)

Après la livraison du Lot 1 (roadmap item D), William a testé en usage réel plutôt qu'en se fiant au rapport de vérification — bonne pratique qui a fait remonter 3 problèmes réels, aucun en lien direct avec la fonctionnalité livrée mais tous dans du code adjacent touché ou traversé pendant les tests.

- **Usurpation d'identité via l'activation manuelle** : `activationManuelle()` (écran PWA "Pas de carte ? Saisir mon code manuellement", pour le personnel intérimaire sans carte BTP) demandait un code **et** un nom en texte libre, sans jamais vérifier leur correspondance. William a tapé le code réel d'un collègue (`RIJB`) avec un nom inventé — accepté tel quel, connecté sous l'identité réelle de ce collègue avec un nom affiché faux. Risque réel : les codes terrain ne sont pas secrets (imprimés sur les cartes BTP), donc n'importe qui connaissant un code pouvait s'activer à sa place. **Corrigé en deux temps** : (1) le nom vient désormais toujours de la fiche employé réelle via l'endpoint déjà existant `?noms_employes=1` (créé lors de la pseudonymisation d'`employes.json`, sûr et limité en débit) au lieu d'une saisie libre ; (2) William a immédiatement retesté avec un code **entièrement inventé** (`RJIK`) et a pu quand même s'activer avec un nom inventé — le premier correctif ne couvrait que le cas "code réel, nom faux", pas "code faux". Corrigé : si le serveur répond et ne connaît pas le code, l'activation est bloquée net (pas de repli sur une saisie libre) ; la saisie de secours ne reste possible que si le serveur est réellement injoignable (hors-ligne, cas légitime à Mayotte, dégradation gracieuse déjà en place ailleurs dans l'app).
- **Boucle infinie sur "+ Nouvelle réservation"** (trouvé par l'assistant en testant le Lot 1, pas par William) : `showScreen('screen-reserver')` sans `skipInit` redéclenchait `initReservationScreen()`, qui se terminait elle-même par `showScreen('screen-reserver')` sans le 2e argument — aller-retour infini jusqu'au crash. Corrigé en un mot (`showScreen('screen-reserver', true)`).
- **Crash au démarrage sur `localStorage` corrompu** (remonté par Sentry après un test de William) : `TypeError: Cannot read properties of null (reading 'nom')`. La clé `employe` de `localStorage` contenait la chaîne littérale `"null"` (origine non identifiée avec certitude — probablement une pollution ancienne du navigateur de William, antérieure à cette session, `localStorage` survivant à tous les déploiements) ; `JSON.parse('null')` renvoie `null`, et le code faisait `S.employe.nom` sans jamais vérifier l'objet chargé. Corrigé défensivement : le chargement au démarrage valide que la valeur est un objet exploitable (avec un `code`) avant de s'y fier, sinon la nettoie et retombe sur l'écran d'activation au lieu de planter — protège contre cette classe de corruption quelle qu'en soit l'origine future, pas seulement contre ce cas précis.
- **Leçon retenue** : les trois bugs ont été trouvés parce que William a testé la fonctionnalité en conditions réelles (vrais gestes, vrais codes, y compris volontairement invalides) plutôt que de se fier au seul rapport "167/167 tests, vérifié en navigateur" — les tests automatisés et la vérification avec données factices ne remplacent pas un humain qui essaie de faire "n'importe quoi" avec l'écran de connexion.

## Interface simplifiée pour Mayotte — roadmap item E (août 2026)

- Repris de la roadmap : le référent matériel à Mayotte (`Logistique_Mayotte`) est peu à l'aise avec l'informatique (imprime des documents, envoie des photos par email) — objectif d'un accueil PWA réduit à 3 actions ("Je reçois", "Je rends", "Signaler une panne").
- **Cadrage fait avant tout code**, avec plusieurs allers-retours de clarification avec William plutôt que de deviner :
  - Le déclenchement automatique (nouvelle capacité `ROLE_CAPS.modeSimplifie`, posée uniquement sur `Logistique_Mayotte`) a été validé directement.
  - **"Retour dépôt" pour ce profil** : question posée à William sur la fréquence de ce cas (retourner son propre matériel). Sa réponse ("retour à CONI/admins ou RA, ça peut arriver, matériel HS, sortie du gars, couplé à la fiche de sortie déjà créée") a révélé que ce n'est pas un cas rare mais un usage réel et concret : matériel hors service remis en main propre, ou matériel d'un employé sortant. Ça a fait basculer la conception de "Je reçois" d'un simple lien vers `Transferts_En_Attente` à **deux chemins réunis sous un seul bouton** : valider un transfert déclaré à distance, ou déclarer soi-même une réception immédiate (réutilise le mécanisme "retour direct par un garant" déjà existant, puisque `Logistique_Mayotte` est garant — aucune nouveauté côté Worker). Ça a aussi confirmé pourquoi il fallait garder les 5 états et pas les réduire (le "Hors service" est justement l'état-clé de ce cas d'usage).
  - **5 états conservés** (Neuf/Bon état/Usé/Abîmé/Hors service) : proposition initiale de l'assistant de réduire à 3 pour "moins de texte" — refusée par William, qui utilise bien les 5 aujourd'hui. Bonne illustration que "simplifier" ne veut pas dire "réduire les choix réels", juste changer la façon de les présenter (grille de gros boutons colorés plutôt qu'un menu déroulant).
  - **Vocabulaire** : l'assistant avait proposé un lien "Mode complet" pour la porte de sortie vers les fonctions avancées — William a recadré directement : *"Que veux-tu qu'un créole réunionnais de 55 ans comprenne à ça ?"*. Changé pour "🔧 Autres actions", sans le mot "mode". Leçon retenue explicitement : le vocabulaire technique/produit ("mode", "fonctionnalité"...) n'a pas sa place dans l'interface destinée à ce profil, même quand il semble anodin depuis une perspective développeur — à revalider systématiquement avec William pour tout texte visible par un utilisateur peu à l'aise avec l'informatique.
- **Couche de présentation uniquement, vérifié avant chaque commit** : les 3 écrans appellent exactement les mêmes actions Worker que le mode normal (`valider`, `transfert`, `declarer_panne`, `creer_retour_direct_garant`) — zéro nouvel endpoint, zéro nouvelle liste SharePoint. Contrainte explicite de William respectée à la lettre.
- **Un seul `#screen-accueil`** (pas un second écran dupliqué) : deux blocs internes (`#accueil-normal`/`#accueil-simple`) dont la visibilité est basculée par `afficherEmploye()` — les ~15 endroits du code qui font `showScreen('screen-accueil')` continuent de fonctionner sans modification, quel que soit le mode.
- **`screen-etat`/`screen-accepter` partagés entre tous les rôles** : plutôt que dupliquer ces écrans, une grille de boutons d'état supplémentaire (cachée par défaut) a été ajoutée à côté du menu déroulant existant, basculée par `syncEcransModeSimplifie()` à chaque changement d'écran. Pour tout profil sans `modeSimplifie`, le comportement est strictement identique à avant — vérifié explicitement en navigateur (bascule Logistique_Mayotte ↔ CT, menu déroulant intact pour CT).
- **"Autres actions" non persisté** : la bascule (`S.modeSimplifieOverride`) vit en mémoire seulement, jamais dans `localStorage` ni SharePoint — retour automatique au mode simplifié au prochain chargement de page ou après "Changer d'utilisateur" (reset explicite ajouté à cette fonction). Évite qu'un accès occasionnel aux fonctions avancées reste "collé" pour l'utilisateur cible.
- **Réutilisation systématique de classes CSS déjà présentes mais orphelines** : `.etat-neuf/.etat-bon/.etat-use/.etat-abime/.etat-horsservice` existaient déjà dans `style.css` (mêmes couleurs que les badges `.bn/.bb/.bu/.ba/.bh` du dashboard) mais n'étaient utilisées nulle part dans `app.js` avant ce chantier — repérées et réutilisées pour les nouveaux boutons d'état, aucune nouvelle palette inventée.
- **Vérifié en navigateur avant livraison** (scénarios simulés avec données factices, pas seulement relu) : accueil simplifié affiché automatiquement pour `Logistique_Mayotte`, accueil normal strictement inchangé pour un autre rôle (CT), bascule "Autres actions", liste des transferts en attente (remplie et vide) sur "Je reçois", soumission en un tap des boutons d'état sur `screen-etat` (retour direct, auto-validé) et `screen-accepter` (validation d'un transfert), jusqu'à l'écran de confirmation avec le bon récapitulatif dans les deux cas.
- **Aucun changement côté Worker/tests automatisés** — feature 100% présentation côté PWA, `npm run verify` repassé (171/171, compte inchangé) pour confirmer l'absence de régression.
- **Test d'acceptation réel** : scénario pas-à-pas fourni à William pour le faire dérouler directement par le référent Mayotte — le seul vrai juge de "est-ce que c'est assez simple", pas les tests automatisés ni la vérification de l'assistant.

## Passe de documentation — réalignement doc/code (11 août 2026)

- **Objectif** : les écarts entre la base de connaissance (`00_*`-`05_*`, `ARCHITECTURE_GLOBALE.md`, `SECURITE_ETAT.md`) et le code réel, identifiés lors de l'audit du 9 août mais jamais soldés (`ARCHITECTURE_GLOBALE.md` § 7, `SECURITE_ETAT.md` "Reste à faire" #4), s'étaient encore creusés avec le travail d'août (demandes de matériel planifiées, mode simplifié Mayotte, digest, journal d'audit...) sans que la doc suive. Repris en 2 temps : un rapport d'écarts d'abord (validé par William avant tout changement), puis les corrections.
- **Méthode du rapport d'écarts** : 5 recherches indépendantes en parallèle (SharePoint/colonnes, ROLE_CAPS/endpoints, module Absences, statut roadmap/contexte, staleness d'`ARCHITECTURE_GLOBALE.md`/`SECURITE_ETAT.md`), chacune avec citations fichier:ligne, resynthétisées et vérifiées (compte de recoupement direct par script — 82 actions POST, 15 `PWA_SHARED_ACTIONS` + 2 `LOGIN_ACTIONS` = 17, 22 listes dans `EXPORTABLE_LISTS`) avant d'écrire quoi que ce soit dans la doc — "le code fait foi" pris au sérieux plutôt que recopier les affirmations d'un rapport sans les revérifier soi-même.
- **Corrections appliquées** (documentation uniquement, aucun fichier de code touché) :
  - `00_CONTEXTE_PROJET.md` : dates de mise à jour corrigées (juillet → août 2026, la doc datait littéralement d'avant son propre contenu) ; ajout à "Fait" de 8 éléments réels mais jamais listés (items roadmap B/D/E/F, politique de mot de passe + verrou KV, journal d'audit, module Absences, pseudonymisation d'`employes.json`) ; retrait de la liste "à venir" des 4 items déjà livrés ; statut Matériel IT corrigé (n'est plus "en attente de listes SharePoint", en service depuis longtemps).
  - `01_ARCHITECTURE_TECHNIQUE.md` : liste "Outils de diagnostic" complétée (4 → 10 adresses directes + 2 endpoints à jeton documentés séparément) — était figée depuis le tout début du projet.
  - `02_MODELE_DONNEES.md` : liste `Absences` documentée (colonnes complètes) ; liste fantôme `Chantiers` retirée ("Autres listes") — confirmé qu'aucun appel Graph ne cible une liste de ce nom, `Code_Chantier`/`Nom_Chantier` sont des champs texte libre déjà documentés comme tels ailleurs, `chantiers.json` est un fichier orphelin (non supprimé — décision volontaire de ne pas toucher aux fichiers de données dans une passe doc).
  - `03_REGLES_METIER_ET_ROLES.md` : introduction et matrice des rôles refaites (5-6 capacités documentées → 11 réelles ; 9 rôles → 10, `Compteur_Inventaire` manquait) ; nouvelle section "Module Absences" (règles de déclaration, distinction `absences`/`voitAbsences`, registre à sens unique).
  - `05_ROADMAP_EVOLUTIONS_FUTURES.md` : item A1 corrigé ("en attente de listes SharePoint" → en service, incohérent avec `00_CONTEXTE_PROJET.md` et le reste du journal depuis plusieurs semaines).
  - `ARCHITECTURE_GLOBALE.md` : comptes recalculés (78→82 actions, 16→17 partagées PWA avec `creer_retour_direct_garant` ajoutée à la liste, 19→22 listes SharePoint avec les 3 manquantes) ; sous-onglet Planning et accueil simplifié Mayotte ajoutés à l'inventaire des modules ; `?digest=1`/`?journal_audit=1` ajoutés au tableau des endpoints GET ; § 7 "écarts constatés" mis à jour (points 1/2/5 marqués corrigés, même convention de barré déjà utilisée pour le point 4).
  - `SECURITE_ETAT.md` : point 4 du "Reste à faire" marqué fait.
- **Aucun bug de code signalé par cette passe** : la consigne demandait de signaler (sans corriger) tout cas où le *code* semblait buggé par rapport à l'intention documentée — aucun trouvé. Les écarts constatés étaient uniformément dans un seul sens : la doc en retard sur un code qui, lui, fonctionne comme prévu.
- **Non traité, volontairement** : les fichiers JSON orphelins (`chantiers.json`, `outillage_durees.json`, `outillage_grille_ajouts.json`) restent dans le dépôt — signalés dans `ARCHITECTURE_GLOBALE.md` § 7 mais leur suppression est un changement de fichier de données, hors du périmètre "documentation uniquement" de cette session.
- **Leçon pour la suite** : plusieurs de ces écarts (notamment `00_CONTEXTE_PROJET.md`) provenaient de fonctionnalités livrées sans que la mise à jour de la doc associée soit faite dans la foulée, malgré la consigne déjà présente en pied de `00_CONTEXTE_PROJET.md` ("après chaque évolution significative, penser à mettre à jour..."). Aucun changement de processus décidé cette session au-delà du rappel — à surveiller lors des prochaines sessions fonctionnelles.

## Module « Brasseurs d'air » — implémentation (11 août 2026)

- **Cadrage préalable, entièrement clos avant cette session** (voir `Fichiers divers/Brasseurs d'air/CADRAGE_MODULE_BRASSEURS.md`) : stock calculé, 2 propriétaires en liste fermée (`ELECTRICITE SERVICES REUNION`/`1ST SHINE`, la société a été renommée d'Espace Soleil mi-2026), prix protégés `requireGarant` y compris pour les achats locaux, accessoires en report manuel sauf les pales (liaison automatique 1-pour-1 éditable), numérotation `{PREFIXE}.{AA}.{MM}.{NNN}`. William a créé les 5 listes SharePoint sur les deux sites (production et recette) avant que le code soit écrit — condition explicite posée en fin de cadrage.
- **Vérification systématique avant codage** (`?debug_columns=<liste>` sur les deux sites, comme demandé) : 5 listes présentes, colonnes conformes au cadrage **sauf une** — `Brasseurs_Mouvements`, la colonne quantité tapée "Quantité" (avec l'accent) a été stockée en interne sous le nom échappé **`Quantit_x00e9_`** par SharePoint, pas `Quantite` comme documenté. Identique sur les deux sites (donc pas une erreur de frappe ponctuelle mais le comportement systématique de SharePoint face à un nom de colonne accentué). Même classe d'incident que `Fabricant`/`Fabriquant` (campagne d'inventaire, juillet 2026) et `Code_deverouillage` (Matériel IT, août 2026) — troisième occurrence du même piège, toujours détecté par la même méthode (vérification en lecture seule avant d'écrire le moindre code), jamais par une erreur en production. Corrigé dans le code (constante `BRASSEUR_QTE_FIELD` dans `worker.js`, utilisée systématiquement) plutôt que de redemander à William de renommer la colonne.
- **Décisions de conception prises pendant l'implémentation, non explicitement tranchées par le cadrage** :
  - **Numérotation calculée côté serveur au moment de l'écriture**, jamais un `GET` séparé suivi d'un `POST` côté client (contrairement à l'ancien `?next_code_im=1`) — élimine tout scénario de numéro déjà pris entre la lecture et l'écriture par un second utilisateur simultané. Pattern plus robuste que le précédent historique, retenu ici dès le départ plutôt que reproduit par défaut.
  - **Recalage d'inventaire = un type de mouvement (`Inventaire`), pas une action Worker séparée** : la quantité transmise pour ce type est interprétée comme le niveau de stock **cible**, le Worker calcule et écrit l'écart signé nécessaire (une référence déjà au bon niveau ne génère aucune ligne). Réduit le nombre d'actions à protéger/tester sans perdre en clarté fonctionnelle.
  - **`peutGererBrasseurs` — une seule capacité, sans « voir » séparé pour Encadrement** contrairement à EPI/Outillage : puisque les prix sont protégés `requireGarant` côté Worker (population Admin/Logistique/Logistique_Mayotte, qui n'inclut pas Encadrement), un accès « view-only » plus large côté dashboard aurait échoué de toute façon sur l'onglet Commandes — préféré à une fausse promesse d'accès partiel qui ne fonctionnerait pas en pratique.
  - **Transfert écrit en deux appels séquentiels, pas en `$batch`** : il faut connaître l'id SharePoint généré par la première écriture (la sortie) pour renseigner `Transfert_Lien` sur la seconde (l'entrée), et réciproquement — un lot `$batch` ne permet pas à une requête du lot de référencer l'id généré par une autre requête du même lot.
  - **Un mouvement peut regrouper plusieurs lignes de référence sous un même document**, écrites en `$batch`, validées « tout ou rien » avant toute écriture — reproduit le regroupement déjà observé dans le classeur Excel d'origine (ex. une sortie chantier avec brasseur + pales + accessoires en une seule pièce).
- **Tests automatisés** (`tests/worker.brasseurs.test.js`, 19 tests, Microsoft Graph entièrement mocké) : validations serveur (référence inconnue, dépôt invalide, propriétaire hors liste, signe/stock négatif), transfert générant deux mouvements liés cohérents (`Transfert_Lien` croisé vérifié dans les deux sens, même numéro de document), réception avec écart (statut `Recue_Partielle` vs `Recue` recalculé depuis l'ensemble des lignes), numérotation automatique (format et reprise depuis le max existant), annulation (quantité à 0, commentaire existant conservé), protection `requireGarant` des endpoints exposant des prix. Un test préexistant (`worker.audit-helpers.test.js`) attendait un nombre figé de 62 actions gated — mis à jour à 68 (62 + les 6 nouvelles actions Brasseurs), seule modification apportée à un fichier de test déjà existant. `npm run verify` : 190/190 tests passent.
- **Isolation stricte vis-à-vis des immobilisations vérifiée** : aucune des 5 nouvelles listes ni des 6 nouvelles actions ne référence `Immos`/`Mouvements` — même précaution que pour la campagne d'inventaire par scan (voir plus haut dans ce journal).
- **Périmètre volontairement non couvert cette session** (cohérent avec le cadrage §5) : pas d'écran PWA (session dédiée future), pas de migration des données historiques du classeur Excel (erreurs de signe/numérotation à arbitrer ligne par ligne, pas automatisable), pas de seuils d'alerte personnalisés par article au-delà du mécanisme déjà réutilisé tel quel (`Stock_Mini`, même défaut que EPI/Outillage).
- **Statut en fin de session : testé et validé uniquement en local/recette (`staging/` synchronisé via `npm run sync:staging`), pas encore poussé ni déployé en production** — décision volontaire de ne pas pousser sans confirmation explicite (déploiement Cloudflare automatique sur push, action visible/partagée). À valider par William en recette (bandeau 🧪 RECETTE) avant tout push sur `main`.

## Module « Brasseurs d'air » — gap découvert au premier test en recette (11 août 2026)

- **Constat de William** en ouvrant l'onglet Brasseurs d'air en recette juste après le déploiement : la vue Stock affichait "Aucune référence" — les listes `Brasseurs_Depots`/`Brasseurs_Catalogue` étaient vides (créées vides par conception, cadrage §5 excluant toute migration automatique du classeur Excel).
- **Gap réel découvert en creusant, pas seulement un problème de données de test** : aucune action Worker ne permettait de créer un dépôt ou une référence catalogue — ni depuis le dashboard, ni depuis `scripts/seed-staging-data.js` (qui n'appelle que des actions déjà existantes, par principe). Contrairement à EPI/Outillage (`ajouter_article_epi`), le module Brasseurs livré la veille n'avait aucun bouton "➕ Ajouter" pour ces deux listes — un oubli de parité fonctionnelle avec les modules de référence, pas une décision volontaire du cadrage (qui ne s'était simplement jamais posé la question, focalisé sur les mouvements/mouvements/commandes).
- **Corrigé** : 2 nouvelles actions `ajouter_depot_brasseur`/`ajouter_reference_brasseur` (`requireGarant`, doublon rejeté côté serveur), 2 boutons dans la vue Stock du dashboard, 4 nouveaux tests. `GATED_ACTIONS_AUDIT` passe de 68 à 70 actions. `npm run verify` : 194/194.
- **`scripts/seed-staging-data.js` étendu** avec des dépôts (`OMT`/`TC2`, fictifs mais aux codes plausibles) et un catalogue factice (quelques références type `DCF-FS52920B`/`PALES`/`CMD-M`), plus quelques mouvements initiaux via `creer_mouvement_brasseur` pour que la vue Stock ait du contenu à afficher dès le premier chargement en recette — cohérent avec l'esprit du script existant (données 100% fictives, jamais les vraies).

## Module « Brasseurs d'air » — deux bugs trouvés au premier essai réel en recette (11 août 2026)

- **Bug 1, symptôme "ça reste bloqué sur Enregistrement…"** : les 8 confirmations de succès du module appelaient `toast(...)` — une fonction qui n'a jamais existé dans ce fichier. Le mécanisme réel s'appelle `showToast()` (`window.alert` est surchargé pour l'appeler, voir plus haut dans ce journal, section "Toasts non bloquants") ; aucune fonction globale nommée `toast` n'existe. L'écriture SharePoint réussissait bien (confirmé en rejouant le même appel directement contre le Worker de recette), mais l'appel `toast(...)` juste après levait une `ReferenceError` non rattrapée par le `catch` (elle survient *dans* le bloc `try`, donc capturée, mais affichée comme "Erreur réseau : toast is not defined" — trompeur, l'action avait pourtant réussi). Corrigé : les 8 appels renommés en `showToast(...)`, plus un type `'error'` invalide corrigé en `'danger'` (seuls `success`/`danger`/`warning` existent réellement dans `design-system.css`).
- **Bug 2, découvert en creusant pourquoi "Nouvelle commande" et "Transfert" semblaient bloqués eux aussi** (alors qu'ils avaient en fait réussi, même cause que le bug 1) : en testant plus à fond avec des champs optionnels vides, `creer_commande_brasseur` envoyait `Date_Arrivee_Estimee: ''` (chaîne vide) dès que le champ "Arrivée estimée" n'était pas rempli — Graph refuse une chaîne vide sur une colonne Date ("One of the provided arguments is not acceptable"), la création de commande échouait purement et simplement dans ce cas précis. Corrigé en envoyant `null` plutôt que `''`, même convention que `Date_Sortie_Service` (Matériel IT) déjà en place ailleurs dans `worker.js` ; même correctif appliqué à `editer_commande_brasseur` (`Date_Arrivee_Estimee`/`Date_Commande`) par cohérence, bien que cette action ne soit pas encore reliée à un écran du dashboard.
- **Incohérence UX corrigée au passage, signalée par William** : les 6 formulaires Brasseurs fermaient le modal immédiatement au succès et ne montraient qu'un toast en coin d'écran — différent du reste du dashboard (EPI/Outillage : `enregistrerArticleEPI` et consorts affichent un message vert **à l'intérieur** du modal, laissent la fenêtre visible ~1 seconde, puis la referment automatiquement). Les 6 fonctions (`enregistrerDepotBrasseur`, `enregistrerReferenceBrasseur`, `soumettreMouvementBrasseur`, `soumettreTransfertBrasseur`, `soumettreCommandeBrasseur`, `soumettreReceptionBrasseur`) alignées sur ce pattern existant plutôt que d'introduire une variante propre au module.
- **Méthode de diagnostic** : plutôt que de deviner depuis le code seul, appel direct du Worker de recette avec le jeton de session de William (`curl`, payload identique à ce qu'il avait saisi) pour confirmer que le serveur fonctionnait bien — a immédiatement écarté l'hypothèse d'un blocage serveur/réseau et recentré la recherche sur le client. Puis reproduction du scénario exact dans un navigateur réel (session simulée via `sessionStorage`, sans avoir besoin du mot de passe de recette) pour capturer l'erreur JS précise et confirmer chaque correctif avant de redemander à William de retester.
- **5 nouveaux tests** (dont une régression explicite sur `Date_Arrivee_Estimee: '' ` → `null`), `npm run verify` : 199/199.

## Migration Brasseurs d'air — reprise de l'historique du classeur Excel (11 août 2026)

- **Objectif** : reprendre dans SharePoint (`Brasseurs_Mouvements`) les ~260 mouvements réels enregistrés depuis mai 2025 dans `Fichiers divers/Brasseurs d'air/1.1.3.1. Suivi des entrées et sorties.xlsx` (2 feuilles de mouvements, OMT et TC2, + leurs TCD), avant que le classeur ne devienne une archive lecture seule. En parallèle, lecture de la PI `FS202603051 1stShine validated.pdf` (commande 2026 en cours) en vue de sa saisie ultérieure — non faite cette session, la migration devait d'abord être validée en production.
- **Lecteur XLSX maison, zéro dépendance npm** (`scripts/xlsx-lite.js`) : le projet n'avait jusque-là jamais eu besoin de lire un `.xlsx` depuis Node (les migrations précédentes passaient par un JSON déjà généré en amont, hors dépôt — voir `xlrd`/Python pour EBP). Plutôt qu'ajouter une dépendance npm (rompant avec le principe "zéro build/zéro install" du projet), un lecteur ZIP+XML minimal a été écrit à la main (répertoire central ZIP, `inflateRawSync` natif pour le DEFLATE, regex ciblées sur les XML OOXML des feuilles/`sharedStrings`/`styles`) et vérifié en le faisant tourner sur le vrai classeur avant d'en faire quoi que ce soit.
- **`scripts/migrate-brasseurs.js --dry-run`** : calcule le stock par Dépôt×Référence×Propriétaire à partir des mouvements bruts et le compare aux TCD (tableaux croisés dynamiques) déjà présents dans le classeur — la meilleure preuve disponible que les règles de reprise appliquées sont correctes, sans dépendre d'un calcul manuel. **Résultat : 0 écart**, sur les deux dépôts, dès la première version aboutie du script.
- **Règles de reprise tranchées avec William avant toute écriture** (les 4 recommandations proposées ont toutes été retenues) :
  - `Propriétaire du Stock` vide (167 lignes, 100% de la feuille TC2) → `ELECTRICITE SERVICES REUNION` par défaut, malgré un épisode réel de stock 1ST SHINE à TC2 mentionné au cadrage (§1.7) mais impossible à isoler depuis le classeur seul.
  - `Type_Mouvement` **dérivé du signe** de la quantité plutôt que recopié depuis la colonne "Type" du classeur (11 lignes où les deux divergent) — cohérent avec le cadrage §1.4 (erreurs de saisie accidentelles de l'ancien responsable, pas un pattern métier).
  - Extraction de `Code_Employe` étendue à "Préparé par"/"Inventaire par"/"Récupération par" en plus de "Pris par" demandé initialement (même sémantique).
  - Les ~8 épisodes de transfert historique OMT→TC2 repris comme mouvements `Sortie`/`Entree` indépendants, **sans** `Transfert_Lien` formel — fidèle à ce que le classeur montrait réellement (aucun lien n'a jamais existé entre les deux lignes, dates différentes, jamais rapprochées par l'ancien responsable).
  - Anomalies mineures laissées telles quelles, fidèles à l'historique (pas de "correction" a posteriori) : 1 valeur `ALCLIMA` dans la colonne Propriétaire (probable erreur de saisie, cadrage §1.8), 1 doublon de numéro de document (`TC.26.07.006` réutilisé pour deux transactions distinctes), dérive de préfixe dépôt (`TC` au lieu de `TC2` sur plusieurs documents mi-2026), 2 codes employé cités en commentaire introuvables dans `Employes` (`VIMA` — déjà un gap connu depuis la migration Matériel IT — et `TUMI`, nouveau).
- **Blocage découvert en concevant l'écriture, pas anticipé au cadrage** : l'action `creer_mouvement_brasseur` (existante) génère **toujours** un nouveau `Document` — aucun moyen d'imposer le numéro d'origine (`OM.25.05.001`...), alors que la consigne de migration l'exigeait explicitement. Question posée à William : ajouter une action Worker dédiée, ou renuméroter et garder l'original en commentaire — **il a choisi l'action dédiée**, pour respecter la consigne à la lettre.
  - Nouvelle action **`migrer_mouvement_brasseur`** (`worker.js`, `requireAdmin` — plus strict que `requireGarant`, comme les autres migrations en masse du projet) : accepte un `Document` imposé et une quantité `0` (nécessaire pour l'unique mouvement historique annulé), ce que l'action normale refuse volontairement. **Idempotente** : chaque ligne porte une clé de déduplication (`buildDedupeKey`, ex. `OMT:OM.25.05.001:DCF-FS52920B:L2`) écrite sous forme de marqueur `[MIG:...]` en fin de `Commentaire` ; avant toute écriture, l'action relit tous les `Commentaire` existants et saute les clés déjà présentes — un lot en erreur peut être rejoué sans jamais dupliquer de stock. `GATED_ACTIONS_AUDIT` passe de 70 à 71 actions ; `GATED_ACTIONS` (dashboard.html) mis à jour en conséquence bien qu'aucun écran ne l'appelle (seul le script de migration l'utilise, avec un jeton obtenu manuellement) — nécessaire pour que le test de synchronisation `security.gated-actions.test.js` reste vert.
  - `npm run verify` : 199/199 (un test préexistant vérifiant le nombre figé d'actions gated a été ajusté de 70 à 71).
- **Exécution en recette** : catalogue de recette incomplet au premier essai (`scripts/seed-staging-data.js` n'avait créé que 5 références sur les 14 attendues) — 9 références manquantes ajoutées via l'action déjà existante `ajouter_reference_brasseur` (mêmes libellés que la feuille "Données - Listes" du classeur), puis migration rejouée : **263/263 mouvements écrits**, script idempotent confirmé (60 lignes déjà écrites lors du premier essai correctement sautées au second). Stock recalculé conforme aux TCD en isolant les mouvements migrés (marqueur `[MIG:...]`) des données de test déjà présentes en recette (10 mouvements de démo créés lors d'essais précédents du dashboard, sans rapport).
- **Exécution en production, après feu vert explicite de William** : contrairement à la recette, la production n'avait **aucun** dépôt/catalogue/mouvement Brasseurs — les listes SharePoint avaient été créées vides (cadrage §5, migration volontairement hors périmètre de l'implémentation initiale). Bootstrap complet (2 dépôts, 14 références du catalogue) puis migration : **263/263 mouvements écrits en un seul passage**, aucune donnée préexistante à isoler cette fois. Stock final vérifié conforme aux TCD du classeur : `TC2 DCF-FS52920B=109 / T30=203 / T90=6`, toutes les autres clés Dépôt×Référence×Propriétaire à zéro.
- **Le classeur Excel `1.1.3.1. Suivi des entrées et sorties.xlsx` devient une archive en lecture seule à compter de cette bascule** : toute nouvelle entrée/sortie doit désormais passer par le dashboard (ou l'écran PWA à venir), plus jamais par une saisie manuelle dans le classeur.
- **Non fait cette session, volontairement** : la saisie de la commande PI `FS202603051` (Étape 2 du plan initial) — lecture faite (1530×DCF-FS52920B, 300×T90, 1530×CMD-M, US$93 801.60, ambiguïté de délai signalée : 35j en en-tête vs 50-55j en remarque pour moteur DC, applicable ici), mais pas encore saisie dans `Brasseurs_Commandes`. À reprendre dans une session dédiée.

## Brasseurs d'air — arrivée réelle, délai calculé, et saisie de la PI FS202603051 (11 août 2026)

- **Besoin exprimé par William** : la date d'arrivée doit rester modifiable (déjà le cas via `editer_commande_brasseur`, simplement jamais relié à un écran du dashboard jusqu'ici) ; il faut en plus une **date d'arrivée réelle**, distincte de l'estimation, avec un **délai calculé** entre la commande et la réception.
- **`Date_Arrivee_Reelle`** ajoutée à `Brasseurs_Commandes` (même convention Date que `Date_Arrivee_Estimee` : `''` → `null`, jamais une chaîne vide envoyée à Graph). Volontairement **jamais déduite automatiquement** de la réception SharePoint (`reception_commande_brasseur`) : le statut peut passer à "Recue" un autre jour que l'arrivée physique réelle, saisie manuelle uniquement. Le délai (jours entre `Date_Commande` et l'arrivée réelle si connue, sinon l'estimée — étiqueté "(estimé)" dans ce cas) est **calculé à la volée côté dashboard**, jamais stocké — même principe que la VNC des immos ou la prime annuelle Outillage.
- **Nouveau bouton "✏️ Arrivée" par commande** (vue Commandes fournisseur) ouvrant un petit formulaire à deux champs (estimée/réelle), réutilisant l'action déjà existante `editer_commande_brasseur` — aucune nouvelle action Worker nécessaire pour ce point.
- **Gestion d'erreur améliorée au passage** : `editer_commande_brasseur` renvoyait seulement `{success:false}` en cas d'échec Graph (aucun message exploitable) — corrigé pour renvoyer `error:'sharepoint'` + le message Graph réel, cohérent avec les autres actions du module. Utile immédiatement : c'est ce qui a permis de diagnostiquer l'incident suivant.
- **Piège découvert en saisissant la vraie commande PI FS202603051** : `Brasseurs_Commandes.Notes` est une colonne SharePoint **texte sur une ligne** (limite ≈255 caractères), pas "plusieurs lignes" — un `Notes` détaillé de 612 caractères a fait échouer `creer_commande_brasseur` avec un message Graph générique ("Invalid request", sans nommer le champ fautif), isolé par élimination (test minimal avec un `Notes` court, qui a fonctionné). Texte raccourci à 237 caractères pour la commande réelle. **Nouvelle classe de piège pour ce projet** (aux côtés de `Fabricant`/`Quantit_x00e9_`/`Code_deverouillage`, déjà documentés) : une colonne texte SharePoint standard tronque silencieusement côté écriture — à vérifier systématiquement pour tout champ Notes/commentaire libre appelé à contenir un texte long. Si des notes plus longues sont nécessaires à l'avenir, la colonne devra être recréée par William en "plusieurs lignes de texte" (aucune conversion automatique possible après coup sur une colonne existante).
- **Incident de test, corrigé** : au cours de ce diagnostic, une commande de test (`TEST-DEBUG-001`) a été créée par erreur directement en **production** (aurait dû être testée en recette) — aucune action de service n'existe pour supprimer une commande, marquée `Statut=Annulee` avec un `Notes` explicite plutôt que laissée telle quelle. Sans conséquence (aucune ligne réceptionnée, aucun mouvement de stock généré).
- **Commande PI `FS202603051` saisie en production** (`creer_commande_brasseur`, origine International, fournisseur "1stShine Industrial Co., Ltd") : 1530×`DCF-FS52920B` (US$54,01), 300×`T90` (US$8,10), 1530×`CMD-M` (US$5,71) — références normalisées depuis la PI (qui écrit `DCF-FS52920` sans suffixe couleur, et ne nomme jamais `T90`/`CMD-M` littéralement : déduites de la description physique, "Extend rod L90cm" et "wall control", cohérent avec le catalogue). Montant total US$93 801,60 (acompte 30% = US$28 140,48), FOB Shenzhen. **Ambiguïté de délai tranchée par William** : la PI indique 35 jours ouvrés en en-tête mais 50-55 jours ouvrés en remarque pour un moteur DC — applicable ici (moteur DC confirmé sur la fiche produit) — arrivée estimée retenue au **15/10/2026** (≈61 jours ouvrés depuis la signature du 22/07/2026, marge au-delà du pire cas fournisseur), stockée dans `Date_Arrivee_Estimee` et modifiable à tout moment. Propriétaire du stock à l'arrivée (gouvernance) : `ELECTRICITE SERVICES REUNION` — non stocké sur la commande (le modèle de données ne porte le propriétaire qu'au niveau des mouvements), noté dans `Notes` pour ne pas se reperdre au moment de la réception réelle. **Aucun mouvement d'entrée créé** : naîtra de la réception effective via l'écran dédié, une fois la marchandise physiquement arrivée.
- `npm run verify` : 201/201 (2 nouveaux tests pour `date_arrivee_reelle`, dont la régression sur le message d'erreur Graph exploitable).

## Sortie de stock Brasseurs déclarée par le technicien (écran PWA, 11 août 2026)

- **Besoin exprimé par William** : les sorties du dépôt TC N°2 étaient faites physiquement par les techniciens (historiquement « Pris par GOLU/VIMA/... » dans le classeur Excel), puis ressaisies après coup par le gestionnaire — décalage entre le geste réel et l'enregistrement, source d'erreur et de perte de temps. Objectif : que le technicien déclare lui-même la sortie depuis la PWA, comme il le fait déjà pour les mouvements d'immos.
- **Cadrage court avant développement** (étape 1, validée par William avant tout code) : proposition d'écran (badge scanné, dépôt, référence avec stock affiché, quantité, destination, confirmation — ≈5-6 taps) et point de sécurité posé explicitement, car une sortie de stock erronée fausse des quantités utilisées ensuite (seuils d'alerte, suggestions de commande), contrairement à un mouvement d'immo réversible sans conséquence financière directe.
- **Décisions tranchées par William** (par échanges successifs, la dernière portant sur les rôles non cités explicitement au premier tour) :
  1. **Quantité plafonnée selon le rôle, pas une valeur fixe** : « Un simple ouvrier : pas plus de 10. Un CT, un gestionnaire de dépôt ou un admin : pas de limite (il arrive qu'on donne 120 BA d'un coup pour un chantier). » Puis, pour les rôles non cités : « CT spécialisé et ouvrier spécialisé ne touchent pas aux BA, RA et encadrement : illimité » — traduit en deux catégories serveur : plafonné à 10 (`Ouvrier`, `Ouvrier_Specialise`, `CT_Specialise`, rôle vide/inconnu par défaut prudent) vs illimité au niveau métier (`CT`, `RA`, `Logistique`, `Logistique_Mayotte`, `Admin`, `Encadrement`, super-admins permanents). Un plafond technique de 500 (anti-typo) s'applique malgré tout à tous, y compris les rôles illimités.
  2. **Tous les dépôts actifs**, pas seulement TC2 — le technicien choisit dans la liste, pas verrouillé à un seul dépôt (évite d'avoir à revenir sur le code si un autre dépôt en a besoin).
  3. **Pas d'exception au journal d'audit** (`GATED_ACTIONS_AUDIT`) — cohérent avec toutes les autres actions PWA partagées (`reserver`, `transfert`...) : le mouvement `Brasseurs_Mouvements` lui-même sert de trace. En contrepartie, un badge « 📱 PWA » a été ajouté sur ces lignes dans le journal des mouvements du dashboard (visible dès que `Cree_Par` est vide, puisqu'aucune session n'existe pour ce flux).
  4. **Propriétaire toujours `ELECTRICITE SERVICES REUNION` en silence**, pas de champ affiché — le cas rare de stock 1ST SHINE à TC2 (vécu une fois, voir cadrage §1.7) se corrige après coup par un gestionnaire au dashboard plutôt que d'ajouter de la friction à un geste terrain fréquent.
- **Nouvelle action Worker `sortie_stock_brasseur_pwa`**, volontairement plus étroite que `creer_mouvement_brasseur` (réservée au dashboard) : sans jeton de session (même modèle de confiance que `reserver`/`transfert`, ajoutée à `PWA_SHARED_ACTIONS`), toujours `Type_Mouvement='Sortie'`, `Destination` jamais `Négoce` (refusé côté serveur, insensible casse/accents — une vente reste une décision de gestionnaire), au plus 5 références par sortie, garde-fou stock négatif identique à l'action dashboard. Le rôle de l'auteur est relu côté serveur depuis `Employes.Code_CT` (aucun jeton pour le porter autrement), jamais transmis par le client.
- **Pas de liaison automatique pales↔brasseur sur cet écran** (contrairement au dashboard) : le technicien ajoute une ligne PALES lui-même s'il en prend — simplification volontaire pour un premier jet, à revoir si le besoin se confirme à l'usage.
- **Écran PWA** : sélection dépôt → liste de références du catalogue actif avec stock live affiché (endpoints publics déjà existants, aucun nouvel appel Worker en lecture) → bordereau accumulant jusqu'à 5 lignes → destination (Chantier texte libre / Maintenance) → validation en un seul appel groupé (un `Document` = une sortie). Mode hors-ligne explicitement bloqué avec message clair (« la sortie de stock nécessite le réseau ») plutôt qu'un échec silencieux — cohérent avec le stock **calculé** (jamais un solde stocké), qui exige un contrôle serveur à chaque écriture.
- **Aucune nouvelle liste ni colonne SharePoint** : réutilise intégralement `Brasseurs_Depots`/`Brasseurs_Catalogue`/`Brasseurs_Mouvements` déjà en place — pas d'étape bloquante côté William pour ce lot.
- **Vérifié en navigateur** (recette, `immo-proxy-staging`, aucune donnée réelle touchée) : chargement des dépôts/références avec stock, sélection et ajout au bordereau, activation/désactivation du bouton Valider selon les champs remplis, bascule de destination, message hors-ligne explicite, et le flux complet jusqu'à l'écriture (fetch simulé en local pour ne déclencher aucune écriture réelle pendant la vérification — le payload envoyé a été inspecté directement : `code_employe` résolu depuis l'identité du badge scanné, jamais saisi).
- `npm run verify` : 222/222 (22 nouveaux tests dédiés à `sortie_stock_brasseur_pwa` : plafonds par rôle pour les 9 profils cités par William, stock insuffisant, destination Négoce refusée sous 4 variantes de casse/accent, propriétaire jamais écrasable par le client, dépôt inconnu/inactif, employé inactif, plus de 5 lignes, marqueur d'origine PWA).
- **Poussé sur `main` à la demande explicite de William** (« Oui, pousse maintenant ») — `immo-proxy` (production) et `immo-proxy-staging` (recette) partagent la même branche `main` (voir `PROCEDURE_RECETTE.md`), donc un seul push déploie les deux simultanément ; pas de mécanisme de "push recette seule" dans ce projet.

## Mode avancé PWA (garant) : transfert inter-dépôts, destination client, bon signé archivé (11 août 2026)

- **Retour terrain immédiat de William**, après avoir testé le premier lot : « un non-ouvrier ou non-CT doit pouvoir faire des transferts inter-sites, donc il doit voir tous les dépôts existants mais aussi la possibilité de mettre un client par exemple, puisque ce sont soit les achats soit la logistique (gestionnaire de dépôt ou atelier) qui font physiquement ces remises. Ce serait bien de pouvoir générer le BL de sortie du matériel, ou un bon de transfert entre deux dépôts internes, afin de faire signer et d'archiver. »
- **Cadrage court avant d'écrire le code** (comme pour le premier lot) : trois questions posées, tranchées par William avant d'implémenter —
  1. **Qui a le mode avancé** → Admin, Logistique, Logistique_Mayotte (recommandé et retenu) : exactement la population qui gère déjà le module au dashboard (`peutGererBrasseurs`), cohérent avec « achats ou logistique » cité par William. RA (illimité en quantité côté écran simple, voir lot précédent) n'a pas ce mode avancé — il n'a aujourd'hui aucun droit d'écriture Brasseurs au dashboard non plus.
  2. **Périmètre du lot** → tout en une fois (recommandé et retenu) : écran étendu + génération du bon + archivage photo, plutôt que scinder en deux sessions.
  3. **Gabarit du document** → **deux gabarits distincts** (tranché par William, à l'inverse de ma recommandation d'un seul gabarit à variantes) : « Bon de livraison » (sortie client/chantier) et « Bon de transfert » (inter-dépôts) — des documents visuellement distincts d'emblée.
- **Étape bloquante communiquée puis confirmée par William avant le code** : nouvelle colonne `Photo_Fiche` (Texte) sur `Brasseurs_Mouvements`, même pattern que `Dotations_EPI.Photo_Fiche`/`Lignes_Outillage.Photo_Fiche` — créée sur les deux sites (production et recette) avant que la partie archivage soit écrite.
- **Nouvelle action `transfert_stock_brasseur_pwa`** (PWA, sans jeton, rôle vérifié côté serveur via une nouvelle fonction `estGarantBrasseurServeur(code)` — relit `Employes.Code_CT`, même mécanisme que le plafond de quantité du lot précédent) : miroir de `transfert_brasseur` (dashboard), deux mouvements liés par ligne, jusqu'à 10 références, garde-fou stock négatif au dépôt source. Un appelant non-garant reçoit `droits_insuffisants` (403), rien n'est écrit.
- **`sortie_stock_brasseur_pwa` étendue** (même action que le lot précédent, pas une nouvelle) : la destination `Négoce` devient acceptée pour un appelant garant, à condition de fournir un nom de client (`Tiers`, sinon `client_requis`) — refusée comme avant pour tout autre rôle. La `Destination` saisie est normalisée à la forme accentuée canonique `Négoce` quelle que soit la casse tapée, pour rester cohérente avec les sorties Négoce créées au dashboard.
- **Nouvelle action `upload_fiche_brasseur`** (PWA, sans jeton, mêmes garde-fous que `upload_photo` — signature JPEG réelle des octets vérifiée, taille max, nom de fichier validé) : écrit la preuve sur le drive `/Fiches_Brasseurs/{document}/{fichier}` puis marque `Photo_Fiche` sur **toutes** les lignes `Brasseurs_Mouvements` partageant ce `Document` (un document peut regrouper plusieurs lignes de référence, cadrage §2 du module). Nouvel endpoint de lecture publique `?fiche_brasseur=` (même confiance que `?fiche_epi=`).
- **Pas d'état "en attente de signature"** comme les fiches EPI : le stock Brasseurs est déjà décrémenté au moment du mouvement (modèle "stock calculé" du module, indépendant de l'archivage) — l'archivage de la preuve est postérieur et non bloquant, un simple bouton "📎 Joindre la preuve signée" sur le panneau de confirmation qui suit la validation.
- **Écran PWA** : bascule Sortie/Transfert (masquée pour les non-garants, qui gardent l'écran simple inchangé du lot précédent) ; en mode Sortie, un bouton "👤 Client" supplémentaire ; en mode Transfert, un second sélecteur de dépôt (destination, exclut automatiquement le dépôt source déjà choisi). Après validation, un panneau de confirmation (au lieu du retour direct à l'accueil du lot précédent) propose "🖨️ Générer le bon" et "📎 Joindre la preuve signée" — accessible à tous les rôles, pas seulement aux garants (un simple technicien peut vouloir imprimer/archiver sa propre sortie).
- **Dashboard** : icône "📎" cliquable à côté du numéro de document dans le journal des mouvements Brasseurs dès que `photo_fiche` est renseigné (lecture ajoutée à `?brasseurs_mouvements=1`), lien direct vers la preuve archivée.
- **Vérifié en navigateur** (recette, `immo-proxy-staging`, écritures interceptées localement pour ne déclencher aucun appel réel) : affichage conditionnel du mode avancé selon le rôle (`estGarant`, vérifié absent pour CT, présent pour Logistique), flux Sortie+Client de bout en bout (payload `destination:'Négoce'`, `tiers` renseigné), flux Transfert de bout en bout (dépôt source/destination corrects), génération des deux gabarits (contenu attendu présent dans chacun), et l'upload de preuve (fichier compressé en JPEG, document correctement associé).
- `npm run verify` : 240/240 (18 nouveaux tests : garde-fous des deux nouvelles actions, extension Négoce+client de l'action existante, validations `upload_fiche_brasseur`).

## Contrat d'export universel + territoire systématique (11 août 2026)

- **Déclencheur** : William a constaté que les exports Excel/CSV des immobilisations n'incluaient jamais le territoire (Réunion/Mayotte), alors que la colonne `Site` existe sur `Immos` depuis juillet 2026. Un audit plus large a montré que les exports de l'application étaient très hétérogènes (CSV vs Excel, filtres tantôt respectés tantôt ignorés, plusieurs onglets sans aucun export).
- **Audit préalable (avant tout code)** :
  - Nom interne de la colonne `Site` vérifié via `?debug_columns=Immos` : `Site`, identique au nom affiché — aucun piège comme `Fabricant`/`Fabriquant` ou `Code_deverouillage`.
  - Qualité de la donnée : aucun endpoint existant ne permettait de compter les immos actives à `Site` vide/invalide sans le masquer (`?immo_metadata=1` remplace silencieusement tout Site vide par `'Reunion'` par défaut, comportement de transition datant de la migration EBP de juillet 2026). Ajout d'un endpoint de diagnostic non authentifié `?audit_site_immos=1` (même convention que les `?debug_*` existants, lecture seule, expose seulement `code_im`+`site` brut) pour lever ce point à l'aveugle. Résultat : **819 immos actives, 667 Réunion / 152 Mayotte, 0 avec un Site vide ou invalide** — aucun arbitrage de données nécessaire avant de continuer. Endpoint conservé (outil de contrôle qualité réutilisable, comme `?debug_columns=`).
  - Recensement de 13 exports existants (dont un seul vrai multi-onglets Excel via SheetJS, les 12 autres en CSV texte) et des onglets sans aucun export (Vue générale, Circulation, Dépôt, Transferts, Réservations, Absences, Alertes, Historique, Documents, Maintenance, et plusieurs sous-vues EPI/Outillage/Brasseurs).
- **Contrat retenu** — socle garanti sur tout export d'immobilisations, dans cet ordre (`SOCLE_IMMO_ENTETES` dans `dashboard.html`) :

  `Code IM | Libellé | Catégorie | État | Valeur d'achat (€) | Détenteur actuel | Chantier actuel | Territoire | Date du dernier mouvement`

  suivi des colonnes propres au module (Note, FDS, coût de réparation, N° série...). Pour les modules non-immos, un socle équivalent adapté à la donnée réellement disponible (identifiant, libellé, détenteur/employé, territoire quand il existe, dates pertinentes) — documenté cas par cas ci-dessous.
- **« Chantier actuel » n'existe pas comme champ propre à une immo** dans ce modèle de données (seulement un détenteur, une personne) — point signalé à William avant d'écrire le code. Résolu en le déduisant de la réservation en cours liée à cette immo (`Reservations.Nom_Chantier`) si elle existe, sinon laissé vide plutôt qu'inventé (`chantierActuelImmo()`).
- **Territoire absent par construction sur les catalogues sans dimension site** (EPI/Outillage : `Catalogue_Articles_EPI`, `Catalogue_Outillage`, grilles de dotation) — un article n'a pas de site propre, seul l'employé qui le reçoit en a un. Ces exports gardent donc leurs colonnes actuelles sans Territoire fabriqué ; en revanche Territoire a été ajouté partout où l'entité exportée a réellement un site (Dotations EPI et Prime annuelle Outillage via l'employé, Matériel IT/Lignes téléphoniques, Brasseurs via le dépôt, Absences, Inventaire stock/immos, export comptable).
- **Implémentation** :
  - Une fonction générique unique `exporterExcel(nomFichier, ongletsSpec)` (SheetJS, version 0.18.5 inchangée) remplace tous les anciens exports CSV (`Blob`+BOM) — un seul point de génération de classeur multi-onglets pour toute l'application.
  - `socleImmoLigne(codeIM)` construit le socle pour une immo à partir des données déjà chargées (`im`, `immoMeta`, `d.inventory`, `reservations`) — aucun nouvel appel Worker.
  - Chaque export reflète les filtres actifs de sa vue au moment du clic et ajoute un onglet/une ligne "Filtres actifs" (`ligneFiltresActifs()`) pour ne jamais laisser un fichier restreint se faire passer pour complet.
  - L'export global (bouton d'en-tête "📥 Excel") et les exports dédiés ont été ajoutés/mis à niveau sur : Vue générale (nouveau, avec filtre Territoire — seul endroit où il manquait, Circulation/Dépôt/Historique/Transferts/Réservations/Maintenance/Alertes en disposaient déjà), Circulation, Dépôt, Historique, Transferts, Réservations, Documents, Maintenance, Absences, Alertes, Inventaire stock, Inventaire immos (scan), EPI (Stock, Dotations, Suggestion de commande), Outillage (Stock, Suggestion de commande, Reste à distribuer, Prime annuelle, Consommation), Matériel IT, Lignes téléphoniques, Brasseurs d'air (Stock, Commandes), export comptable.
  - **Non couvert dans cette session** (à traiter dans une session dédiée si le besoin se confirme) : les grilles de configuration EPI/Outillage (tableaux de référence, pas des listes filtrées) et la vue "Remise ponctuelle"/"Distribution par employé" (formulaires d'action, pas des listes à exporter en l'état) — jugés hors du périmètre naturel d'un export tant qu'ils ne présentent pas de données déjà filtrées à l'écran.
- **Sécurité** : aucune nouvelle action Worker créée (tous les exports sont en lecture, à partir de données déjà chargées côté client). Vérifié explicitement que les exports Matériel IT/Lignes téléphoniques (données PIN/PUK/RIO) n'opèrent que sur `materielItList`/`lignesTelList`, peuplées exclusivement via les endpoints `?materiel_it=1`/`?lignes_telephoniques=1` déjà protégés `requireGarant` depuis l'audit de sécurité d'août 2026 — aucun contournement possible.
- **Vérifié** : `npm run verify` (240/240 tests, aucun nouveau test requis — changements purement client-side sans nouvelle action Worker gated) ; chargement de `dashboard.html` dans un navigateur sans erreur console jusqu'à l'écran de connexion. La vérification interactive complète des exports (nécessite une session employé réelle contre le Worker de production) n'a pas été faite dans cette session — à valider par William en conditions réelles.

## Correctifs visuels : mur de couleur warn-row sur les tables de stock, toast illisible en sombre, palette sombre éclaircie (11 août 2026)

- **Signalé par William avec capture d'écran** (onglet Brasseurs d'air → Stock) : quand la majorité des références d'un catalogue est sous son seuil d'alerte (cas typique d'un module tout juste déployé, stock encore à 0 partout), la table entière se transformait en un « mur » de lignes orange avec une bordure gauche colorée sur chacune — lisait comme un bug plutôt que comme une alerte utile.
- **Cause identifiée en reproduisant l'écran avec des données factices dans le navigateur** (injection directe de `brasseursCatalogue`/`brasseursStock` puis appel du rendu réel, sans passer par une vraie session) : les 3 tables de stock (EPI, Outillage, Brasseurs) appliquaient **à la fois** un badge coloré par ligne (`bn`/`bu`/`ba`, déjà suffisant pour signaler l'alerte) **et** la classe `warn-row` sur la ligne entière (fond + bordure gauche) dès qu'un article était sous le seuil — double signal, dont le second devient écrasant quand une majorité d'articles est concernée (`--ds-warning-bg`/`--ds-warning` appliqués en masse). Corrigé en retirant `warn-row` de ces 3 rendus (`renderEPIStock`/`refreshEPIStockResults`, `renderOutilStock`, `renderBrasseursStockPivot`) — le badge par ligne reste seul signal, cohérent avec le reste de l'application où `warn-row`/`urgent-row` restent réservés à des listes d'actions ponctuelles et naturellement bornées (pannes, transferts en retard, garanties, candidats à la réforme), jamais à un état de catalogue pouvant concerner 100% des lignes.
- **Bug distinct trouvé en vérifiant le mode sombre** : les toasts (`.ds-toast`, remplaçant `alert()`) utilisaient `background:var(--ds-neutral-800)` — correct en mode clair (alias de `--ds-ink-800`, sombre), mais l'échelle `--ds-neutral-*` est **volontairement inversée** en mode sombre (pour que les badges restent lisibles) : `--ds-neutral-800` y devient presque blanc, donc un toast avec un fond quasi blanc et un texte blanc codé en dur (`color:#fff`) — illisible. Corrigé en fixant le fond du toast sur `var(--ds-ink-800)` (jamais réécrit par le bloc `[data-theme="dark"]`), qui reste une « puce » sombre dans les deux thèmes, cohérent avec son texte blanc fixe.
- **Retour direct de William sur le mode sombre existant** : « trop sombre », pas assez pensé comme un miroir du mode clair. Éclairci l'ensemble des tokens de surface en mode sombre (`--ds-bg`, `--ds-surface`, `--ds-surface-alt`, `--ds-border`, `--ds-border-strong`, `--ds-text-muted`, et les fonds `-bg` des badges sémantiques success/warning/danger/info, plus `--ds-gold-50` utilisé pour le survol de ligne) — passage d'un charbon proche du noir à un charbon plus clair, tout en gardant `--ds-text` (#EDEFF2) inchangé : le contraste texte/fond reste largement confortable après l'éclaircissement. L'échelle `--ds-neutral-50..200` (utilisée par les badges/survols neutres) a été réalignée sur ces nouvelles valeurs pour rester cohérente.
- **Vérifié dans le navigateur** (données factices injectées directement en JS, aucune session réelle nécessaire) : les 3 tables de stock ne montrent plus de mur de couleur (zébrage normal + badge par ligne uniquement), le toast affiche un fond sombre fixe lisible dans les deux thèmes, et les nouveaux tokens de fond sombre sont bien appliqués (`--ds-bg` #1D2128 au lieu de #15181E, etc.).
- `npm run verify` : 240/240 (changements CSS/JS de présentation uniquement, aucun comportement métier modifié, aucun nouveau test nécessaire).

## Zébrage renforcé + barres de navigation roses (11 août 2026)

- **Retour de William après le correctif précédent** : (1) le contraste entre lignes de tableau reste trop faible, à renforcer encore un peu ; (2) faire tendre la couleur des barres de navigation (header/sidebar/bottom-nav) vers le rose, avec une teinte différente entre les deux thèmes plutôt qu'une couleur fixe identique en clair et en sombre (jusqu'ici `--blue-chrome`, marine, volontairement invariable).
- **Zébrage** : nouveau token dédié `--ds-zebra` (au lieu de réutiliser `--ds-surface-alt`, qui sert aussi aux en-têtes de tableau et aux survols — le changer aurait assombri ces usages sans rapport). Clair : `#EDE7DC` (gris chaud nettement visible, contre `#FAFAF8` quasi blanc avant). Sombre : `#333A45` (contre `#2E343E`, écart plus marqué avec `--ds-surface`).
- **Barres de navigation** : nouveau token `--ds-nav-bg`, distinct de `--blue-chrome` (qui reste inchangé et continue d'habiller les boutons d'action — des centaines d'usages dans `dashboard.html`, hors périmètre de la demande qui portait explicitement sur « les barres de navigation »). Appliqué uniquement à `#header`, `#sidebar`, `#bottom-nav` et `#mobile-menu` (menu "Plus" mobile, extension du bottom-nav) — jamais à l'écran de connexion (`#pin-overlay`) ni au panneau de fiche (`#fiche-hd`), qui ne sont pas des barres de navigation. Clair : `#7A1F44` (rose/bordeaux, contraste confortable avec le texte blanc déjà en place). Sombre : `#3D0F26` (même famille de teinte, mais sombre — crée un vrai rapport clair/sombre entre les deux thèmes, alors que la couleur était auparavant strictement identique dans les deux).
- **Vérifié dans le navigateur** (styles calculés, sans session réelle) : `#header`/`#sidebar` passent bien à `rgb(122,31,68)` en clair et `rgb(61,15,38)` en sombre ; une ligne zébrée de test passe à `rgb(237,231,220)` en clair et `rgb(51,58,69)` en sombre.
- `npm run verify` : 240/240 (CSS uniquement, aucun comportement modifié).

## Onglet CODIR / Pilotage direction (11 août 2026)

- **Contexte** : la direction d'Espace Soleil doit arbitrer entre Immo Tracker et des solutions du marché (Hector, Organilog) — voir `00_CONTEXTE_PROJET.md`. Demande de William : un écran de pilotage chiffré, argument différenciant qu'aucun SaaS générique ne peut offrir sur mesure, appuyé sur les données réelles déjà suivies par l'app (pas de nouvelle saisie).
- **Prérequis** : réutilise directement le contrat d'export universel et le filtre Territoire de la session précédente (`exporterExcel`, `SOCLE_IMMO_ENTETES`, `territoireLabel`, `ligneFiltresActifs`, pattern `SITE_FILTERS`/`setSiteFilter`) — aucune nouvelle mécanique d'export inventée.
- **Droits — décision volontairement plus stricte que les autres onglets view-only** : `peutVoirCodir()` limite l'accès à Admin + Encadrement, **sans Logistique** (contrairement à `peutVoirCompta`/`peutVoirEPI`/`peutVoirOutillage`, qui incluent tous Logistique). Raison : c'est un écran de pilotage direction, pas un outil de gestion courante — Logistique n'a pas besoin des montants agrégés du parc pour son travail quotidien. Vérifié en navigateur avec des identités factices : rôle Logistique bloqué, Admin et Encadrement passent.
- **Réutilisation quasi totale des données déjà chargées** : Bloc 1 (valeur du parc) et l'essentiel des Blocs 2/3 sont recalculés côté client à partir de `d` (`?dashboard=1`), `immoMeta` (`?immo_metadata=1`) et `reservations` (`?reservations=1`), déjà en mémoire pour le reste du dashboard — aucun nouvel appel Worker pour ces parties. Les candidats à la réforme réutilisent les mêmes fonctions pures que l'onglet Analyses (`ratiosReforme`, `franchitSeuilReforme`, `coutReparationMouvement`, `estImmoActivite`, `SEUIL_REFORME_PCT`) via une nouvelle fonction `calculerCandidatsReforme()` qui **recalcule indépendamment** la liste (mêmes critères, même tri) plutôt que d'appeler `renderAnalyse()` — décision volontaire pour ne rien risquer sur un onglet déjà en production, au prix d'une trentaine de lignes dupliquées mais isolées.
- **Seule donnée manquante : le délai moyen de validation des transferts (Bloc 3)** — `Transferts_En_Attente` ne conserve aucun identifiant commun avec le `Mouvement` créé par l'action `valider` (voir `worker.js`), donc l'historique complet (tous statuts, y compris déjà validés) n'était nulle part disponible côté client. Nouvel endpoint GET **`?codir=1`**, en lecture seule, qui renvoie uniquement `transferts_historique` (code IM, statut, date de création) — le reste de l'onglet ne dépend d'aucun autre nouvel endpoint. Le délai est ensuite **estimé** côté client en appariant chaque demande validée (`created`) au Mouvement Transfert/Retour du même code IM survenu juste après (fenêtre de 30 jours) — heuristique assumée et affichée comme telle (compteur "N/M appariés" visible à l'écran), faute d'identifiant commun entre les deux listes. Vérifiée avec un jeu de données factice reproduisant un cas de délai connu (~27h) : le calcul retombe exactement sur la valeur attendue.
- **Protection de `?codir=1` — décision explicitement demandée par le cadrage** : évaluée et **alignée sur ce que voit déjà l'onglet Analyses**, c'est-à-dire aucun jeton — `?dashboard=1`/`?immo_metadata=1` (qui exposent déjà des montants, des noms de détenteurs) sont eux-mêmes consommés sans authentification par Analyses depuis leur création. `?codir=1` n'expose que des codes IM/statuts/dates, strictement moins sensible. Pas un nouveau palier de confidentialité, donc pas de régression de sécurité introduite. Budget sous-requêtes : 6 pages max (`paginate(..., 6)`), largement sous le plafond de 45 déjà en place ailleurs dans le Worker.
- **Taux de sortie annuel — écart avec le vocabulaire du cadrage** : le document de cadrage direction parle de sorties "HS/Vol/Cession", mais l'app n'a jamais implémenté de motif "Cession" (`ouvrirModalSortie` ne propose que Hors service définitif / Perdu / Volé / Disparu). Plutôt que d'inventer un motif inexistant dans les données, le calcul utilise les 4 motifs réellement enregistrés (`CODIR_ETATS_SORTIE`) et l'écart est signalé explicitement dans l'interface ("pas de motif « Cession » implémenté à ce jour").
- **Dénominateur du taux de sortie** : `parc de référence = parc actif actuel + immos sorties sur la période`, pas seulement le parc actuel — sinon le taux se retrouve artificiellement gonflé (le dénominateur ne doit pas être amputé de ce qu'on compte au numérateur).
- **ROI (Bloc 4) — méthode actée par William avant développement** : séparation stricte entre (1) les éléments **factuels** déjà suivis par l'app (coût des réparations, valeur des immos réformées à temps sur 12 mois glissants — présentés comme preuve de valeur, jamais additionnés à un gain, car un coût suivi n'est pas un gain) et (2) une **hypothèse chiffrée explicitement déclarée à l'écran** : nombre de mouvements + réservations non annulées enregistrés dans l'app (proxy assumé pour "l'app a évité une recherche physique", faute de télémétrie réelle sur l'usage de la fonction Rechercher) × 15 minutes économisées (`CODIR_MIN_PAR_RECHERCHE`) × un taux horaire **paramétrable à l'écran** (`CODIR_TAUX_HORAIRE`, défaut 25 €/h via `CODIR_TAUX_HORAIRE_DEFAUT`). Le texte de l'hypothèse reste visible en permanence à côté du chiffre, jamais masqué — cohérent avec le principe déjà appliqué ailleurs dans le projet ("un chiffre honnête avec hypothèse visible est plus crédible qu'un chiffre gonflé").
- **Écarts d'inventaire résolus (partie factuelle du ROI)** : volontairement affiché "n/d" plutôt que recalculé cette session — la logique de rapprochement (scans vs localisation théorique) vit dans l'onglet Inventaire immos et n'est chargée qu'à son ouverture ; reproduire ce calcul ici aurait doublé une logique déjà en place pour un point secondaire du ROI, hors du périmètre demandé (comparaison de coût direct vs marché, seul élément vraiment décisif).
- **Comparaison de coût** : chiffres repris tels quels du cadrage (< 200 €/an Immo Tracker vs 500-1500 €/mois marché), affichés en mensuel et en annuel pour la lisibilité — pas recalculés depuis la Note de synthèse direction (document Word, pas relu cette session).
- **Impression** : feuille `@media print` dédiée à `#pane-codir` (masque sidebar/header/filtres, force l'affichage de l'onglet CODIR même si un autre onglet est actif à l'écran au moment d'un Ctrl+P général — seul ce bouton `window.print()` déclenche ce cas), compacte les cartes/tableaux/graphiques pour tenir sur 1-2 pages A4. Aucune autre vue du dashboard n'a de mise en page d'impression dédiée à ce jour.
- **Vérifié** : `npm run verify` (240/240, aucun test cassé — nouvel endpoint GET non gated, donc hors périmètre du garde-fou `security.gated-actions.test.js` qui ne suit que les actions POST). Vérification fonctionnelle faite en navigateur avec des données factices injectées directement en JS (immos/mouvements/réservations/employés fabriqués, sans toucher au Worker de production sauf l'appel `?codir=1` lui-même, en lecture seule) : les 4 blocs s'affichent sans erreur console, tous les calculs recoupés à la main (valeur par territoire, circulation/dépôt, candidats réforme, taux de sortie à 365 jours strict, employés actifs 30j, délai de validation apparié, ROI) tombent juste ; filtre Territoire, tri des colonnes et export Excel testés sans erreur. Pas de session réelle contre le Worker de production dans cette session (comme les sessions précédentes de ce type) — à valider par William en conditions réelles, notamment le rendu d'impression (dépend du navigateur/imprimante, non simulable ici).

## Correctifs onglet CODIR → « Synthèse direction » : nom et chiffres marché réels (12 août 2026)

- **Retour de William après premier essai** : (1) le nom "CODIR" jugé trop pompeux et faisant doublon avec l'onglet "Analyses" existant ; (2) les montants de comparaison marché du Bloc 4 ("500-1500 €/mois") étaient **inventés**, pas sourcés — William a transmis le vrai devis Hector reçu.
- **Renommage** : "CODIR" → **"Synthèse direction"** (choix validé avec William parmi plusieurs options). Changement limité aux libellés visibles (nav-item, entrée menu mobile, titre d'impression, nom du fichier Excel exporté) — les identifiants internes (`renderCodir`, `CODIR_*`, `pane-codir`, endpoint `?codir=1`) sont restés inchangés, un renommage purement cosmétique ne justifiant pas de toucher au code fonctionnel déjà vérifié.
- **Chiffres marché corrigés à partir d'un vrai devis** : devis Hector reçu par William (`HECTOR-DEV-008757`, daté du 25/06/2026) pour l'abonnement "Armure" palier **501-1000 actifs** (correspondant au parc réel, ~1023 immos) :
  - Abonnement annuel avec 10% de remise : **2 003,88 €/an** (≈ 167 €/mois) — la remise (-200,39 €) s'applique uniquement à cette ligne.
  - Frais uniques, année 1 seulement : activation 29,99 € + banque de 6h d'accompagnement/formation 669 € = **≈ 699 €** (n'est pas un coût récurrent).
  - Total 1ère année tout compris (devis) : **3 002,48 €**. À partir de l'année 2 : uniquement l'abonnement récurrent (~2 004 €/an), sauf option supplémentaire.
  - Le tableau distingue désormais explicitement coût récurrent et coût de mise en route (une fois), pour ne pas mélanger les deux comme le faisait le chiffre inventé précédent.
- **Organilog non chiffré, volontairement** : aucun devis Organilog n'a été obtenu à ce jour (contrairement à Hector) — plutôt que de réutiliser le tarif public par utilisateur (19-59 €/utilisateur/mois, mentionné dans `00_CONTEXTE_PROJET.md`) sans savoir s'il s'applique tel quel à l'échelle d'Espace Soleil, la ligne Organilog affiche "non chiffré — pas de devis reçu" à la place d'un montant. Leçon retenue de l'incident précédent : ne jamais présenter une estimation non vérifiée comme un chiffre au même niveau de confiance qu'un devis réel.
- **Vérifié** : `npm run verify` (240/240) ; rendu vérifié en navigateur (données factices) — nouveaux libellés ("Synthèse direction" partout) et nouveau tableau de comparaison (Immo Tracker / Hector / Organilog) affichés correctement, aucune erreur console.

## Lisibilité de la navigation, logo, session expirée et service worker (12 août 2026)

- **Retour de William** : (1) en mode clair, le fond des barres de navigation (`--ds-nav-bg`, rose/bordeaux depuis la refonte du 11 août) rendait les en-têtes de section du menu ("TABLEAU DE BORD", "INVENTAIRE"...) difficiles à lire ; (2) le logo du header doit être la version sur fond blanc (`logo.jpg`), en un peu plus grand ; (3) mode sombre, lisibilité à améliorer un peu sur ce même secteur ; (4) en ouvrant Brasseurs d'air → Commandes, popup "🔒 Accès commandes réservé aux gestionnaires dépôt" alors que connecté en Admin ; (5) erreurs console (`sw.js` sur `staging/employes.json`, 401 sur `?brasseurs_commandes=1`/`?brasseurs_lignes_commande=1`).
- **Nav-group** : la cause du point (1) était surtout l'opacité du texte des en-têtes de section (`.nav-group`, `rgba(255,255,255,.35)`) — réglée à l'origine pour un fond très sombre (marine), devenue insuffisante contre le rose plus clair introduit la veille. Remonté à `.78`. `--ds-nav-bg` légèrement éclairci en parallèle dans les deux thèmes (`#7A1F44`→`#9C3D63` clair, `#3D0F26`→`#5A2038` sombre) — répond aux points (1) et (3) en un seul changement, puisque la sidebar utilise le même texte blanc translucide dans les deux thèmes (seul le fond change).
- **Logo** : `logo-dark.jpg` (fond charbon, texte "ÉLECTRICITÉ SERVICES" en noir — illisible sur un fond déjà sombre) remplacé par `logo.jpg` (fond blanc, confirmé visuellement) dans le header ; hauteur `30px`→`38px`, léger padding/coin arrondi blanc pour qu'il se détache proprement du bandeau coloré plutôt que de coller un rectangle brut au bord.
- **Bug réel trouvé en creusant le point (4)** : la vraie cause n'était **pas** un problème de droits (le compte AIWI est bien Admin), mais un **jeton de session expiré** (12h) — le message affiché ("réservé aux gestionnaires dépôt") était trompeur, écrit pour ne réagir qu'à un vrai refus de droits mais en réalité déclenché sur **toute** erreur retournée par `?brasseurs_commandes=1`, y compris `session_invalide`. Le même défaut existait en silence sur `?materiel_it=1` (aucun message du tout dans ce cas, juste une liste vide sans explication) et sur `?journal_audit=1` — plus un bug latent distinct : ces deux derniers appelaient une fonction `toast(...)` qui n'existe pas (seule `showToast(...)` existe), donc le message, même dans le bon cas, ne s'affichait jamais (exception silencieusement avalée par le `try/catch` englobant).
- **Corrigé à la racine plutôt qu'au cas par cas** : nouveau point unique `gererSessionExpiree()`, déclenché depuis l'interception `window.fetch` déjà en place (celle qui injecte le jeton sur les actions POST gated) — étendue pour inspecter aussi la **réponse** de tout appel vers le Worker (POST gated ou GET avec jeton en paramètre d'URL) et détecter un 401 `{error:'session_invalide'}`. Affiche un toast "Session expirée — merci de te reconnecter" et rouvre l'écran de connexion (`changerIdentite()`), avec un cooldown de 4s pour ne pas spammer si plusieurs appels échouent au même instant. `chargerBrasseursCommandes`/`loadMaterielIT`/`loadJournalAudit` ne montrent plus leur message "droits insuffisants" que pour ce cas précis (`error==='droits_insuffisants'`), plus les deux appels `toast(...)` cassés corrigés en `showToast(...)`.
- **`sw.js`** : la stratégie stale-while-revalidate de `immos.json`/`employes.json` pouvait résoudre `event.respondWith()` à `null` (aucune version en cache **et** échec réseau — hors-ligne ou, comme ici, un aléa réseau sur `staging/employes.json`), ce que le navigateur ne peut pas convertir en `Response` (`Failed to convert value to 'Response'`, "the promise was rejected"). Même risque, non encore observé, sur le repli de la coquille applicative (`catch` final si ni le cache de la page ni `index.html` ne sont disponibles). Corrigé aux deux endroits : toujours renvoyer une vraie `Response` (503 explicite) plutôt que `null`/`undefined`.
- **Vérifié** : `npm run verify` (240/240) ; en navigateur — couleurs de fond recalculées pour les deux thèmes (`#9C3D63`/`#5A2038` confirmés), logo et taille confirmés, `gererSessionExpiree()` testée directement (toast + réouverture de connexion + cooldown, tous corrects), le nouveau branchement `droits_insuffisants` vs `session_invalide` de `chargerBrasseursCommandes` vérifié sur les deux cas. Test réseau réel contre le Worker de production bloqué par CORS depuis ce bac à sable (origine non whitelistée) — confirme au passage que la protection CORS de l'audit de sécurité fonctionne comme prévu, mais n'a pas permis un test de bout en bout avec un vrai jeton expiré ; à confirmer par William en conditions réelles.

## Comment utiliser ce journal
Ajouter une entrée à chaque décision structurante : la date approximative, ce qui a été décidé, et surtout **pourquoi** (le contexte qui a motivé le choix). Ne pas y mettre le détail technique (qui vit dans le code et les autres documents) mais le raisonnement métier.
